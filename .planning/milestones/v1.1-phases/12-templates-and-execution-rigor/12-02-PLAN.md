---
phase: 12-templates-and-execution-rigor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done-r/templates/context.md
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-03, WKFL-05]

must_haves:
  truths:
    - "context.md template includes canonical_refs section for downstream agent references"
    - "TMPL-01 is documented as N/A (no upstream agents/ directory)"
    - "TMPL-03 is documented as N/A (no upstream hooks/ directory)"
    - "WKFL-05 research-project/SUMMARY.md is already current (no merge needed)"
    - "Shared templates that are namespace-only or research-customized are confirmed current"
  artifacts:
    - path: "get-shit-done-r/templates/context.md"
      provides: "Context template with canonical_refs section"
      contains: "<canonical_refs>"
  key_links:
    - from: "get-shit-done-r/templates/context.md"
      to: "gsd-r-planner agent"
      via: "Template consumed during discuss-phase to produce CONTEXT.md files"
      pattern: "canonical_refs"
---

<objective>
Add canonical_refs section to context.md template and close out the remaining Phase 12 requirements (TMPL-01 N/A, TMPL-02 shared template audit, TMPL-03 N/A, WKFL-05 SUMMARY.md already current).

Purpose: Complete template sync by adding the one substantive template improvement (canonical_refs) and formally documenting that the other requirements are satisfied or N/A.
Output: Updated context.md with canonical_refs, verification that remaining requirements need no changes.
</objective>

<execution_context>
@~/.claude/get-shit-done-r/workflows/execute-plan.md
@~/.claude/get-shit-done-r/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-templates-and-execution-rigor/12-CONTEXT.md
@.planning/phases/12-templates-and-execution-rigor/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical_refs section to context.md template</name>
  <files>get-shit-done-r/templates/context.md</files>
  <read_first>get-shit-done-r/templates/context.md, ~/.claude/get-shit-done/templates/context.md</read_first>
  <action>
Edit the GSD-R context.md template to add a `<canonical_refs>` section. This section goes AFTER the `</specifics>` closing tag and BEFORE the `<code_context>` opening tag (in the File Template code block).

Insert the following between `</specifics>` and `<code_context>`:

```
<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

[List every spec, ADR, feature doc, or design doc that defines requirements or constraints for this phase. Use full relative paths so agents can read them directly. Group by topic area when the phase has multiple concerns.]

### [Topic area 1]
- `path/to/spec-or-adr.md` -- [What this doc decides/defines that's relevant]
- `path/to/doc.md` section N -- [Specific section and what it covers]

### [Topic area 2]
- `path/to/feature-doc.md` -- [What capability this defines]

[If the project has no external specs: "No external specs -- requirements are fully captured in decisions above"]

</canonical_refs>
```

Also update the downstream consumers note near the top of the file. The current text references `gsd-r-phase-researcher` and `gsd-r-planner`. Add a note that canonical_refs are consumed by all downstream agents:

Change the downstream consumers section to:
```
**Downstream consumers:**
- `gsd-r-phase-researcher` -- Reads decisions to focus research (e.g., "card layout" -> research card component patterns)
- `gsd-r-planner` -- Reads decisions to create specific tasks (e.g., "infinite scroll" -> task includes virtualization)
- All downstream agents -- Read canonical_refs for specs, ADRs, and design docs that constrain implementation
```

Also add to the guidelines section at the bottom (inside `<guidelines>` tags):
```
**Canonical References:**
- List specs, ADRs, and design docs that downstream agents must read
- Use full relative paths so agents can read them directly
- Group by topic area when the phase has multiple concerns
- If no external specs exist, state explicitly: "No external specs"
- NEVER silently omit this section -- even "no specs" is useful information
```

Do NOT change any existing content in the examples sections. The examples predate canonical_refs and that is fine -- they serve as examples of the decisions pattern. Do NOT change the `gsd-r-*` agent names to `gsd-*`.
  </action>
  <verify>
    <automated>grep -c 'canonical_refs' get-shit-done-r/templates/context.md</automated>
    <automated>grep -c 'gsd-r-' get-shit-done-r/templates/context.md</automated>
  </verify>
  <acceptance_criteria>
    - context.md contains `<canonical_refs>` at least 2 times (open tag in template + reference in guidelines)
    - context.md contains `</canonical_refs>` at least 1 time (close tag in template)
    - context.md contains the string `Downstream agents MUST read these` inside the canonical_refs section
    - context.md contains `NEVER silently omit` in the guidelines section
    - context.md contains `gsd-r-phase-researcher` (GSD-R agent name preserved, not upstream `gsd-phase-researcher`)
    - context.md contains `gsd-r-planner` (GSD-R agent name preserved)
    - context.md still contains `<decisions>` section (not removed)
    - context.md still contains `<specifics>` section (not removed)
    - context.md still contains `<deferred>` section (not removed)
    - context.md still contains `<code_context>` section (not removed)
    - The `<canonical_refs>` section appears AFTER `</specifics>` and BEFORE `<code_context>` in the template
  </acceptance_criteria>
  <done>context.md template includes canonical_refs section between specifics and code_context, with guidelines for its use, and all GSD-R naming preserved</done>
</task>

<task type="auto">
  <name>Task 2: Verify remaining requirements and document N/A status</name>
  <files>get-shit-done-r/templates/context.md</files>
  <read_first>get-shit-done-r/templates/research-project/SUMMARY.md, ~/.claude/get-shit-done/templates/research-project/SUMMARY.md, .planning/phases/12-templates-and-execution-rigor/12-RESEARCH.md</read_first>
  <action>
This task verifies the remaining Phase 12 requirements and confirms their status. No file modifications needed -- this is a verification-only task.

**TMPL-01 (Agent prompts sync): N/A**
Verify: Run `ls ~/.claude/get-shit-done/agents/ 2>/dev/null` to confirm upstream has no agents directory. If confirmed empty/missing, TMPL-01 is N/A as documented in RESEARCH.md.

**TMPL-02 (Template files sync): Already current**
Verify the research findings from 12-RESEARCH.md that shared templates are namespace-only or research-customized:
- Run `diff get-shit-done-r/templates/VALIDATION.md ~/.claude/get-shit-done/templates/VALIDATION.md 2>/dev/null | head -20` -- should show only namespace differences (gsd-r vs gsd)
- Run `diff get-shit-done-r/templates/DEBUG.md ~/.claude/get-shit-done/templates/DEBUG.md 2>/dev/null | head -20` -- should show only namespace differences
- Run `diff get-shit-done-r/templates/UAT.md ~/.claude/get-shit-done/templates/UAT.md 2>/dev/null | head -20` -- should show only namespace differences
- The substantive content changes (context.md canonical_refs, phase-prompt.md rigor fields) are handled by Task 1 and Plan 01. Remaining shared templates need no content merge.

**TMPL-03 (Hook sync): N/A**
Verify: Run `ls ~/.claude/get-shit-done/hooks/ 2>/dev/null` to confirm upstream has no hooks directory. If confirmed, TMPL-03 is N/A as documented.

**WKFL-05 (Research-project templates): Already current**
Verify: Compare get-shit-done-r/templates/research-project/SUMMARY.md with upstream. The headings differ intentionally (Research Landscape vs Recommended Stack, Research Questions vs Expected Features, etc.) per user decision to keep GSD-R's research-focused set. Confirm no structural improvements in upstream worth merging (both have same template/guidelines structure).

Document all verification results as commit message notes when committing.
  </action>
  <verify>
    <automated>ls ~/.claude/get-shit-done/agents/ 2>/dev/null; echo "exit:$?"</automated>
    <automated>ls ~/.claude/get-shit-done/hooks/ 2>/dev/null; echo "exit:$?"</automated>
    <automated>diff get-shit-done-r/templates/research-project/SUMMARY.md ~/.claude/get-shit-done/templates/research-project/SUMMARY.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls ~/.claude/get-shit-done/agents/` returns error or empty (confirming TMPL-01 N/A)
    - `ls ~/.claude/get-shit-done/hooks/` returns error or empty (confirming TMPL-03 N/A)
    - `diff` of VALIDATION.md, DEBUG.md, UAT.md show only namespace differences (gsd-r vs gsd), not content differences
    - research-project/SUMMARY.md files differ in headings but share identical structure (both have Executive Summary, Key Findings, Implications for Roadmap, Confidence Assessment, Sources sections)
  </acceptance_criteria>
  <done>TMPL-01 confirmed N/A (no upstream agents/), TMPL-02 shared templates confirmed current (namespace-only diffs), TMPL-03 confirmed N/A (no upstream hooks/), WKFL-05 SUMMARY.md confirmed current (intentional heading differences, identical structure)</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -c 'canonical_refs' get-shit-done-r/templates/context.md` returns >= 2
- [ ] `grep 'NEVER silently omit' get-shit-done-r/templates/context.md` finds a match
- [ ] `ls ~/.claude/get-shit-done/agents/ 2>/dev/null` confirms no upstream agents dir
- [ ] `ls ~/.claude/get-shit-done/hooks/ 2>/dev/null` confirms no upstream hooks dir
- [ ] `node --test test/*.test.cjs` passes (no regressions)
</verification>

<success_criteria>
- context.md template includes canonical_refs section with downstream agent guidance
- TMPL-01 verified N/A (no upstream agents directory)
- TMPL-02 shared templates verified current (namespace-only differences)
- TMPL-03 verified N/A (no upstream hooks directory)
- WKFL-05 research-project/SUMMARY.md verified current (intentional heading differences)
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-templates-and-execution-rigor/12-02-SUMMARY.md`
</output>

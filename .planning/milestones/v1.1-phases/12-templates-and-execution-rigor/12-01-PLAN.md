---
phase: 12-templates-and-execution-rigor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done-r/templates/phase-prompt.md
  - get-shit-done-r/workflows/execute-plan.md
autonomous: true
requirements: [EXEC-01, EXEC-02]

must_haves:
  truths:
    - "phase-prompt.md task template includes read_first field for executor pre-reading"
    - "phase-prompt.md task template includes acceptance_criteria field for grep-verifiable conditions"
    - "execute-plan.md has mandatory read_first gate in execute step"
    - "execute-plan.md has mandatory acceptance_criteria check in execute step"
    - "execute-plan.md has precommit_failure_handling section"
    - "execute-plan.md has node-repair-aware verification_failure_gate"
    - "GSD-R research src blocks are preserved in phase-prompt.md"
    - "GSD-R agent names (gsd-r-*) and paths (get-shit-done-r) are preserved in execute-plan.md"
  artifacts:
    - path: "get-shit-done-r/templates/phase-prompt.md"
      provides: "Task XML template with read_first + acceptance_criteria + src blocks"
      contains: "<read_first>"
    - path: "get-shit-done-r/workflows/execute-plan.md"
      provides: "Execution workflow with rigor gates and node-repair"
      contains: "MANDATORY read_first gate"
  key_links:
    - from: "get-shit-done-r/templates/phase-prompt.md"
      to: "get-shit-done-r/workflows/execute-plan.md"
      via: "phase-prompt defines fields, execute-plan enforces them"
      pattern: "read_first.*acceptance_criteria"
---

<objective>
Add execution rigor fields (read_first, acceptance_criteria) to the phase-prompt task template and add mandatory enforcement gates plus precommit handling and node-repair to execute-plan.md.

Purpose: Bring GSD-R's execution quality to upstream v1.24.0 parity -- executors will be forced to read source files before editing and verify grep-checkable criteria after each task.
Output: Updated phase-prompt.md with new task XML fields, updated execute-plan.md with 4 new content blocks.
</objective>

<execution_context>
@~/.claude/get-shit-done-r/workflows/execute-plan.md
@~/.claude/get-shit-done-r/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-templates-and-execution-rigor/12-CONTEXT.md
@.planning/phases/12-templates-and-execution-rigor/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add read_first and acceptance_criteria to phase-prompt.md task template</name>
  <files>get-shit-done-r/templates/phase-prompt.md</files>
  <read_first>get-shit-done-r/templates/phase-prompt.md, ~/.claude/get-shit-done/templates/phase-prompt.md</read_first>
  <action>
Modify the GSD-R phase-prompt.md to add two new XML fields to the task template. Do NOT overwrite the file -- edit the existing GSD-R version in place. The GSD-R file has research-specific `<src>` blocks that upstream lacks -- these MUST be preserved.

**In the File Template section (the markdown code block starting with frontmatter):**

1. In the first `<task type="auto">` example (around line 63-69), change it to include the new fields. The field order must be: `<name>`, `<files>`, `<read_first>`, `<action>`, `<verify>`, `<acceptance_criteria>`, `<done>`. The updated task XML:

```xml
<task type="auto">
  <name>Task 1: [Action-oriented name]</name>
  <files>path/to/file.ext, another/file.ext</files>
  <read_first>path/to/reference.ext, path/to/source-of-truth.ext</read_first>
  <action>[Specific implementation - what to do, how to do it, what to avoid and WHY. Include CONCRETE values: exact identifiers, parameters, expected outputs, file paths, command arguments. Never say "align X with Y" without specifying the exact target state.]</action>
  <verify>[Command or check to prove it worked]</verify>
  <acceptance_criteria>
    - [Grep-verifiable condition: "file.ext contains 'exact string'"]
    - [Measurable condition: "output.ext uses 'expected-value', NOT 'wrong-value'"]
  </acceptance_criteria>
  <done>[Measurable acceptance criteria]</done>
</task>
```

2. In the second `<task type="auto">` example (around line 71-77), add the same fields:

```xml
<task type="auto">
  <name>Task 2: [Action-oriented name]</name>
  <files>path/to/file.ext</files>
  <read_first>path/to/reference.ext</read_first>
  <action>[Specific implementation with concrete values]</action>
  <verify>[Command or check]</verify>
  <acceptance_criteria>
    - [Grep-verifiable condition]
  </acceptance_criteria>
  <done>[Acceptance criteria]</done>
</task>
```

3. Leave ALL checkpoint task examples unchanged (they don't get read_first/acceptance_criteria).

4. In the Examples section near the bottom (the "Autonomous parallel plan" example around line 297), update the task examples there too to include `<read_first>` and `<acceptance_criteria>` fields with realistic example values.

5. In the Anti-Patterns section, add a new "Bad" pattern:

```
**Bad: Missing read_first and acceptance_criteria**
```xml
<task type="auto">
  <name>Update auth module</name>
  <files>src/auth.ts</files>
  <action>Update the auth module to match the new spec</action>
  <verify>Tests pass</verify>
  <done>Auth updated</done>
</task>
```

6. In the Guidelines section at the bottom, add: "- Include `read_first` and `acceptance_criteria` in every auto task"

7. CRITICAL: Do NOT remove or alter any existing `<src method="" format="">` references or research-specific content. The `<src>` block is GSD-R-specific and must remain alongside the new fields.
  </action>
  <verify>
    <automated>grep -c 'read_first' get-shit-done-r/templates/phase-prompt.md</automated>
    <automated>grep -c 'acceptance_criteria' get-shit-done-r/templates/phase-prompt.md</automated>
    <automated>grep -c 'gsd-r' get-shit-done-r/templates/phase-prompt.md</automated>
  </verify>
  <acceptance_criteria>
    - phase-prompt.md contains `<read_first>` at least 4 times (2 template tasks + 2 example tasks)
    - phase-prompt.md contains `<acceptance_criteria>` at least 4 times (2 template tasks + 2 example tasks)
    - phase-prompt.md contains `get-shit-done-r` (GSD-R paths preserved)
    - phase-prompt.md contains `gsd-r-planner` or `gsd-r-executor` (GSD-R agent names preserved)
    - phase-prompt.md contains the string `CONCRETE values` in the action field guidance
    - phase-prompt.md contains `Missing read_first` in the Anti-Patterns section
    - phase-prompt.md still contains `execution_context` section with `~/.claude/get-shit-done-r/` paths
  </acceptance_criteria>
  <done>phase-prompt.md task XML includes read_first and acceptance_criteria fields in all auto task examples, with GSD-R research content fully preserved</done>
</task>

<task type="auto">
  <name>Task 2: Add execution rigor gates and precommit handling to execute-plan.md</name>
  <files>get-shit-done-r/workflows/execute-plan.md</files>
  <read_first>get-shit-done-r/workflows/execute-plan.md, ~/.claude/get-shit-done/workflows/execute-plan.md</read_first>
  <action>
Modify the GSD-R execute-plan.md to add 4 new content blocks from upstream. Do NOT overwrite -- edit the existing GSD-R version. Preserve ALL gsd-r-* agent names, $HOME/.claude/get-shit-done-r paths, and research workflow logic.

**Change 1: Add read_first gate and acceptance_criteria check to step name="execute" (around line 134-144)**

Replace the current step name="execute" content with:

```
<step name="execute">
Deviations are normal -- handle via rules below.

1. Read @context files from prompt
2. Per task:
   - **MANDATORY read_first gate:** If the task has a `<read_first>` field, you MUST read every listed file BEFORE making any edits. This is not optional. Do not skip files because you "already know" what's in them -- read them. The read_first files establish ground truth for the task.
   - `type="auto"`: if `tdd="true"` -> TDD execution. Implement with deviation rules + auth gates. Verify done criteria. Commit (see task_commit). Track hash for Summary.
   - `type="checkpoint:*"`: STOP -> checkpoint_protocol -> wait for user -> continue only after confirmation.
   - **MANDATORY acceptance_criteria check:** After completing each task, if it has `<acceptance_criteria>`, verify EVERY criterion before moving to the next task. Use grep, file reads, or CLI commands to confirm each criterion. If any criterion fails, fix the implementation before proceeding. Do not skip criteria or mark them as "will verify later".
3. Run `<verification>` checks
4. Confirm `<success_criteria>` met
5. Document deviations in Summary
</step>
```

**Change 2: Add precommit_failure_handling section**

Insert a new `<precommit_failure_handling>` section AFTER the `</tdd_plan_execution>` closing tag and BEFORE the `<task_commit>` opening tag. Content:

```
<precommit_failure_handling>
## Pre-commit Hook Failure Handling

Your commits may trigger pre-commit hooks. Auto-fix hooks handle themselves transparently -- files get fixed and re-staged automatically.

If a commit is BLOCKED by a hook:

1. The `git commit` command fails with hook error output
2. Read the error -- it tells you exactly which hook and what failed
3. Fix the issue (type error, lint violation, secret leak, etc.)
4. `git add` the fixed files
5. Retry the commit
6. Do NOT use `--no-verify`

This is normal and expected. Budget 1-2 retry cycles per commit.
</precommit_failure_handling>
```

**Change 3: Replace verification_failure_gate with node-repair version**

Replace the current `<step name="verification_failure_gate">` (the simple STOP-and-present version around line 289-291) with:

```
<step name="verification_failure_gate">
If verification fails:

**Check if node repair is enabled** (default: on):
```bash
NODE_REPAIR=$(node "$HOME/.claude/get-shit-done-r/bin/gsd-r-tools.cjs" config-get workflow.node_repair 2>/dev/null || echo "true")
```

If `NODE_REPAIR` is `true`: invoke `@$HOME/.claude/get-shit-done-r/workflows/node-repair.md` with:
- FAILED_TASK: task number, name, done-criteria
- ERROR: expected vs actual result
- PLAN_CONTEXT: adjacent task names + phase goal
- REPAIR_BUDGET: `workflow.node_repair_budget` from config (default: 2)

Node repair will attempt RETRY, DECOMPOSE, or PRUNE autonomously. Only reaches this gate again if repair budget is exhausted (ESCALATE).

If `NODE_REPAIR` is `false` OR repair returns ESCALATE: STOP. Present: "Verification failed for Task [X]: [name]. Expected: [criteria]. Actual: [result]. Repair attempted: [summary of what was tried]." Options: Retry | Skip (mark incomplete) | Stop (investigate). If skipped -> SUMMARY "Issues Encountered".
</step>
```

CRITICAL namespace rules:
- Use `gsd-r-tools.cjs` NOT `gsd-tools.cjs`
- Use `get-shit-done-r` NOT `get-shit-done` in ALL paths
- Use `gsd-r-executor` NOT `gsd-executor` in agent references
- The node-repair.md reference must use `$HOME/.claude/get-shit-done-r/workflows/node-repair.md`
  </action>
  <verify>
    <automated>grep -c 'MANDATORY read_first gate' get-shit-done-r/workflows/execute-plan.md</automated>
    <automated>grep -c 'MANDATORY acceptance_criteria check' get-shit-done-r/workflows/execute-plan.md</automated>
    <automated>grep -c 'precommit_failure_handling' get-shit-done-r/workflows/execute-plan.md</automated>
    <automated>grep -c 'node_repair' get-shit-done-r/workflows/execute-plan.md</automated>
    <automated>grep 'gsd-tools.cjs' get-shit-done-r/workflows/execute-plan.md | grep -v 'gsd-r-tools.cjs' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - execute-plan.md contains `MANDATORY read_first gate` exactly 1 time
    - execute-plan.md contains `MANDATORY acceptance_criteria check` exactly 1 time
    - execute-plan.md contains `precommit_failure_handling` at least 2 times (open + close tags)
    - execute-plan.md contains `node_repair` at least 3 times (config-get, workflow reference, budget)
    - execute-plan.md contains `get-shit-done-r` in ALL path references (zero `get-shit-done/` without the -r suffix)
    - execute-plan.md contains `gsd-r-tools.cjs` (not bare `gsd-tools.cjs`)
    - execute-plan.md contains `gsd-r-executor` (not bare `gsd-executor`)
    - execute-plan.md still contains `deviation_rules` section (not removed)
    - execute-plan.md still contains `checkpoint_protocol` step (not removed)
    - grep for `gsd-tools.cjs` minus `gsd-r-tools.cjs` returns 0 lines (no upstream namespace leaks)
  </acceptance_criteria>
  <done>execute-plan.md has all 4 upstream content blocks (read_first gate, acceptance_criteria check, precommit handling, node-repair verification gate) with correct GSD-R namespace throughout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -c 'read_first' get-shit-done-r/templates/phase-prompt.md` returns >= 4
- [ ] `grep -c 'acceptance_criteria' get-shit-done-r/templates/phase-prompt.md` returns >= 4
- [ ] `grep -c 'MANDATORY read_first gate' get-shit-done-r/workflows/execute-plan.md` returns 1
- [ ] `grep -c 'precommit_failure_handling' get-shit-done-r/workflows/execute-plan.md` returns >= 2
- [ ] `grep -c 'node_repair' get-shit-done-r/workflows/execute-plan.md` returns >= 3
- [ ] Zero namespace leaks: `grep 'get-shit-done/' get-shit-done-r/workflows/execute-plan.md | grep -v 'get-shit-done-r/' | wc -l` returns 0
- [ ] `node --test test/*.test.cjs` passes (no regressions)
</verification>

<success_criteria>
- phase-prompt.md task XML includes read_first and acceptance_criteria in all auto task examples
- execute-plan.md has mandatory read_first gate, acceptance_criteria check, precommit handling, and node-repair verification gate
- All GSD-R-specific content preserved (research src blocks, gsd-r-* names, get-shit-done-r paths)
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-templates-and-execution-rigor/12-01-SUMMARY.md`
</output>

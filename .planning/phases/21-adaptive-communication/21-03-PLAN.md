---
phase: 21-adaptive-communication
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - grd/workflows/new-research.md
  - grd/workflows/scope-inquiry.md
  - grd/workflows/plan-inquiry.md
  - grd/workflows/conduct-inquiry.md
  - grd/workflows/verify-inquiry.md
  - grd/workflows/progress.md
  - test/tier-strip.test.cjs
autonomous: true
requirements:
  - TIER-01
  - TIER-03
  - TIER-04
  - TEST-06

must_haves:
  truths:
    - "Each of the 6 key workflows has tier-conditional Next Up sections with XML blocks"
    - "Verifier agent prompt includes a <researcher_tier> context block that adapts verification output formatting"
    - "Error messages in workflows use tier-conditional blocks (Guided=explains, Standard=rationale, Expert=terse)"
    - "Checkpoint descriptions in verify-inquiry.md (saturation gate) adapt by tier"
    - "Agent-to-agent communication (PLAN.md, STATE.md, verification reports structure) is NOT tier-adapted"
    - "Workflow content tests verify tier blocks exist and strip cleanly"
  artifacts:
    - path: "grd/workflows/new-research.md"
      provides: "Tier-adapted Next Up sections"
      contains: "<tier-guided>"
    - path: "grd/workflows/scope-inquiry.md"
      provides: "Tier-adapted Next Up sections"
      contains: "<tier-guided>"
    - path: "grd/workflows/plan-inquiry.md"
      provides: "Tier-adapted Next Up sections and planner tier context"
      contains: "<tier-guided>"
    - path: "grd/workflows/conduct-inquiry.md"
      provides: "Tier-adapted Next Up sections and executor tier context"
      contains: "<tier-guided>"
    - path: "grd/workflows/verify-inquiry.md"
      provides: "Tier-adapted verification output, saturation gate, and Next Up"
      contains: "<tier-guided>"
    - path: "grd/workflows/progress.md"
      provides: "Tier-adapted routing output"
      contains: "<tier-guided>"
  key_links:
    - from: "grd/workflows/verify-inquiry.md"
      to: "<researcher_tier> block"
      via: "Verifier agent prompt context"
      pattern: "researcher_tier"
    - from: "grd/workflows/conduct-inquiry.md"
      to: "<researcher_tier> block"
      via: "Executor agent prompt context"
      pattern: "researcher_tier"
---

<objective>
Add tier-conditional XML blocks to all 6 key workflows for Next Up routing, checkpoint descriptions, error messages, and agent prompt context blocks. This makes user-facing workflow output adapt to the researcher's experience level.

Purpose: Workflows are where the researcher interacts with GRD. Tier-adapted output means Guided researchers get explanations of what's happening and why, Standard researchers get concise academic context, and Expert researchers get commands only.
Output: 6 adapted workflows + workflow content tests appended to tier-strip.test.cjs
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-adaptive-communication/21-CONTEXT.md
@.planning/phases/21-adaptive-communication/21-RESEARCH.md
@.planning/phases/21-adaptive-communication/21-01-SUMMARY.md

<interfaces>
From grd/bin/lib/tier-strip.cjs (created in Plan 01):
```javascript
const VALID_TIERS = ['guided', 'standard', 'expert'];
function stripTierContent(content, tier, format = 'xml') { ... }
module.exports = { stripTierContent, VALID_TIERS };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier-conditional blocks to all 6 key workflows</name>
  <files>
    grd/workflows/new-research.md
    grd/workflows/scope-inquiry.md
    grd/workflows/plan-inquiry.md
    grd/workflows/conduct-inquiry.md
    grd/workflows/verify-inquiry.md
    grd/workflows/progress.md
  </files>
  <read_first>
    grd/workflows/new-research.md
    grd/workflows/scope-inquiry.md
    grd/workflows/plan-inquiry.md
    grd/workflows/conduct-inquiry.md
    grd/workflows/verify-inquiry.md
    grd/workflows/progress.md
    docs/GRD-v1.2-Research-Reorientation-Spec.md
    .planning/phases/21-adaptive-communication/21-RESEARCH.md
    .planning/phases/21-adaptive-communication/21-CONTEXT.md
  </read_first>
  <action>
    For each workflow, add three categories of tier-conditional XML blocks. Workflows use XML format (`<tier-guided>`, `<tier-standard>`, `<tier-expert>`) because they are prompt files, not markdown templates.

    **Category 1: Next Up sections** -- adapt every `## ▶ Next Up` section in each workflow.

    Pattern per spec (from RESEARCH.md code example):
    ```
    ## ▶ Next Up

    <tier-guided>
    [Explanation of what the next step does and why it matters.]

    **[Action Name]** -- [description]

    `/grd:[command] [args]`

    <sub>`/clear` first -- this gives you a fresh context window for execution</sub>
    </tier-guided>
    <tier-standard>
    **[Action Name]** -- [brief description]

    `/grd:[command] [args]`

    <sub>`/clear` first -- fresh context window</sub>
    </tier-standard>
    <tier-expert>
    `/grd:[command] [args]`
    </tier-expert>
    ```

    Apply this pattern to all Next Up sections:
    - **new-research.md** (line ~1117): Next Up routes to scope-inquiry. Guided explains what inquiry scoping does and why.
    - **scope-inquiry.md** (line ~639): Next Up routes to plan-inquiry. Guided explains that planning creates a search protocol.
    - **plan-inquiry.md** (line ~786): Next Up routes to conduct-inquiry. Guided explains that execution runs the search plans.
    - **conduct-inquiry.md** (line ~354): Next Up routes to verify-inquiry. Guided explains that verification checks evidence quality.
    - **verify-inquiry.md** (line ~630): Next Up routes to next phase or complete-study. Guided explains what comes next.
    - **progress.md** (multiple Next Up sections at lines ~179, 201, 218, 291, 319, 350): Adapt all 6 routing sections. Guided explains each route option.

    **Category 2: Researcher tier context blocks** -- add `<researcher_tier>` block to user-facing agent prompts within workflows.

    For **conduct-inquiry.md** (executor agent): Add near the top of the agent spawn prompt section:
    ```xml
    <researcher_tier>
    ## Communication Style: ${researcher_tier}

    <tier-guided>
    **Vocabulary:** Explain technical terms on first use. Use everyday language alongside academic terms.
    **Explanation depth:** Explain what you are doing and why at each step. Provide context for decisions.
    **Information density:** Prefer clarity over brevity. One concept per sentence.
    **Examples:** Include concrete examples for abstract concepts.
    </tier-guided>
    <tier-standard>
    **Vocabulary:** Academic vocabulary with brief context where non-obvious.
    **Explanation depth:** Standard scholarly communication.
    **Information density:** Balanced -- clear but concise.
    </tier-standard>
    <tier-expert>
    **Vocabulary:** Precise terminology without elaboration. Maximum information density.
    **Explanation depth:** No elaboration unless ambiguous.
    **Information density:** Maximum. Assume familiarity with all concepts.
    </tier-expert>
    </researcher_tier>
    ```

    For **verify-inquiry.md** (verifier agent): Add `<researcher_tier>` block with verification-specific adaptation:
    ```xml
    <researcher_tier>
    ## Communication Style: ${researcher_tier}

    <tier-guided>
    **When reporting verification results:**
    - Explain what each check means before stating pass/fail
    - When something fails, explain WHY it matters (not just that it failed)
    - Suggest specific next steps for each failure
    - Use plain language alongside technical terms
    </tier-guided>
    <tier-standard>
    **When reporting verification results:**
    - State what failed with the relevant standard
    - Include the specific requirement that was not met
    - Brief rationale for why the check exists
    </tier-standard>
    <tier-expert>
    **When reporting verification results:**
    - Terse failure statements only
    - Requirement ID + failure description
    - No elaboration unless ambiguous
    </tier-expert>
    </researcher_tier>
    ```

    For **plan-inquiry.md** (planner agent): Add `<researcher_tier>` block with plan description adaptation:
    ```xml
    <researcher_tier>
    ## Communication Style: ${researcher_tier}

    <tier-guided>
    **When describing plans:** Explain what each plan does, why it matters, and what the researcher will get from it. Use everyday language.
    **When presenting options:** Explain tradeoffs in concrete terms with examples.
    </tier-guided>
    <tier-standard>
    **When describing plans:** Use standard scholarly vocabulary with brief context.
    **When presenting options:** State tradeoffs concisely.
    </tier-standard>
    <tier-expert>
    **When describing plans:** Terse descriptions. Methodology terms only.
    **When presenting options:** Options list, no elaboration.
    </tier-expert>
    </researcher_tier>
    ```

    **Category 3: Checkpoint and error message adaptation** -- adapt interactive gates and error output.

    For **verify-inquiry.md** saturation gate (TRAP-03): Wrap the gate description and options in tier blocks. Guided explains what saturation means and what each option does. Standard states the options. Expert shows options only.

    For **plan-inquiry.md** TRAP-02 gate: Wrap the review type mismatch description in tier blocks. Guided explains what review type mismatch means and consequences of each choice.

    For error messages in workflows (e.g., "No planning structure found", validation errors): Add tier-conditional blocks where the error provides user guidance. Guided explains the concept and suggests next steps. Standard states requirement and rationale. Expert states requirement only.

    IMPORTANT constraints:
    - Do NOT add tier blocks to bash commands, JSON parsing, agent spawn parameters, or any machine-processed sections
    - Do NOT adapt internal agent communication (PLAN.md structure, STATE.md updates, frontmatter)
    - The `researcher_tier` value comes from init JSON -- it is already propagated to all workflows by init.cjs
    - Keep the existing workflow structure intact. Only wrap/add to user-facing output sections
    - `<tier-*>` blocks go on their own lines within the workflow markdown
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const workflows = ['new-research.md','scope-inquiry.md','plan-inquiry.md','conduct-inquiry.md','verify-inquiry.md','progress.md'];
let ok = true;
for (const w of workflows) {
  const c = fs.readFileSync('grd/workflows/' + w, 'utf8');
  if (!c.includes('<tier-guided>')) { console.error(w + ' missing <tier-guided>'); ok = false; }
  if (!c.includes('</tier-guided>')) { console.error(w + ' missing </tier-guided>'); ok = false; }
}
if (ok) console.log('All 6 workflows have tier blocks');
process.exit(ok ? 0 : 1);
"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 workflows contain `<tier-guided>` and `</tier-guided>` blocks
    - grep -l "tier-guided" grd/workflows/new-research.md grd/workflows/scope-inquiry.md grd/workflows/plan-inquiry.md grd/workflows/conduct-inquiry.md grd/workflows/verify-inquiry.md grd/workflows/progress.md returns all 6 files
    - verify-inquiry.md contains `<researcher_tier>` block with verification-specific adaptation
    - conduct-inquiry.md contains `<researcher_tier>` block with executor-specific adaptation
    - plan-inquiry.md contains `<researcher_tier>` block with planner-specific adaptation
    - grep -c "researcher_tier" grd/workflows/verify-inquiry.md returns >= 2 (init reference + context block)
    - No tier blocks inside bash command blocks or JSON parsing sections
  </acceptance_criteria>
  <done>All 6 workflows have tier-conditional Next Up sections, 3 agent-facing workflows have researcher_tier context blocks, interactive gates adapt by tier</done>
</task>

<task type="auto">
  <name>Task 2: Workflow content tests and full suite validation</name>
  <files>test/tier-strip.test.cjs</files>
  <read_first>
    test/tier-strip.test.cjs
    grd/bin/lib/tier-strip.cjs
    grd/workflows/verify-inquiry.md
    grd/workflows/conduct-inquiry.md
  </read_first>
  <action>
    Append new describe blocks to test/tier-strip.test.cjs:

    1. describe('Workflow tier block completeness') -- for each of the 6 adapted workflows:
       - it(`${workflow} has tier-guided blocks`) -- verify `<tier-guided>` exists
       - it(`${workflow} has tier-standard blocks`) -- verify `<tier-standard>` exists
       - it(`${workflow} has tier-expert blocks`) -- verify `<tier-expert>` exists
       - it(`${workflow} has no orphaned tier tags after stripping`) -- for each tier, call stripTierContent(content, tier, 'xml'), verify no `<tier-` or `</tier-` remains

    2. describe('Agent researcher_tier context blocks'):
       - it('verify-inquiry.md has researcher_tier context block') -- verify `<researcher_tier>` present
       - it('conduct-inquiry.md has researcher_tier context block') -- verify `<researcher_tier>` present
       - it('plan-inquiry.md has researcher_tier context block') -- verify `<researcher_tier>` present
       - it('researcher_tier block contains all three tier variants') -- for each of the 3 workflow files, verify all three `<tier-guided>`, `<tier-standard>`, `<tier-expert>` appear within the `<researcher_tier>` block

    3. describe('Workflow tier content verification'):
       - it('progress.md guided tier explains next steps') -- strip for guided, verify explanatory text exists
       - it('progress.md expert tier shows commands only') -- strip for expert, verify `/grd:` commands present but no explanatory paragraphs around them in Next Up sections
       - it('verify-inquiry.md guided tier explains verification failures') -- strip for guided, verify "explain" or "why" pattern in researcher_tier block

    ADAPTED_WORKFLOWS constant:
    ```javascript
    const ADAPTED_WORKFLOWS = [
      'new-research.md', 'scope-inquiry.md', 'plan-inquiry.md',
      'conduct-inquiry.md', 'verify-inquiry.md', 'progress.md',
    ];
    ```

    After appending tests, run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>node --test test/tier-strip.test.cjs && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ADAPTED_WORKFLOWS" test/tier-strip.test.cjs returns >= 1
    - grep -c "Workflow tier block completeness" test/tier-strip.test.cjs returns >= 1
    - grep -c "researcher_tier context" test/tier-strip.test.cjs returns >= 1
    - node --test test/tier-strip.test.cjs exits 0
    - node scripts/run-tests.cjs exits 0 (full suite green, no regressions)
  </acceptance_criteria>
  <done>Workflow completeness tests verify all 6 workflows have tier blocks, agent context block tests verify 3 agent-facing workflows have researcher_tier blocks, content tests verify tier adaptation produces expected output, full test suite passes</done>
</task>

</tasks>

<verification>
- node --test test/tier-strip.test.cjs -- all tests green (unit + template + workflow)
- node scripts/run-tests.cjs -- full suite passes
- All 6 workflows have <tier-guided>, <tier-standard>, <tier-expert> blocks
- verify-inquiry.md, conduct-inquiry.md, plan-inquiry.md each have <researcher_tier> blocks
</verification>

<success_criteria>
- All 6 key workflows have tier-adapted Next Up sections
- 3 agent-facing workflows have researcher_tier context blocks
- Interactive gates (TRAP-02, TRAP-03) adapt checkpoint descriptions by tier
- Error messages adapt by tier in workflow orchestration
- No tier blocks in machine-processed sections (bash, JSON, agent spawn params)
- Agent-to-agent communication structure unchanged
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-adaptive-communication/21-03-SUMMARY.md`
</output>

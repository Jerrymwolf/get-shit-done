---
phase: 21-adaptive-communication
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - grd/templates/research-note.md
  - grd/templates/source-log.md
  - grd/templates/research-task.md
  - grd/templates/project.md
  - grd/templates/bootstrap.md
  - grd/templates/requirements.md
  - grd/templates/roadmap.md
  - test/tier-strip.test.cjs
autonomous: true
requirements:
  - TIER-02

must_haves:
  truths:
    - "Guided tier templates include inline guidance comments explaining each section's purpose"
    - "Standard tier templates include section headers with brief descriptions (current content unchanged)"
    - "Expert tier templates include section headers only -- no description text"
    - "Stripping for any tier produces valid markdown with no orphaned comment tags"
    - "Every adapted template has at least one <!-- tier:guided --> block"
  artifacts:
    - path: "grd/templates/research-note.md"
      provides: "Tier-conditional research note template"
      contains: "<!-- tier:guided -->"
    - path: "grd/templates/source-log.md"
      provides: "Tier-conditional source log template"
      contains: "<!-- tier:guided -->"
    - path: "grd/templates/research-task.md"
      provides: "Tier-conditional research task template"
      contains: "<!-- tier:guided -->"
    - path: "grd/templates/project.md"
      provides: "Tier-conditional prospectus template"
      contains: "<!-- tier:guided -->"
    - path: "grd/templates/bootstrap.md"
      provides: "Tier-conditional bootstrap template"
      contains: "<!-- tier:guided -->"
    - path: "grd/templates/requirements.md"
      provides: "Tier-conditional requirements template"
      contains: "<!-- tier:guided -->"
    - path: "grd/templates/roadmap.md"
      provides: "Tier-conditional roadmap template"
      contains: "<!-- tier:guided -->"
  key_links:
    - from: "test/tier-strip.test.cjs"
      to: "grd/templates/*.md"
      via: "fs.readFileSync content scan"
      pattern: "tier:guided"
---

<objective>
Add tier-conditional comment blocks to all 7 research-facing templates so they adapt by researcher tier (Guided=inline guidance, Standard=brief descriptions, Expert=headers only).

Purpose: Templates are the primary user-facing content surface. Tier adaptation here ensures researchers at every level get appropriate scaffolding without changing what they produce.
Output: 7 adapted templates + completeness tests appended to tier-strip.test.cjs
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-adaptive-communication/21-CONTEXT.md
@.planning/phases/21-adaptive-communication/21-RESEARCH.md
@.planning/phases/21-adaptive-communication/21-01-SUMMARY.md

<interfaces>
From grd/bin/lib/tier-strip.cjs (created in Plan 01):
```javascript
const VALID_TIERS = ['guided', 'standard', 'expert'];
function stripTierContent(content, tier, format = 'xml') { ... }
module.exports = { stripTierContent, VALID_TIERS };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier-conditional blocks to all 7 templates</name>
  <files>
    grd/templates/research-note.md
    grd/templates/source-log.md
    grd/templates/research-task.md
    grd/templates/project.md
    grd/templates/bootstrap.md
    grd/templates/requirements.md
    grd/templates/roadmap.md
  </files>
  <read_first>
    grd/templates/research-note.md
    grd/templates/source-log.md
    grd/templates/research-task.md
    grd/templates/project.md
    grd/templates/bootstrap.md
    grd/templates/requirements.md
    grd/templates/roadmap.md
    docs/GRD-v1.2-Research-Reorientation-Spec.md
    .planning/phases/21-adaptive-communication/21-RESEARCH.md
  </read_first>
  <action>
    Apply the additive/subtractive model to each template. The content model:
    - Unwrapped content = visible to ALL tiers (section headers, frontmatter, structural elements)
    - `<!-- tier:guided -->` blocks = Guided-only additions (one guidance sentence per section explaining what to write and why)
    - `<!-- tier:standard -->` blocks = Standard descriptions that Expert doesn't need (current brief description comments)
    - `<!-- tier:expert -->` blocks = Expert-only content (rarely needed -- Expert is mainly the absence of guidance)

    Current content IS the Standard tier baseline. Apply these changes:

    **research-note.md** (73 lines):
    - Wrap existing `<!-- 2-3 sentence summary... -->` comment under Key Findings in `<!-- tier:standard -->` block
    - Add `<!-- tier:guided -->` block under Key Findings: "Write 2-3 sentences summarizing what this note concludes. What is the single most important thing someone should take away? State it clearly enough that a reader who only reads this section understands the core finding."
    - Wrap existing `<!-- Main body of the research note... -->` comment under Analysis in `<!-- tier:standard -->` block
    - Add `<!-- tier:guided -->` block under Analysis: "This is the main body of your research note. Use inline citations like (Author, Year) and link to local source files in -sources/. Structure with subheadings as needed. Focus on what the evidence says, not just what each source says individually."
    - Wrap the entire Evidence Quality guidance comment (lines 26-37) in `<!-- tier:standard -->` block
    - Add `<!-- tier:guided -->` block before Evidence Quality guidance: "Rate the quality of your evidence. The format depends on your review type (check your config). For systematic reviews, fill in the table below for each source. For narrative reviews, a brief strengths/limitations paragraph is sufficient. Your epistemological stance (from config) influences what counts as quality evidence."
    - Leave section headers, frontmatter, and table structures unwrapped (visible to all tiers)

    **source-log.md** (16 lines):
    - Add `<!-- tier:guided -->` block after the header: "This log tracks every source you encounter during your research. Add one row per source as you find it. The status column tracks whether you've read it, noted it, or excluded it. This is your audit trail -- it proves your search was thorough."
    - Wrap any existing description content in `<!-- tier:standard -->` block
    - Table header and structure stays unwrapped

    **research-task.md** (137 lines):
    - Add `<!-- tier:guided -->` blocks at key sections explaining what the executor should do at each step
    - Wrap existing description comments in `<!-- tier:standard -->` blocks
    - Leave structural XML elements (`<src>`, action items, verification) unwrapped

    **project.md** (209 lines -- this is a template wrapper, the inner template block matters):
    - Inside the `<template>` block, add `<!-- tier:guided -->` blocks after each section header explaining what to write
    - Wrap existing bracket-enclosed guidance text `[What problem...]` in `<!-- tier:standard -->` blocks where appropriate
    - Keep section headers and frontmatter structure unwrapped
    - Guided guidance for Problem Statement: "Describe the research gap or question driving this study in 2-3 sentences. Use your own words -- this should make sense to someone unfamiliar with the topic."
    - Guided guidance for Significance: "Explain why answering this question matters. Who benefits? What broader scholarly or practical implications does it connect to?"
    - Guided guidance for Research Questions: "List your main research questions. Primary questions are what you must answer. Secondary questions are nice-to-have. Out of Scope questions clarify boundaries."

    **bootstrap.md** (32 lines):
    - Add `<!-- tier:guided -->` block: "This assessment maps what is known, debated, and unexplored in your research domain. Established findings are well-supported claims. Contested areas have conflicting evidence. Unexplored territory identifies gaps your research could fill."
    - Wrap existing section guidance in `<!-- tier:standard -->` blocks

    **requirements.md** (196 lines -- template wrapper):
    - Inside template block, add `<!-- tier:guided -->` blocks explaining research objectives and acceptance criteria
    - Guided for objectives section: "Each objective is a specific aim of your research. The ID helps track it throughout the study. Acceptance criteria define what 'answered' means -- what evidence would satisfy this objective?"
    - Wrap existing guidance in `<!-- tier:standard -->` blocks

    **roadmap.md** (202 lines -- template wrapper):
    - Inside template block, add `<!-- tier:guided -->` blocks explaining lines of inquiry and phase structure
    - Guided for phases section: "Each phase represents a line of inquiry -- a focused investigation into one aspect of your research questions. Later phases can depend on earlier ones if findings build on each other."
    - Wrap existing guidance in `<!-- tier:standard -->` blocks

    IMPORTANT: Do NOT wrap structural elements (frontmatter, section headers, table structures, XML tags) in tier blocks. Only wrap descriptive/guidance content. The standard tier must produce output identical to current templates when non-standard blocks are stripped.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const path = require('path');
const templates = ['research-note.md','source-log.md','research-task.md','project.md','bootstrap.md','requirements.md','roadmap.md'];
let ok = true;
for (const t of templates) {
  const c = fs.readFileSync(path.join('grd/templates', t), 'utf8');
  if (!c.includes('<!-- tier:guided -->')) { console.error(t + ' missing guided blocks'); ok = false; }
  if (!c.includes('<!-- /tier:guided -->')) { console.error(t + ' missing guided closing'); ok = false; }
}
if (ok) console.log('All 7 templates have tier blocks');
process.exit(ok ? 0 : 1);
"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 templates contain `<!-- tier:guided -->` and `<!-- /tier:guided -->` (at least one block each)
    - grep -l "tier:guided" grd/templates/research-note.md grd/templates/source-log.md grd/templates/research-task.md grd/templates/project.md grd/templates/bootstrap.md grd/templates/requirements.md grd/templates/roadmap.md returns all 7 files
    - No template has orphaned tier tags (every open has a matching close)
    - Section headers (## Key Findings, ## Analysis, etc.) are NOT inside tier blocks
    - Existing test suites still pass: node --test test/research-note-template.test.cjs exits 0
  </acceptance_criteria>
  <done>All 7 templates have tier-conditional comment blocks with Guided guidance, Standard descriptions, and Expert getting headers only</done>
</task>

<task type="auto">
  <name>Task 2: Template completeness and round-trip content tests</name>
  <files>test/tier-strip.test.cjs</files>
  <read_first>
    test/tier-strip.test.cjs
    grd/bin/lib/tier-strip.cjs
    grd/templates/research-note.md
  </read_first>
  <action>
    Append new describe blocks to test/tier-strip.test.cjs (created in Plan 01):

    1. describe('Template completeness scan') -- for each of the 7 adapted templates:
       - it(`${template} has guided tier blocks`) -- verify `<!-- tier:guided -->` exists
       - it(`${template} has no orphaned tier tags`) -- strip for each tier, verify no `<!-- tier:` or `<!-- /tier:` remains in output

    2. describe('Template round-trip safety') -- for each of the 7 templates and each of the 3 tiers:
       - Read template, call stripTierContent(content, tier, 'comment')
       - Verify result contains no orphaned `<!-- tier:` or `<!-- /tier:` tags
       - Verify result is non-empty
       - Verify section headers from original are preserved (## still present)

    3. describe('Template content verification'):
       - it('research-note.md guided tier has Key Findings guidance') -- strip for guided, verify guidance text present
       - it('research-note.md expert tier has no description comments') -- strip for expert, verify no `<!--` guidance comments (only structural content)
       - it('research-note.md standard tier matches baseline') -- strip for standard, verify existing description comments preserved

    Import the stripTierContent function at the top of the test file (if not already imported):
    ```javascript
    const { stripTierContent, VALID_TIERS } = require(path.join(__dirname, '..', 'grd', 'bin', 'lib', 'tier-strip.cjs'));
    ```

    ADAPTED_TEMPLATES constant:
    ```javascript
    const ADAPTED_TEMPLATES = [
      'research-note.md', 'source-log.md', 'research-task.md',
      'project.md', 'bootstrap.md', 'requirements.md', 'roadmap.md',
    ];
    ```
  </action>
  <verify>
    <automated>node --test test/tier-strip.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ADAPTED_TEMPLATES" test/tier-strip.test.cjs returns >= 1
    - grep -c "Template completeness" test/tier-strip.test.cjs returns >= 1
    - grep -c "round-trip" test/tier-strip.test.cjs returns >= 1 (case insensitive)
    - node --test test/tier-strip.test.cjs exits 0
    - node scripts/run-tests.cjs exits 0 (full suite)
  </acceptance_criteria>
  <done>Completeness scan verifies all 7 templates have tier blocks, round-trip safety confirms no orphaned tags for any tier, content tests verify guided has guidance and expert has headers only</done>
</task>

</tasks>

<verification>
- node --test test/tier-strip.test.cjs -- all tests green including completeness and round-trip
- node --test test/research-note-template.test.cjs -- existing template tests still pass
- node scripts/run-tests.cjs -- full suite passes
- All 7 templates scannable for <!-- tier:guided --> blocks
</verification>

<success_criteria>
- All 7 research-facing templates have tier-conditional comment blocks
- Guided tier adds inline guidance explaining each section
- Standard tier preserves current template content (baseline unchanged)
- Expert tier strips to headers only (no description comments)
- Completeness tests prevent future regressions (missing tier variants caught)
- Round-trip safety tests confirm clean stripping with no orphaned tags
</success_criteria>

<output>
After completion, create `.planning/phases/21-adaptive-communication/21-02-SUMMARY.md`
</output>

---
phase: 31-command-reconceptualization----export-and-presentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grd/workflows/ship.md
  - grd/workflows/pr-branch.md
autonomous: true
requirements: [CMD-01, CMD-03]

must_haves:
  truths:
    - "/grd:ship describes packaging research notes and sources for Obsidian vault export, manuscript assembly, or deliverable packaging"
    - "/grd:pr-branch describes packaging research notes without .planning/ artifacts for sharing or submission"
  artifacts:
    - path: "grd/workflows/ship.md"
      provides: "Research export workflow"
      contains: "research"
    - path: "grd/workflows/pr-branch.md"
      provides: "Clean research export workflow"
      contains: "research"
  key_links:
    - from: "grd/workflows/ship.md"
      to: "grd-tools.cjs"
      via: "node calls preserved"
      pattern: "grd-tools\\.cjs"
    - from: "grd/workflows/pr-branch.md"
      to: "git commands"
      via: "cherry-pick filtering preserved"
      pattern: "cherry-pick"
---

<objective>
Reconceptualize /grd:ship and /grd:pr-branch from PM shipping commands into research export tools.

Purpose: These two output-oriented commands currently describe code deployment and PR creation. They need to describe research deliverable packaging and clean export for sharing.
Output: Two rewritten workflow files speaking research language.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/grd/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-command-reconceptualization----diagnostics-and-corpus/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconceptualize ship workflow as research export</name>
  <files>grd/workflows/ship.md</files>
  <read_first>grd/workflows/ship.md</read_first>
  <action>
Rewrite grd/workflows/ship.md to describe research export instead of code shipping. Apply the same Phase 30 pattern: rewrite all descriptive content, preserve all structural elements.

Changes to make:
- `<purpose>`: Change from "Create a pull request from completed phase/milestone work" to describing packaging research notes, sources, and synthesis documents for Obsidian vault export, manuscript assembly, or deliverable packaging. The PR mechanism is preserved but framed as "exporting research deliverables for review."
- Step "preflight_checks": Reframe verification as "research findings verified" not "code verified." Keep all bash commands and structural checks identical.
- Step "generate_pr_body": Reframe sections:
  - "Summary" section: "What was investigated" not "what was built"
  - "Changes" section: "Findings and artifacts" not "code changes"
  - "Requirements section": "Research questions addressed" not "requirements addressed"
  - "Testing section": "Verification" stays but describes evidence quality and source coverage, not test results
  - "Decisions section": "Methodological decisions" not "key decisions"
- Step "report": Change "Shipped" to "Exported" in the final banner. Change next-step suggestions to use research vocabulary (e.g., "/grd:complete-study" is already correct).
- `<offer_next>`: Already uses research terms ("/grd:complete-study", "/grd:conduct-inquiry") -- verify and preserve.
- `<success_criteria>`: Reframe items to describe research export (e.g., "Research deliverables packaged" not "Branch pushed")

Preserve EXACTLY:
- All bash commands, variable names, grd-tools.cjs calls
- All step names and step structure
- All git operations (push, gh pr create)
- All AskUserQuestion structures
- The node "/Users/jeremiahwolf/.claude/grd/bin/grd-tools.cjs" paths
  </action>
  <verify>
    <automated>grep -c "research\|findings\|sources\|corpus\|scholarly\|manuscript\|export\|deliverable" grd/workflows/ship.md | xargs test 8 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep "research" grd/workflows/ship.md returns matches (research language present)
    - grep "grd-tools.cjs" grd/workflows/ship.md returns matches (structural elements preserved)
    - grep "gh pr create" grd/workflows/ship.md returns match (git operations preserved)
    - grep -i "deploy\|feature\|sprint\|release" grd/workflows/ship.md returns NO matches (PM vocabulary removed)
  </acceptance_criteria>
  <done>ship.md describes research export workflow with all structural elements intact; no PM vocabulary remains in descriptive text</done>
</task>

<task type="auto">
  <name>Task 2: Reconceptualize pr-branch workflow as clean research export</name>
  <files>grd/workflows/pr-branch.md</files>
  <read_first>grd/workflows/pr-branch.md</read_first>
  <action>
Rewrite grd/workflows/pr-branch.md to describe clean research export instead of PR branch filtering. Apply the same Phase 30 pattern: rewrite descriptive content, preserve structural elements.

Changes to make:
- `<purpose>`: Change from "Create a clean branch for pull requests by filtering out .planning/ commits" to describing packaging research notes and source files for sharing or submission, filtering out internal planning artifacts. The .planning/ filtering concept is preserved but framed as "producing a clean export of research deliverables without internal workflow artifacts."
- Step "detect_state": Change display banner from "PR BRANCH" to "EXPORT CLEAN" or similar research-native label
- Step "analyze_commits": Reframe "Code commits" as "Research commits" (touch research notes, sources, synthesis documents). "Planning-only commits" stays similar (internal workflow artifacts). "Mixed commits" reframe similarly.
- Step "create_pr_branch": Keep all git operations identical. Reframe variable concept but keep variable names unchanged for structural safety.
- Step "verify": Change display from "PR branch created" to "Clean export branch created." Change "Next steps" to describe sharing/submission rather than PR creation. Preserve the /grd:ship reference.
- `<success_criteria>`: Reframe to describe clean research export (e.g., "Clean export of research artifacts without .planning/ files")

Preserve EXACTLY:
- All bash commands, git operations (cherry-pick, checkout, rev-list)
- All variable names ($CURRENT_BRANCH, $TARGET, $PR_BRANCH, etc.)
- The .planning/ filtering logic
- All conditional checks and display formatting structure
  </action>
  <verify>
    <automated>grep -c "research\|findings\|sources\|notes\|scholarly\|export\|deliverable\|sharing\|submission" grd/workflows/pr-branch.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - grep "research" grd/workflows/pr-branch.md returns matches (research language present)
    - grep "cherry-pick" grd/workflows/pr-branch.md returns matches (git operations preserved)
    - grep "\.planning/" grd/workflows/pr-branch.md returns matches (filtering logic preserved)
    - grep -i "code changes\|reviewers don't see\|pull request" grd/workflows/pr-branch.md returns NO matches in purpose (PM framing removed from descriptive text)
  </acceptance_criteria>
  <done>pr-branch.md describes clean research export workflow with all git operations and .planning/ filtering intact; no PM vocabulary remains in descriptive text</done>
</task>

</tasks>

<verification>
- Both workflow files use research vocabulary throughout descriptive sections
- All bash commands, grd-tools.cjs calls, git operations preserved unchanged
- No PM vocabulary (deploy, feature, sprint, release, code changes) in descriptive text
- File paths and variable names unchanged
</verification>

<success_criteria>
- /grd:ship workflow describes research export: Obsidian vault export, manuscript assembly, deliverable packaging
- /grd:pr-branch workflow describes clean research export: notes without .planning/ artifacts for sharing
- All structural elements (bash, git, grd-tools.cjs) preserved exactly
</success_criteria>

<output>
After completion, create `.planning/phases/31-command-reconceptualization----export-and-presentation/31-01-SUMMARY.md`
</output>

---
phase: 28-agent-and-workflow-reorientation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/grd-executor.md
  - agents/grd-verifier.md
  - agents/grd-plan-checker.md
  - agents/grd-roadmapper.md
autonomous: true
requirements: [AGT-01, AGT-02, AGT-03]

must_haves:
  truths:
    - "grd-executor.md, grd-verifier.md, grd-plan-checker.md, grd-roadmapper.md contain zero GSD references"
    - "grd-executor.md describes research outputs (notes, sources, synthesis documents) as valid deliverables"
    - "grd-verifier.md describes its purpose as validating research findings and source completeness"
  artifacts:
    - path: "agents/grd-executor.md"
      provides: "Research-aware executor agent prompt"
      contains: "research"
    - path: "agents/grd-verifier.md"
      provides: "Research-aware verifier agent prompt"
      contains: "research findings"
    - path: "agents/grd-plan-checker.md"
      provides: "GRD-branded plan checker prompt"
      contains: "GRD"
    - path: "agents/grd-roadmapper.md"
      provides: "GRD-branded roadmapper prompt"
      contains: "GRD"
  key_links:
    - from: "agents/grd-executor.md"
      to: "research outputs"
      via: "role description and deviation rules"
      pattern: "research|notes|sources|synthesis|findings"
    - from: "agents/grd-verifier.md"
      to: "research validation"
      via: "role description and core principle"
      pattern: "research findings|source completeness|evidence"
---

<objective>
Fix all "GSD" references in the 4 target agent files and rewrite their role descriptions to acknowledge research outputs alongside code.

Purpose: Agent prompts currently identify as "GSD" tools and describe their purpose in code-centric terms. Researchers using GRD see agents that don't speak their language.
Output: 4 agent files with GRD branding and research-aware descriptions.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-agent-and-workflow-reorientation/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GSD references and add research awareness to grd-executor.md and grd-verifier.md</name>
  <files>agents/grd-executor.md, agents/grd-verifier.md</files>
  <read_first>agents/grd-executor.md, agents/grd-verifier.md</read_first>
  <action>
In agents/grd-executor.md:
1. Line 3 (frontmatter description): Change "Executes GSD plans" to "Executes GRD plans". Change "Spawned by execute-phase orchestrator or execute-plan command." to "Spawned by conduct-inquiry orchestrator or execute-plan command."
2. Line 17 (role): Change "You are a GSD plan executor." to "You are a GRD plan executor."
3. After the existing role paragraph (line 21), add a new paragraph: "You handle both code-producing plans and research-producing plans. Valid deliverables include source code, configuration, research notes, source documents, synthesis reports, and any other artifacts the plan specifies. Apply the same rigor to research outputs (notes with proper citations, source files with provenance, synthesis documents with evidence chains) as to code outputs (passing tests, correct behavior)."
4. In the deviation_rules section, after the SCOPE BOUNDARY paragraph (around line 170-174), the text says "Pre-existing warnings, linting errors, or failures in unrelated files are out of scope." -- leave this as-is since it's generic enough. No other GSD references to fix.

In agents/grd-verifier.md:
1. Line 17 (role): Change "You are a GSD phase verifier." to "You are a GRD phase verifier."
2. After "not just completed its TASKS." on line 17, add: "For research phases, this means verifying that research findings are substantive, sources are complete and properly filed, and the research question has been answered -- not just that note files exist."
3. In the core_principle section (line 43-53), after the existing example about chat component placeholder, add a parallel research example: "Similarly, a task 'investigate SDT motivation theory' can be marked complete when a note file exists with only a title. The task was done -- a file was created -- but the goal 'substantive analysis of SDT' was not achieved."
4. Verify no other "GSD" strings remain anywhere in the file.

Do NOT change any tool names, CLI commands, config keys, or file paths. Only change human-readable descriptions and role text.
  </action>
  <verify>
    <automated>grep -c "GSD" agents/grd-executor.md agents/grd-verifier.md | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GSD" agents/grd-executor.md` returns 0
    - `grep -c "GSD" agents/grd-verifier.md` returns 0
    - `grep -c "GRD plan executor" agents/grd-executor.md` returns 1
    - `grep -c "GRD phase verifier" agents/grd-verifier.md` returns 1
    - `grep -c "research notes" agents/grd-executor.md` returns at least 1
    - `grep -c "research findings" agents/grd-verifier.md` returns at least 1
    - `grep -c "source completeness" agents/grd-verifier.md` returns at least 1
  </acceptance_criteria>
  <done>grd-executor.md and grd-verifier.md have zero "GSD" references, executor describes research outputs as valid deliverables, verifier describes its purpose as validating research findings and source completeness</done>
</task>

<task type="auto">
  <name>Task 2: Fix GSD references in grd-plan-checker.md and grd-roadmapper.md</name>
  <files>agents/grd-plan-checker.md, agents/grd-roadmapper.md</files>
  <read_first>agents/grd-plan-checker.md, agents/grd-roadmapper.md</read_first>
  <action>
In agents/grd-plan-checker.md:
1. Line 11 (role): Change "You are a GSD plan checker." to "You are a GRD plan checker."
2. Verify no other "GSD" strings remain anywhere in the file.

In agents/grd-roadmapper.md:
1. Line 18 (role): Change "You are a GSD roadmapper." to "You are a GRD roadmapper."
2. Verify no other "GSD" strings remain anywhere in the file.

These two files only need the identity line fix -- they don't have additional PM-centric language that needs research reframing (plan-checker talks about plans generically, roadmapper already uses research vocabulary from Phase 27 changes).

Do NOT change any tool names, CLI commands, config keys, or file paths.
  </action>
  <verify>
    <automated>grep -c "GSD" agents/grd-plan-checker.md agents/grd-roadmapper.md | grep -v ":0$" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GSD" agents/grd-plan-checker.md` returns 0
    - `grep -c "GSD" agents/grd-roadmapper.md` returns 0
    - `grep -c "GRD plan checker" agents/grd-plan-checker.md` returns 1
    - `grep -c "GRD roadmapper" agents/grd-roadmapper.md` returns 1
  </acceptance_criteria>
  <done>grd-plan-checker.md and grd-roadmapper.md have zero "GSD" references, both identify as GRD agents</done>
</task>

</tasks>

<verification>
Run across all 4 agent files to confirm zero GSD references and presence of research language:
```bash
grep -c "GSD" agents/grd-executor.md agents/grd-verifier.md agents/grd-plan-checker.md agents/grd-roadmapper.md
grep -l "research" agents/grd-executor.md agents/grd-verifier.md
```
All 4 files should return 0 for GSD count. Both executor and verifier should appear in the research grep.
</verification>

<success_criteria>
- Zero "GSD" strings in any of the 4 agent files
- grd-executor.md mentions research notes, source documents, synthesis reports as valid deliverables
- grd-verifier.md mentions validating research findings and source completeness
- All CLI commands, tool references, and file paths unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/28-agent-and-workflow-reorientation/28-01-SUMMARY.md`
</output>

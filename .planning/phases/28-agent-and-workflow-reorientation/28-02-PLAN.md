---
phase: 28-agent-and-workflow-reorientation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - grd/workflows/scope-inquiry.md
  - grd/workflows/verify-inquiry.md
  - grd/workflows/new-milestone.md
autonomous: true
requirements: [WFL-01, WFL-02, WFL-03]

must_haves:
  truths:
    - "scope-inquiry.md examples use methodology choices, scope boundaries, and theoretical emphasis instead of UI features or sprint planning"
    - "verify-inquiry.md describes validation of research findings, evidence quality, and source coverage instead of feature testing"
    - "new-milestone.md researcher dimension labels read Landscape/Questions/Frameworks/Debates in the display banner"
  artifacts:
    - path: "grd/workflows/scope-inquiry.md"
      provides: "Research-native scope inquiry examples"
      contains: "methodology"
    - path: "grd/workflows/verify-inquiry.md"
      provides: "Research-native verification description"
      contains: "research findings"
    - path: "grd/workflows/new-milestone.md"
      provides: "Research dimension labels in display banner"
      contains: "Landscape, Questions, Frameworks, Debates"
  key_links:
    - from: "grd/workflows/scope-inquiry.md"
      to: "research vocabulary"
      via: "example blocks in scope_guardrail and gray_area_identification"
      pattern: "methodology|scope boundaries|theoretical"
    - from: "grd/workflows/verify-inquiry.md"
      to: "research validation"
      via: "purpose statement and extract_tests step"
      pattern: "research findings|evidence quality|source coverage"
---

<objective>
Rewrite workflow examples and descriptions from PM/feature vocabulary to research vocabulary.

Purpose: Workflow files guide user interaction. When examples reference "UI features", "sprint planning", or "deploying", researchers see a tool not designed for them. Research-native examples (methodology choices, evidence quality, source coverage) make the tool feel natural.
Output: 3 workflow files with research-native examples and descriptions.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-agent-and-workflow-reorientation/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite scope-inquiry.md examples with research vocabulary</name>
  <files>grd/workflows/scope-inquiry.md</files>
  <read_first>grd/workflows/scope-inquiry.md</read_first>
  <action>
Replace PM-style examples in the scope_guardrail section (around lines 41-67) with research equivalents:

CURRENT (lines 44-50):
```
**Allowed (clarifying ambiguity):**
- "How should posts be displayed?" (layout, density, info shown)
- "What happens on empty state?" (within the feature)
- "Pull to refresh or manual?" (behavior choice)

**Not allowed (scope creep):**
- "Should we also add comments?" (new capability)
- "What about search/filtering?" (new capability)
- "Maybe include bookmarking?" (new capability)
```

REPLACE WITH:
```
**Allowed (clarifying ambiguity):**
- "Should we focus on experimental or correlational evidence?" (methodology choice)
- "How far back should the literature search go?" (scope boundary)
- "Which theoretical tradition should anchor the analysis?" (theoretical emphasis)

**Not allowed (scope creep):**
- "Should we also cover motivation theory?" (new research question)
- "What about adding a bibliometric analysis?" (new capability)
- "Maybe include a systematic review protocol?" (new capability)
```

ALSO replace the code context annotation examples (around lines 350-360):

CURRENT:
```
**Code context annotations:** When the scout found relevant existing code, annotate the gray area description:
```
☐ Layout style — Cards vs list vs timeline?
  (You already have a Card component with shadow/rounded variants. Reusing it keeps the app consistent.)
```

**Combining both:** When both prior decisions and code context apply:
```
☐ Loading behavior — Infinite scroll or pagination?
  (You chose infinite scroll in Phase 4. useInfiniteQuery hook already set up.)
```
```

REPLACE WITH:
```
**Research context annotations:** When the scout found relevant existing research, annotate the gray area description:
```
☐ Theoretical scope — Core SDT or include mini-theories?
  (You already have notes on CET from Phase 3. Extending to other mini-theories builds on existing coverage.)
```

**Combining both:** When both prior decisions and research context apply:
```
☐ Evidence standards — Experimental only or include correlational?
  (You chose experimental focus in Phase 4. Existing source log already filtered for RCTs.)
```
```

ALSO replace the "code context" annotation instructions in the discuss_areas step (around line 449-455):

CURRENT:
```
   - **Annotate options with code context** when relevant:
     ```
     "How should posts be displayed?"
     - Cards (reuses existing Card component — consistent with Messages)
     - List (simpler, would be a new pattern)
     - Timeline (needs new Timeline component — none exists yet)
     ```
```

REPLACE WITH:
```
   - **Annotate options with research context** when relevant:
     ```
     "Which theoretical tradition should anchor the analysis?"
     - SDT (builds on existing notes from Phase 3 — consistent coverage)
     - Achievement Goal Theory (would require new literature search)
     - Expectancy-Value (needs new source acquisition — no existing coverage)
     ```
```

Do NOT change any step logic, AskUserQuestion calls, CLI commands, or file paths. Only change example text and annotation labels.
  </action>
  <verify>
    <automated>grep -c "How should posts be displayed\|Pull to refresh\|Layout style.*Cards\|Loading behavior.*Infinite\|Card component with shadow\|useInfiniteQuery" grd/workflows/scope-inquiry.md | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "How should posts be displayed" grd/workflows/scope-inquiry.md` returns 0
    - `grep -c "Pull to refresh" grd/workflows/scope-inquiry.md` returns 0
    - `grep -c "Layout style" grd/workflows/scope-inquiry.md` returns 0
    - `grep -c "Loading behavior.*Infinite" grd/workflows/scope-inquiry.md` returns 0
    - `grep -c "methodology choice" grd/workflows/scope-inquiry.md` returns at least 1
    - `grep -c "scope boundary" grd/workflows/scope-inquiry.md` returns at least 1
    - `grep -c "theoretical emphasis" grd/workflows/scope-inquiry.md` returns at least 1
    - `grep -c "Research context annotations" grd/workflows/scope-inquiry.md` returns at least 1
  </acceptance_criteria>
  <done>scope-inquiry.md examples use methodology choices, scope boundaries, and theoretical emphasis. No PM-style examples (posts, cards, infinite scroll, UI components) remain.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite verify-inquiry.md description and reframe new-milestone.md dimension labels</name>
  <files>grd/workflows/verify-inquiry.md, grd/workflows/new-milestone.md</files>
  <read_first>grd/workflows/verify-inquiry.md, grd/workflows/new-milestone.md</read_first>
  <action>
In grd/workflows/verify-inquiry.md:

1. Line 2 (purpose): Change "Validate built features through conversational testing with persistent state." to "Validate research findings through conversational verification with persistent state."

2. Line 4: Change "User tests, Claude records. One test at a time. Plain text responses." to "Researcher verifies, Claude records. One finding at a time. Plain text responses."

3. Lines 199-200 (active session examples): Replace the PM examples:
CURRENT:
```
| 1 | 04-comments | testing | 3. Reply to Comment | 2/6 |
| 2 | 05-auth | testing | 1. Login Form | 0/4 |
```
REPLACE WITH:
```
| 1 | 04-sdt-motivation | testing | 3. Evidence Chain | 2/6 |
| 2 | 05-cultural-critique | testing | 1. Source Coverage | 0/4 |
```

4. Lines 241-258 (extract_tests step): After the existing instruction "Focus on USER-OBSERVABLE outcomes, not implementation details." add: "For research phases, observable outcomes include: findings answer the research question, sources are filed and logged, evidence chains are traceable, and synthesis documents have proper citations."

Replace the PM example (lines 253-256):
CURRENT:
```
- Accomplishment: "Added comment threading with infinite nesting"
  → Test: "Reply to a Comment"
  → Expected: "Clicking Reply opens inline composer below comment. Submitting shows reply nested under parent with visual indentation."
```
REPLACE WITH:
```
- Accomplishment: "Investigated SDT's account of intrinsic motivation with 6 sources"
  → Test: "Evidence Coverage"
  → Expected: "Research note answers the stated question with citations to at least 3 sources. All cited sources exist in the -sources/ directory with entries in SOURCE-LOG.md."
```

5. Lines 260-268 (Cold-start smoke test): After the server file patterns list and the cold-start test block, add a parallel research smoke test block:
```
**Research note smoke test injection:**

After extracting tests from SUMMARYs, scan the SUMMARY files for modified/created file paths. If ANY path matches these patterns:

`*-Research/*`, `*-sources/*`, `SOURCE-LOG.md`, `*.md` files with `domain:` frontmatter

Then **prepend** this test to the test list:

- name: "Research Note Completeness Smoke Test"
- expected: "Each research note has: (1) a clear research question in frontmatter or title, (2) substantive findings (not just headers/placeholders), (3) citations to sources that exist in the corresponding -sources/ directory, (4) a SOURCE-LOG.md with entries matching the actual source files."
```

In grd/workflows/new-milestone.md:

1. Line 113: Change the display banner text from:
```
  → Stack, Features, Architecture, Pitfalls
```
to:
```
  → Landscape, Questions, Frameworks, Debates
```

This is the ONLY line that needs changing in new-milestone.md. The actual researcher dimension table at line 152 already uses the correct labels (Landscape, Questions, Frameworks, Debates). Line 113 is a display banner that was missed during previous reorientation.

Do NOT change any step logic, CLI commands, state management, severity inference rules, or file paths.
  </action>
  <verify>
    <automated>echo "verify-inquiry:"; grep -c "Validate built features" grd/workflows/verify-inquiry.md; grep -c "Validate research findings" grd/workflows/verify-inquiry.md; echo "new-milestone:"; grep -c "Stack, Features, Architecture, Pitfalls" grd/workflows/new-milestone.md; grep "Landscape, Questions, Frameworks, Debates" grd/workflows/new-milestone.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Validate built features" grd/workflows/verify-inquiry.md` returns 0
    - `grep -c "Validate research findings" grd/workflows/verify-inquiry.md` returns 1
    - `grep -c "Researcher verifies" grd/workflows/verify-inquiry.md` returns 1
    - `grep -c "04-comments" grd/workflows/verify-inquiry.md` returns 0
    - `grep -c "Login Form" grd/workflows/verify-inquiry.md` returns 0
    - `grep -c "comment threading" grd/workflows/verify-inquiry.md` returns 0
    - `grep -c "Evidence Coverage" grd/workflows/verify-inquiry.md` returns at least 1
    - `grep -c "Research Note Completeness" grd/workflows/verify-inquiry.md` returns at least 1
    - `grep -c "Stack, Features, Architecture, Pitfalls" grd/workflows/new-milestone.md` returns 0
    - `grep -c "Landscape, Questions, Frameworks, Debates" grd/workflows/new-milestone.md` returns at least 2
  </acceptance_criteria>
  <done>verify-inquiry.md describes validation of research findings and evidence quality (not feature testing). new-milestone.md display banner reads Landscape/Questions/Frameworks/Debates (not Stack/Features/Architecture/Pitfalls).</done>
</task>

</tasks>

<verification>
Run across all 3 workflow files to confirm research vocabulary:
```bash
echo "=== PM vocabulary that should be gone ==="
grep -n "Validate built features\|How should posts\|Stack, Features, Architecture, Pitfalls\|comment threading\|Login Form\|04-comments\|05-auth\|Card component with shadow\|useInfiniteQuery" grd/workflows/scope-inquiry.md grd/workflows/verify-inquiry.md grd/workflows/new-milestone.md
echo "=== Research vocabulary that should be present ==="
grep -c "methodology\|scope boundary\|theoretical emphasis\|research findings\|evidence\|source coverage\|Landscape.*Questions.*Frameworks.*Debates" grd/workflows/scope-inquiry.md grd/workflows/verify-inquiry.md grd/workflows/new-milestone.md
```
First grep should return nothing. Second grep should return nonzero counts for all 3 files.
</verification>

<success_criteria>
- scope-inquiry.md: All examples use methodology/scope/theory vocabulary, zero PM examples (posts, cards, infinite scroll)
- verify-inquiry.md: Purpose describes research finding validation, examples reference research phases (SDT, evidence chains), not features (comments, auth)
- new-milestone.md: Display banner says "Landscape, Questions, Frameworks, Debates" not "Stack, Features, Architecture, Pitfalls"
- All CLI commands, step logic, and file paths unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/28-agent-and-workflow-reorientation/28-02-SUMMARY.md`
</output>

---
phase: 30-command-reconceptualization----diagnostics-and-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/grd/debug.md
  - grd/workflows/diagnose-issues.md
  - grd/templates/debug-subagent-prompt.md
  - agents/grd-debugger.md
autonomous: true
requirements: [CMD-04]

must_haves:
  truths:
    - "/grd:diagnose investigates methodology gaps, source conflicts, and analytical dead ends -- not code bugs"
    - "The diagnose workflow orchestrates parallel diagnosis agents to investigate research gaps found during verification"
    - "The debugger agent uses scientific method to investigate research methodology issues, not software bugs"
    - "The debug-subagent-prompt template frames issues as research problems, not code problems"
  artifacts:
    - path: "commands/grd/debug.md"
      provides: "CLI route for /grd:diagnose"
      contains: "methodology gaps"
    - path: "grd/workflows/diagnose-issues.md"
      provides: "Orchestrator for parallel diagnosis of research gaps"
      contains: "source conflicts"
    - path: "agents/grd-debugger.md"
      provides: "Agent prompt for research diagnosis"
      contains: "analytical dead ends"
    - path: "grd/templates/debug-subagent-prompt.md"
      provides: "Template for spawning diagnosis agents"
      contains: "methodology"
  key_links:
    - from: "commands/grd/debug.md"
      to: "grd/workflows/diagnose-issues.md"
      via: "execution_context reference"
      pattern: "diagnose-issues"
    - from: "grd/workflows/diagnose-issues.md"
      to: "agents/grd-debugger.md"
      via: "spawns grd-debugger agents"
      pattern: "grd-debugger"
    - from: "grd/templates/debug-subagent-prompt.md"
      to: "agents/grd-debugger.md"
      via: "template fills agent prompt"
      pattern: "grd-debugger"
---

<objective>
Reconceptualize the /grd:debug command end-to-end as /grd:diagnose -- a research diagnosis tool that investigates methodology gaps, source conflicts, and analytical dead ends instead of software bugs.

Purpose: Researchers need a tool to systematically investigate why their research process has gaps -- conflicting sources, weak methodology, analytical dead ends. The existing debug infrastructure (scientific method, hypothesis testing, persistent sessions) maps perfectly to research diagnosis.

Output: 4 rewritten files -- CLI route, workflow, agent prompt, and subagent template -- all speaking research-native language.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite diagnose CLI route, workflow, and subagent template</name>
  <files>commands/grd/debug.md, grd/workflows/diagnose-issues.md, grd/templates/debug-subagent-prompt.md</files>
  <read_first>commands/grd/debug.md, grd/workflows/diagnose-issues.md, grd/templates/debug-subagent-prompt.md</read_first>
  <action>
Rewrite the content of all 3 files to use research-native vocabulary. The structural patterns (YAML frontmatter, XML tags, step names, tool references, subagent spawning) stay identical -- only the conceptual framing changes.

**commands/grd/debug.md:**
- Change `description` from "Systematic debugging with persistent state across context resets" to "Systematic diagnosis of research gaps with persistent state across context resets"
- Rewrite `<objective>` from debugging code issues to diagnosing research methodology gaps, source conflicts, and analytical dead ends
- In symptom gathering (step 2), reframe questions from software symptoms to research symptoms:
  - "Expected behavior" becomes "Expected finding or conclusion"
  - "Actual behavior" becomes "Actual finding or gap encountered"
  - "Error messages" becomes "Conflicting evidence or sources"
  - "Timeline" becomes "When did this gap emerge in the research process?"
  - "Reproduction" becomes "How do you encounter this gap? Which sources or analyses reveal it?"
- In step 3 (spawn agent), reframe the prompt template from bug investigation to research gap investigation
- In step 4 (handle return), reframe options from "Fix now / Plan fix / Manual fix" to "Address gap now / Plan gap closure / Manual resolution"
- Keep all structural elements: YAML frontmatter keys, allowed-tools list, process steps, grd-tools.cjs calls, Task() spawning patterns

**grd/workflows/diagnose-issues.md:**
- Rewrite `<purpose>` from "orchestrate parallel debug agents to investigate UAT gaps" to "orchestrate parallel diagnosis agents to investigate research verification gaps"
- Rewrite `<core_principle>` from code debugging analogy to research analogy: "Verification tells us WHAT gap exists (symptoms). Diagnosis agents find WHY (root cause). plan-inquiry --gaps then creates targeted closure based on actual causes."
  - Replace the code example (useEffect missing dependency) with a research example (e.g., "Source conflict between Smith 2020 and Jones 2021 on methodology" -> "Smith's sample excluded the population Jones studied" -> precise resolution)
- In parse_gaps step, keep YAML structure but reframe field descriptions:
  - "truth" stays (it's already research-compatible from verify-inquiry)
  - "severity" stays
  - Ensure examples use research language, not code language
- In report_plan step, reframe table headers and descriptions for research context
- In spawn_agents step, keep Task() pattern but reframe template placeholders for research context
- In collect_results step, reframe "Root Cause" as "Diagnosis" -- but keep the structured return format identical
- In update_uat step, reframe "root_cause" field descriptions but keep the YAML structure
- In report_results step, reframe banner and table for research context
- Keep all structural elements: step names, bash commands, grd-tools.cjs paths, commit patterns

**grd/templates/debug-subagent-prompt.md:**
- Reframe the template description from bug investigation to research gap investigation
- In the template itself, reframe placeholders:
  - `{issue_summary}` -> keep name but describe as "research gap description"
  - `{expected}` -> "Expected finding or conclusion"
  - `{actual}` -> "Actual finding or gap encountered"
  - `{errors}` -> "Conflicting evidence or anomalous findings"
  - `{reproduction}` -> "How the gap manifests in the research"
  - `{timeline}` -> "When this gap emerged"
- In the Placeholders table, update Source and Example columns with research examples
- In the Continuation template, keep structure, reframe description
- Keep all structural elements: markdown template format, Task() examples, placeholder table format

CRITICAL: Do NOT change any file paths, tool names, function calls, grd-tools.cjs references, agent names (grd-debugger), directory paths (.planning/debug/), or YAML frontmatter key names. Only change descriptive text, examples, and conceptual framing.
  </action>
  <verify>
    <automated>grep -c "methodology\|source conflict\|analytical dead end\|research gap\|evidence" commands/grd/debug.md grd/workflows/diagnose-issues.md grd/templates/debug-subagent-prompt.md | grep -v ':0$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "bug\|code bug\|software bug" commands/grd/debug.md returns 0
    - grep -c "bug\|code bug\|software bug" grd/workflows/diagnose-issues.md returns 0
    - grep -c "methodology" commands/grd/debug.md returns at least 1
    - grep -c "source conflict\|conflicting" grd/workflows/diagnose-issues.md returns at least 1
    - grep "grd-debugger" commands/grd/debug.md returns matches (agent name preserved)
    - grep "grd-tools.cjs" commands/grd/debug.md returns matches (tool paths preserved)
    - grep ".planning/debug/" grd/workflows/diagnose-issues.md returns matches (directory paths preserved)
  </acceptance_criteria>
  <done>CLI route describes research diagnosis, workflow orchestrates research gap investigation, template frames research problems -- all structural elements (paths, tools, agent names) preserved</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite grd-debugger agent prompt for research diagnosis</name>
  <files>agents/grd-debugger.md</files>
  <read_first>agents/grd-debugger.md</read_first>
  <action>
Rewrite the 1257-line grd-debugger agent prompt to frame diagnosis as research methodology investigation rather than code debugging. The scientific method backbone is domain-agnostic and stays -- only the application domain shifts from software to research.

**Frontmatter:**
- Change `description` from "Investigates bugs using scientific method..." to "Investigates research gaps using scientific method, manages diagnosis sessions, handles checkpoints. Spawned by /grd:diagnose orchestrator."

**`<role>` section:**
- Change "You are a GSD debugger" to "You are a GRD research diagnostician"
- Change "investigate bugs" to "investigate research gaps -- methodology weaknesses, source conflicts, analytical dead ends, and evidence gaps"
- Change spawned-by list to reference `/grd:diagnose` command and `diagnose-issues` workflow
- Change core responsibilities to: find root cause of research gaps through hypothesis testing, maintain diagnosis file state, optionally resolve and verify

**`<philosophy>` section:**
- "User = Reporter, Claude = Investigator" stays but reframe what user knows:
  - User knows: what finding they expected, what they actually found (or failed to find), which sources conflict, when the gap appeared
  - User does NOT know: why sources conflict, which methodology is flawed, what evidence is missing
- "Meta-Debugging: Your Own Code" becomes "Meta-Diagnosis: Your Own Research" -- when diagnosing gaps in research you conducted, you're fighting your own analytical assumptions
- "Foundation Principles" stays nearly identical (already domain-agnostic)
- "Cognitive Biases" table stays identical (already applicable to research)
- "Systematic Investigation Disciplines" stays (already domain-agnostic)
- "When to Restart" reframe from code hours to research investigation hours

**`<hypothesis_testing>` section:**
- Reframe "Bad (unfalsifiable)" examples from code ("Something is wrong with the state") to research ("Something is wrong with the methodology")
- Reframe "Good (falsifiable)" examples to research: "Source A's sample excluded population X, causing divergent findings from Source B"
- Replace JavaScript code example in "Multiple Hypotheses Strategy" with a research methodology example showing parallel hypothesis testing for a source conflict
- Keep all subsections (Falsifiability, Forming Hypotheses, Experimental Design, Evidence Quality, Decision Point, Recovery, Multiple Hypotheses, Pitfalls) -- just reframe examples

**`<investigation_techniques>` section:**
- Reframe each technique for research context:
  - "Binary Search" -> narrowing which part of the research pipeline introduced the gap (data collection? analysis? interpretation?)
  - "Rubber Duck Debugging" -> "Rubber Duck Diagnosis" (explain the research gap aloud)
  - "Minimal Reproduction" -> strip away complexity until the core source conflict or methodology gap is isolated
  - "Working Backwards" -> trace from expected conclusion back to where evidence diverges
  - "Differential Debugging" -> compare what changed between when the research was coherent and when the gap appeared
  - "Observability First" -> add evidence logging/annotation before changing methodology
  - "Comment Out Everything" -> temporarily exclude sources or analytical steps to isolate the gap
  - "Git Bisect" -> trace which addition to the research (source, note, analytical step) introduced the inconsistency
- Replace all JavaScript/code examples with research equivalents
- "Technique Selection" table: reframe situations for research context

**`<verification_patterns>` section:**
- Reframe "What Verified Means" for research: original gap no longer exists, you understand why the resolution works, related findings still hold, resolution is stable
- Reframe all subsections from code verification to research verification
- Replace code examples with research equivalents

**`<research_vs_reasoning>` section:**
- This section is about when to use external research vs internal reasoning during diagnosis -- it applies equally to research diagnosis. Reframe the specific triggers:
  - "Error messages you don't recognize" -> "Methodological critiques from reviewers you don't understand"
  - "Library/framework behavior" -> "Analytical framework producing unexpected results"
  - "Domain knowledge gaps" -> same (already applicable)
  - "Platform-specific behavior" -> "Discipline-specific norms (what's acceptable in psychology vs sociology)"

**`<debug_file_protocol>` section:**
- Keep file structure identical but reframe field descriptions:
  - "trigger" -> "verbatim gap description"
  - "hypothesis" -> stays (already scientific)
  - "Symptoms" -> "Gap Description" (expected finding, actual finding, conflicting evidence, how gap manifests, when gap emerged)
  - "Resolution" -> stays but reframe: root_cause becomes "diagnosis", fix becomes "resolution approach"

**`<execution_flow>` section:**
- Reframe step descriptions but keep step names and structural patterns identical
- "symptom_gathering" -> gather gap description through questioning
- "investigation_loop" -> autonomous investigation of research gap
- "fix_and_verify" -> resolve gap and verify resolution
- Keep all bash commands, file paths, status transitions, grd-tools.cjs calls unchanged

**`<checkpoint_behavior>`, `<structured_returns>`, `<modes>`, `<success_criteria>` sections:**
- Reframe descriptive text for research context
- Keep all structural patterns, return formats, and mode flags identical

CRITICAL CONSTRAINTS:
- Do NOT change: file paths (.planning/debug/), tool names (Read, Write, Bash, etc.), grd-tools.cjs references, status values (gathering, investigating, fixing, verifying, resolved), frontmatter YAML keys (status, trigger, created, updated), section names in debug file protocol (Current Focus, Symptoms, Eliminated, Evidence, Resolution), mode flags (symptoms_prefilled, find_root_cause_only, find_and_fix)
- Do NOT change the agent name "grd-debugger" in the frontmatter
- The scientific method structure is the backbone -- preserve it entirely, just reframe the domain from software to research
  </action>
  <verify>
    <automated>grep -c "methodology\|source conflict\|analytical dead end\|research gap\|evidence gap\|research diagnostician" agents/grd-debugger.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "code bug\|software bug\|codebase" agents/grd-debugger.md returns 0 (no PM/code vocabulary in descriptive text)
    - grep "grd-debugger" agents/grd-debugger.md returns matches (agent name preserved in frontmatter)
    - grep ".planning/debug/" agents/grd-debugger.md returns matches (directory paths preserved)
    - grep "gathering.*investigating.*fixing.*verifying" agents/grd-debugger.md returns match (status values preserved)
    - grep "find_root_cause_only\|find_and_fix" agents/grd-debugger.md returns matches (mode flags preserved)
    - grep "research diagnostician\|methodology" agents/grd-debugger.md returns at least 2 matches
    - wc -l agents/grd-debugger.md shows file is within 15% of original 1257 lines (no major truncation or bloat)
  </acceptance_criteria>
  <done>grd-debugger agent describes itself as a research diagnostician investigating methodology gaps, source conflicts, and analytical dead ends using scientific method -- all structural elements preserved</done>
</task>

</tasks>

<verification>
Run across all 4 modified files:

1. Zero PM/code-bug vocabulary in descriptive text:
   ```bash
   grep -i "code bug\|software bug\|codebase\|unit test\|feature.*broken" commands/grd/debug.md grd/workflows/diagnose-issues.md grd/templates/debug-subagent-prompt.md agents/grd-debugger.md
   ```
   Expected: no output

2. Research vocabulary present:
   ```bash
   grep -c "methodology\|source conflict\|analytical\|evidence\|research" commands/grd/debug.md grd/workflows/diagnose-issues.md grd/templates/debug-subagent-prompt.md agents/grd-debugger.md
   ```
   Expected: each file shows at least 1 match

3. Structural elements preserved:
   ```bash
   grep "grd-debugger\|grd-tools.cjs\|.planning/debug/" commands/grd/debug.md grd/workflows/diagnose-issues.md agents/grd-debugger.md
   ```
   Expected: matches in all files
</verification>

<success_criteria>
- /grd:diagnose CLI route describes research gap diagnosis, not code debugging
- diagnose-issues workflow orchestrates research gap investigation with research examples
- grd-debugger agent uses scientific method for research methodology diagnosis
- debug-subagent-prompt template frames issues as research problems
- All file paths, tool names, agent names, status values, and mode flags unchanged
- Each command's workflow file, agent prompt, and CLI route updated end-to-end (per CMD-04)
</success_criteria>

<output>
After completion, create `.planning/phases/30-command-reconceptualization----diagnostics-and-corpus/30-01-SUMMARY.md`
</output>

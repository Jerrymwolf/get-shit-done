---
phase: 30-command-reconceptualization----diagnostics-and-corpus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/grd/map-codebase.md
  - grd/workflows/map-codebase.md
  - agents/grd-codebase-mapper.md
  - commands/grd/add-tests.md
  - grd/workflows/add-tests.md
autonomous: true
requirements: [CMD-05, CMD-02]

must_haves:
  truths:
    - "/grd:map-corpus surveys existing sources and the knowledge landscape of a research project -- not a codebase"
    - "/grd:add-verification adds evidence checks and source coverage assertions -- not software test cases"
    - "map-corpus workflow spawns mapper agents that write corpus analysis documents, not codebase analysis documents"
    - "add-verification workflow classifies research artifacts for verification, not code files for testing"
  artifacts:
    - path: "commands/grd/map-codebase.md"
      provides: "CLI route for /grd:map-corpus"
      contains: "corpus"
    - path: "grd/workflows/map-codebase.md"
      provides: "Orchestrator for parallel corpus mapping"
      contains: "knowledge landscape"
    - path: "commands/grd/add-tests.md"
      provides: "CLI route for /grd:add-verification"
      contains: "evidence checks"
    - path: "grd/workflows/add-tests.md"
      provides: "Workflow for adding verification criteria to research phases"
      contains: "source coverage"
  key_links:
    - from: "commands/grd/map-codebase.md"
      to: "grd/workflows/map-codebase.md"
      via: "execution_context reference"
      pattern: "map-codebase"
    - from: "commands/grd/add-tests.md"
      to: "grd/workflows/add-tests.md"
      via: "execution_context reference"
      pattern: "add-tests"
    - from: "grd/workflows/map-codebase.md"
      to: "agents/grd-codebase-mapper.md"
      via: "spawns grd-codebase-mapper agents"
      pattern: "grd-codebase-mapper"
---

<objective>
Reconceptualize /grd:map-codebase and /grd:add-tests as research-native tools: /grd:map-corpus surveys existing sources and the knowledge landscape; /grd:add-verification adds evidence checks and source coverage assertions.

Purpose: Researchers need to (1) understand the corpus of sources they're working with (survey the landscape), and (2) add verification criteria to completed research phases (did we cover enough sources? is evidence strong enough?). The existing infrastructure (parallel mapper agents, file classification, test plan approval) maps to these research needs.

Output: 5 rewritten files -- 2 CLI routes, 2 workflows, and 1 agent file -- all speaking research-native language.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite map-codebase CLI route, workflow, and mapper agent for corpus mapping</name>
  <files>commands/grd/map-codebase.md, grd/workflows/map-codebase.md, agents/grd-codebase-mapper.md</files>
  <read_first>commands/grd/map-codebase.md, grd/workflows/map-codebase.md, agents/grd-codebase-mapper.md</read_first>
  <action>
Rewrite the content of all 3 files to use research-native vocabulary. Structural patterns (YAML frontmatter, XML tags, step names, tool references, agent spawning) stay identical -- only conceptual framing changes.

**commands/grd/map-codebase.md:**
- Change `description` from "Analyze codebase with parallel mapper agents to produce .planning/codebase/ documents" to "Survey research corpus with parallel mapper agents to produce .planning/codebase/ analysis documents"
- Change `argument-hint` from "specific area to map, e.g., 'api' or 'auth'" to "specific domain to survey, e.g., 'methodology' or 'theoretical framework'"
- Rewrite `<objective>` from codebase analysis to corpus/literature landscape survey. Output is still .planning/codebase/ folder (path unchanged) but described as "structured documents about the research corpus and knowledge landscape"
- Rewrite `<when_to_use>` section:
  - "Brownfield projects" -> "Projects with existing source materials and notes"
  - "Refreshing codebase map" -> "Refreshing corpus survey after acquiring new sources"
  - "Onboarding to an unfamiliar codebase" -> "Onboarding to an unfamiliar research domain"
  - "Before major refactoring" -> "Before major analytical reorientation"
  - "Skip for greenfield" -> "Skip for new research projects with no existing sources"
- Rewrite `<process>` list items with research vocabulary but keep structure
- Keep all structural elements: YAML frontmatter keys, allowed-tools, execution_context path, grd-tools.cjs calls

**grd/workflows/map-codebase.md:**
- Rewrite `<purpose>` from "orchestrate parallel codebase mapper agents" to "orchestrate parallel corpus mapper agents to analyze research sources and produce structured documents"
- Rewrite `<philosophy>` section:
  - "Fresh context per domain" stays (applicable)
  - "Document quality over length" stays
  - "Always include file paths" -> "Always include source references" (but keep backtick formatting advice)
- In `init_context` step, keep grd-tools.cjs call and JSON extraction unchanged
- In `check_existing` step, keep .planning/codebase/ path checks, reframe user options:
  - "Refresh - Delete existing and remap codebase" -> "Refresh - Delete existing and resurvey corpus"
  - "Update - Keep existing, only update specific documents" -> stays
  - "Skip - Use existing codebase map as-is" -> "Skip - Use existing corpus survey as-is"
- In `create_structure` step, reframe expected output file descriptions:
  - STACK.md -> "Sources and databases" (was "from tech mapper")
  - INTEGRATIONS.md -> "External connections and cross-references"
  - ARCHITECTURE.md -> "Theoretical framework and analytical structure"
  - STRUCTURE.md -> "Corpus organization and taxonomy"
  - CONVENTIONS.md -> "Methodological conventions and standards"
  - TESTING.md -> "Verification criteria and evidence thresholds"
  - CONCERNS.md -> "Gaps, limitations, and areas of uncertainty"
- In `spawn_agents` step, reframe agent prompts:
  - Agent 1 (Tech): "Analyze sources, databases, reference management tools, and dependencies" -> writes STACK.md, INTEGRATIONS.md
  - Agent 2 (Arch): "Analyze theoretical framework, analytical structure, and knowledge organization" -> writes ARCHITECTURE.md, STRUCTURE.md
  - Agent 3 (Quality): "Analyze methodological conventions, citation standards, and evidence thresholds" -> writes CONVENTIONS.md, TESTING.md
  - Agent 4 (Concerns): "Analyze research gaps, limitations, conflicting evidence, and areas of uncertainty" -> writes CONCERNS.md
- In `sequential_mapping` step, apply same reframing for each pass
- In `verify_output` step, keep bash commands and verification checklist unchanged
- In `scan_for_secrets` step, keep entirely unchanged (still security-relevant)
- In `commit_codebase_map` and `offer_next` steps, reframe descriptions:
  - "Codebase mapping complete" -> "Corpus survey complete"
  - Descriptions for each document in the summary use research vocabulary
  - Next steps: `/grd:new-research` stays, `/grd:map-codebase` stays (filename unchanged)
- Keep all structural elements: step names, bash commands, file paths (.planning/codebase/), grd-tools.cjs calls, Task() patterns, mkdir commands

**agents/grd-codebase-mapper.md:**
- This file is currently EMPTY (0 lines). Write a minimal agent prompt (the mapper agents are simple -- they receive focus area and write documents):
  - Frontmatter with name: grd-codebase-mapper, description about surveying research corpus
  - Brief role section: "You are a GRD corpus mapper. You survey research sources, notes, and analytical structures within a specific focus area and write structured analysis documents."
  - Instructions to read the project broadly within the assigned focus area and write documents to .planning/codebase/
  - Note: keep agent name as grd-codebase-mapper (filename unchanged)
  - Target: ~30-50 lines (these agents are document-writers, not complex reasoners)

CRITICAL: Do NOT change any file paths (.planning/codebase/), tool names, grd-tools.cjs references, agent names (grd-codebase-mapper), document filenames (STACK.md, ARCHITECTURE.md, etc.), or YAML frontmatter key names. Only change descriptive text, examples, and conceptual framing.
  </action>
  <verify>
    <automated>grep -c "corpus\|knowledge landscape\|research source\|literature\|survey" commands/grd/map-codebase.md grd/workflows/map-codebase.md | grep -v ':0$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "codebase" commands/grd/map-codebase.md returns 0 or only appears in file path references (map-codebase.md, .planning/codebase/)
    - grep "corpus\|knowledge landscape" commands/grd/map-codebase.md returns at least 1 match
    - grep "corpus\|knowledge landscape\|research source" grd/workflows/map-codebase.md returns at least 2 matches
    - grep ".planning/codebase/" grd/workflows/map-codebase.md returns matches (directory paths preserved)
    - grep "grd-codebase-mapper" grd/workflows/map-codebase.md returns matches (agent name preserved)
    - grep "grd-tools.cjs" grd/workflows/map-codebase.md returns matches (tool paths preserved)
    - wc -l agents/grd-codebase-mapper.md shows file has at least 20 lines (no longer empty)
    - grep "corpus\|research" agents/grd-codebase-mapper.md returns at least 1 match
  </acceptance_criteria>
  <done>map-codebase CLI describes corpus survey, workflow orchestrates research landscape mapping, agent describes itself as corpus mapper -- all file paths, tool names, and agent names preserved</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite add-tests CLI route and workflow for research verification</name>
  <files>commands/grd/add-tests.md, grd/workflows/add-tests.md</files>
  <read_first>commands/grd/add-tests.md, grd/workflows/add-tests.md</read_first>
  <action>
Rewrite both files to frame test generation as research verification criteria addition. Structural patterns stay identical.

**commands/grd/add-tests.md:**
- Change `description` from "Generate tests for a completed phase based on UAT criteria and implementation" to "Add verification criteria for a completed research phase based on findings and evidence quality"
- Change `argument-hint` from "&lt;phase&gt; [additional instructions]" -- keep the format, reframe the example:
  - "Example: /grd:add-tests 12 focus on edge cases in the pricing module" -> "Example: /grd:add-tests 12 focus on source coverage in the methodology section"
- Rewrite `<objective>` from generating tests to adding verification criteria:
  - "Generate unit and E2E tests" -> "Add evidence checks and source coverage assertions"
  - "SUMMARY.md, CONTEXT.md, and VERIFICATION.md as specifications" -> "SUMMARY.md and CONTEXT.md as specifications for what needs verification"
  - Output: "Verification criteria committed" (not "Test files committed")
- Keep execution_context reference to add-tests.md workflow unchanged
- Keep context references to STATE.md, ROADMAP.md unchanged
- Keep process description but reframe: "Execute the add-tests workflow... Preserve all workflow gates (classification approval, verification plan approval, evidence checking, gap reporting)"

**grd/workflows/add-tests.md:**
- Rewrite `<purpose>` from test generation to verification criteria addition:
  - "Generate unit and E2E tests for a completed phase" -> "Add verification criteria for a completed research phase -- evidence checks, source coverage assertions, and finding validation"
  - "Classifies each changed file into TDD (unit), E2E (browser), or Skip" -> "Classifies each research artifact into Evidence Check (source verification), Coverage Assertion (completeness check), or Skip"
- In `parse_arguments` step, keep structure, reframe error message example
- In `init_context` step, keep grd-tools.cjs call, reframe banner:
  - "ADD TESTS" -> "ADD VERIFICATION" in the banner display
- In `analyze_implementation` step, reframe classification categories:
  - **Evidence Check** (was TDD): "Research notes with specific claims that can be verified against sources -- factual assertions, statistical claims, methodology descriptions"
  - **Coverage Assertion** (was E2E): "Research phases where completeness of source coverage, domain coverage, or analytical coverage can be assessed"
  - **Skip** (stays): "Administrative artifacts, planning documents, configuration -- not meaningfully verifiable"
  - Reframe the classification criteria tables entirely:
    - Evidence Check: factual claims, statistical assertions, methodology descriptions, source attributions, quoted findings
    - Coverage Assertion: literature completeness, methodology coverage, analytical breadth, cross-referencing completeness
    - Skip: planning docs, templates, configuration files, administrative notes
- In `present_classification` step, reframe section headers:
  - "TDD (Unit Tests)" -> "Evidence Checks"
  - "E2E (Browser Tests)" -> "Coverage Assertions"
  - "Skip" stays
- In `discover_test_structure` step, reframe for research verification:
  - Instead of finding test directories and frameworks, look for existing verification criteria, evidence notes, and source coverage patterns
  - Look for .planning/phases/*/VERIFICATION.md patterns
  - Identify how prior phases documented verification
- In `generate_test_plan` step:
  - "For TDD files" -> "For Evidence Check artifacts": identify verifiable claims, list the claim, the source it references, and how to verify
  - "For E2E files" -> "For Coverage Assertion artifacts": identify completeness criteria from CONTEXT.md, describe what coverage looks like, what gaps would indicate failure
- In `execute_tdd_generation` step -> `execute_evidence_checks`:
  - Instead of writing test files with arrange/act/assert, write verification criteria documents or checklists
  - "Run the test" -> "Verify the claim against sources"
  - "Test passes" -> "Claim verified" / "Test fails with assertion error" -> "Claim contradicted or unsupported"
  - Flag contradictions as findings, not bugs
- In `execute_e2e_generation` step -> `execute_coverage_assertions`:
  - Instead of browser tests, assess source coverage completeness
  - Check whether all domains mentioned in CONTEXT.md have corresponding notes/sources
  - Report coverage gaps
- In `summary_and_commit` step:
  - Reframe table: "Unit" -> "Evidence Checks", "E2E" -> "Coverage Assertions"
  - "Bugs Discovered" -> "Contradictions or Gaps Discovered"
  - Commit message: "verify(phase-${phase_number}): add verification criteria from add-verification command"
  - Next steps reference /grd:add-verification (not add-tests)

CRITICAL: Do NOT change file paths, grd-tools.cjs references, step names in the XML structure, AskUserQuestion tool usage, or YAML frontmatter key names. Only change descriptive text, examples, classification criteria, and conceptual framing.
  </action>
  <verify>
    <automated>grep -c "evidence check\|source coverage\|verification criteria\|coverage assertion" commands/grd/add-tests.md grd/workflows/add-tests.md | grep -v ':0$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "unit test\|E2E test\|browser test" grd/workflows/add-tests.md returns 0 (no PM/code test vocabulary)
    - grep "evidence check\|source coverage" grd/workflows/add-tests.md returns at least 2 matches
    - grep "verification criteria" commands/grd/add-tests.md returns at least 1 match
    - grep "grd-tools.cjs" grd/workflows/add-tests.md returns matches (tool paths preserved)
    - grep "AskUserQuestion" grd/workflows/add-tests.md returns matches (tool usage preserved)
    - grep "SUMMARY.md" grd/workflows/add-tests.md returns matches (artifact references preserved)
  </acceptance_criteria>
  <done>add-tests CLI describes research verification criteria, workflow classifies research artifacts for evidence checks and coverage assertions -- all structural elements preserved</done>
</task>

</tasks>

<verification>
Run across all 5 modified files:

1. Zero PM/code-test vocabulary in descriptive text:
   ```bash
   grep -i "unit test\|E2E test\|browser test\|codebase analysis" commands/grd/map-codebase.md grd/workflows/map-codebase.md commands/grd/add-tests.md grd/workflows/add-tests.md agents/grd-codebase-mapper.md
   ```
   Expected: no output (or only in filename/path references)

2. Research vocabulary present:
   ```bash
   grep -c "corpus\|evidence\|source coverage\|verification criteria\|knowledge landscape\|methodology" commands/grd/map-codebase.md grd/workflows/map-codebase.md commands/grd/add-tests.md grd/workflows/add-tests.md agents/grd-codebase-mapper.md
   ```
   Expected: each file shows at least 1 match

3. Structural elements preserved:
   ```bash
   grep "grd-codebase-mapper\|grd-tools.cjs\|.planning/codebase/" grd/workflows/map-codebase.md
   ```
   Expected: matches for all three patterns
</verification>

<success_criteria>
- /grd:map-corpus CLI route describes corpus/literature survey, not codebase mapping
- map-codebase workflow orchestrates research landscape survey with parallel agents
- grd-codebase-mapper agent (formerly empty) has a minimal research-native prompt
- /grd:add-verification CLI route describes evidence checks and source coverage assertions
- add-tests workflow classifies research artifacts and generates verification criteria
- All file paths, tool names, agent names, and grd-tools.cjs references unchanged
- Each command's workflow file, agent prompt, and CLI route updated end-to-end (per CMD-05, CMD-02)
</success_criteria>

<output>
After completion, create `.planning/phases/30-command-reconceptualization----diagnostics-and-corpus/30-02-SUMMARY.md`
</output>

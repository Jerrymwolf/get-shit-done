---
phase: 07-cli-wiring-and-agent-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grd/bin/grd-tools.cjs
autonomous: true
requirements:
  - KNOW-02
  - KNOW-03
  - ORCH-06
  - VERI-04
gap_closure: true

must_haves:
  truths:
    - "state add-note, update-note-status, get-notes subcommands respond with correct JSON output"
    - "state add-gap, resolve-gap, get-gaps subcommands respond with correct JSON output"
    - "verify research-plan reads a plan file and BOOTSTRAP.md, runs validateResearchPlan, returns issues JSON"
    - "bootstrap generate reads findings JSON, runs generateBootstrap, writes BOOTSTRAP.md content"
  artifacts:
    - path: "grd/bin/grd-tools.cjs"
      provides: "CLI routes for all 6 state note/gap commands, verify research-plan, bootstrap generate"
      contains: "add-note"
  key_links:
    - from: "grd-tools.cjs state add-note"
      to: "state.cjs cmdStateAddNote"
      via: "CLI switch case dispatching to imported function"
      pattern: "cmdStateAddNote"
    - from: "grd-tools.cjs state add-gap"
      to: "state.cjs cmdStateAddGap"
      via: "CLI switch case dispatching to imported function"
      pattern: "cmdStateAddGap"
    - from: "grd-tools.cjs verify research-plan"
      to: "plan-checker-rules.cjs validateResearchPlan"
      via: "CLI switch case reading files and calling validateResearchPlan"
      pattern: "validateResearchPlan"
    - from: "grd-tools.cjs bootstrap generate"
      to: "bootstrap.cjs generateBootstrap"
      via: "CLI switch case calling generateBootstrap"
      pattern: "generateBootstrap"
---

<objective>
Wire all implemented-but-disconnected functions into the CLI tool layer in grd-tools.cjs.

Purpose: 6 state.cjs functions, validateResearchPlan from plan-checker-rules.cjs, and generateBootstrap from bootstrap.cjs are all implemented and tested but have no CLI routes. Agents cannot call them through the standard tool interface. This plan adds all missing CLI switch cases in one pass.

Output: grd-tools.cjs with 8 new CLI routes, all existing tests still passing.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/grd/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/grd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- From grd/bin/lib/state.cjs — functions to wire as CLI subcommands -->

cmdStateAddNote(cwd, options, raw)
  options: { note: string, status: string, sources?: string, date?: string }
  Adds/updates a row in STATE.md Note Status table

cmdStateUpdateNoteStatus(cwd, note, status, raw)
  Updates status column for an existing note

cmdStateGetNotes(cwd, raw)
  Returns { notes: [{ note, status, sources, last_updated }] }

cmdStateAddGap(cwd, options, raw)
  options: { note: string, source: string, reason?: string, impact?: string }
  Adds a row to STATE.md Source Gaps table

cmdStateResolveGap(cwd, note, source, raw)
  Removes matching row from Source Gaps table

cmdStateGetGaps(cwd, raw)
  Returns { gaps: [{ note, source, reason, impact }] }

<!-- From grd/bin/lib/plan-checker-rules.cjs -->

validateResearchPlan(planContent, bootstrapContent)
  Returns { valid: boolean, issues: string[] }
  Runs 4 checks: source duplication, source limits, acquisition methods, context budget

<!-- From grd/bin/lib/bootstrap.cjs -->

generateBootstrap(findings)
  findings: { established?: object[], partial?: object[], notResearched?: object[] }
  Returns string (BOOTSTRAP.md markdown content)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add state note/gap CLI subcommands to grd-tools.cjs</name>
  <files>grd/bin/grd-tools.cjs</files>
  <action>
In the `case 'state':` switch block in grd-tools.cjs, add 6 new `else if` branches after the existing `record-session` branch and before the final `else` (which calls cmdStateLoad):

1. `add-note` subcommand:
   - Parse `--note`, `--status`, `--sources`, `--date` from args
   - Call `state.cmdStateAddNote(cwd, { note, status, sources, date }, raw)`
   - CLI: `grd-tools.cjs state add-note --note "my-note" --status draft --sources 3`

2. `update-note-status` subcommand:
   - Parse `--note`, `--status` from args
   - Call `state.cmdStateUpdateNoteStatus(cwd, note, status, raw)`
   - CLI: `grd-tools.cjs state update-note-status --note "my-note" --status reviewed`

3. `get-notes` subcommand:
   - No args needed
   - Call `state.cmdStateGetNotes(cwd, raw)`
   - CLI: `grd-tools.cjs state get-notes`

4. `add-gap` subcommand:
   - Parse `--note`, `--source`, `--reason`, `--impact` from args
   - Call `state.cmdStateAddGap(cwd, { note, source, reason, impact }, raw)`
   - CLI: `grd-tools.cjs state add-gap --note "my-note" --source "paper.pdf" --reason "paywall"`

5. `resolve-gap` subcommand:
   - Parse `--note`, `--source` from args
   - Call `state.cmdStateResolveGap(cwd, note, source, raw)`
   - CLI: `grd-tools.cjs state resolve-gap --note "my-note" --source "paper.pdf"`

6. `get-gaps` subcommand:
   - No args needed
   - Call `state.cmdStateGetGaps(cwd, raw)`
   - CLI: `grd-tools.cjs state get-gaps`

Follow the exact arg-parsing pattern already used in the file (indexOf for named args). No new imports needed -- state.cjs is already imported.

Also update the header comment block to document these 6 new subcommands under the State Progression section.
  </action>
  <verify>
    <automated>cd /Users/jeremiahwolf/Desktop/Projects/APPs/GSDR && node grd/bin/grd-tools.cjs state add-note --note "test-note" --status draft --sources 0 --cwd /tmp/gsdr-test-07 2>&1 || echo "EXPECTED_FAIL_NO_STATE"</automated>
  </verify>
  <done>All 6 state subcommands parse args and dispatch to the correct state.cjs functions. Existing test suite still passes: `node --test test/state.test.cjs`</done>
</task>

<task type="auto">
  <name>Task 2: Add verify research-plan and bootstrap generate CLI routes</name>
  <files>grd/bin/grd-tools.cjs</files>
  <action>
Two changes to grd-tools.cjs:

**A. Add `verify research-plan` subcommand:**

In the `case 'verify':` switch block, add an `else if` branch for `research-plan`:

```
} else if (subcommand === 'research-plan') {
  const planPath = args[2];
  const bootstrapIdx = args.indexOf('--bootstrap');
  const bootstrapPath = bootstrapIdx !== -1 ? args[bootstrapIdx + 1] : null;
```

- Read plan file content from `planPath` (resolve relative to cwd)
- Read bootstrap file content from `bootstrapPath` if provided; if not provided, try `.planning/BOOTSTRAP.md` relative to cwd; if neither exists, use empty string
- `require('./lib/plan-checker-rules.cjs')` at the top of the file (add import alongside existing requires)
- Call `planCheckerRules.validateResearchPlan(planContent, bootstrapContent)`
- Output the result `{ valid, issues }` using the `output()` helper from core.cjs (already available via the `error` import pattern -- use the same output pattern as verify.cjs commands)

CLI: `grd-tools.cjs verify research-plan path/to/PLAN.md --bootstrap path/to/BOOTSTRAP.md`

**B. Add `bootstrap generate` command:**

Add a new top-level `case 'bootstrap':` in the main switch block (after the `verify` case):

```
case 'bootstrap': {
  const subcommand = args[1];
  if (subcommand === 'generate') {
    const dataIdx = args.indexOf('--data');
    ...
  }
}
```

- `require('./lib/bootstrap.cjs')` at the top of the file (add import)
- Parse `--data` flag containing JSON string of findings: `{ established: [...], partial: [...], notResearched: [...] }`
- Also support `--output` flag for optional output file path
- Call `bootstrap.generateBootstrap(findings)`
- If `--output` provided, write result to file; otherwise output to stdout
- Output `{ generated: true, content: result }` or write file and output `{ generated: true, path: outputPath }`

CLI: `grd-tools.cjs bootstrap generate --data '{"established":[],"partial":[],"notResearched":[]}' --output BOOTSTRAP.md`

Also update the top-of-file header comment to document both new routes.
  </action>
  <verify>
    <automated>cd /Users/jeremiahwolf/Desktop/Projects/APPs/GSDR && node grd/bin/grd-tools.cjs verify research-plan test/fixtures/sample-plan.md 2>&1; echo "---"; node grd/bin/grd-tools.cjs bootstrap generate --data '{"established":[],"partial":[],"notResearched":[]}' 2>&1</automated>
  </verify>
  <done>`verify research-plan` reads a plan file, invokes validateResearchPlan, returns { valid, issues }. `bootstrap generate` accepts findings JSON, invokes generateBootstrap, returns markdown content. All existing tests pass: `node --test test/plan-checker-rules.test.cjs && node --test test/bootstrap.test.cjs`</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:
```bash
node --test test/state.test.cjs && node --test test/plan-checker-rules.test.cjs && node --test test/bootstrap.test.cjs
```

Verify new CLI routes are reachable:
```bash
node grd-tools.cjs state get-notes --cwd .
node grd-tools.cjs state get-gaps --cwd .
node grd-tools.cjs verify research-plan --help 2>&1 || true
node grd-tools.cjs bootstrap generate --data '{}' 2>&1 || true
```
</verification>

<success_criteria>
1. All 6 state note/gap subcommands (add-note, update-note-status, get-notes, add-gap, resolve-gap, get-gaps) work as CLI subcommands of grd-tools.cjs
2. `verify research-plan` invokes validateResearchPlan() and returns { valid, issues }
3. `bootstrap generate` invokes generateBootstrap() and returns BOOTSTRAP.md content
4. All existing tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-wiring-and-agent-integration/07-01-SUMMARY.md`
</output>

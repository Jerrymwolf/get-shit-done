---
phase: 17-namespace-migration
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - grd/workflows/new-research.md
  - grd/workflows/scope-inquiry.md
  - grd/workflows/plan-inquiry.md
  - grd/workflows/conduct-inquiry.md
  - grd/workflows/verify-inquiry.md
  - grd/workflows/complete-study.md
  - grd/workflows/insert-inquiry.md
  - grd/workflows/add-inquiry.md
  - grd/workflows/remove-inquiry.md
  - grd/workflows/audit-study.md
  - grd/bin/grd-tools.cjs
  - grd/bin/lib/init.cjs
  - grd/bin/lib/commands.cjs
  - grd/workflows/*.md
autonomous: true
requirements: [NS-02, NS-06]

must_haves:
  truths:
    - "Workflow filenames match research-native command vocabulary"
    - "CLI entry point routes new subcommand names (conduct-inquiry, scope-inquiry, etc.)"
    - "All Skill() calls in workflows reference new command names"
    - "Init subcommand routing accepts new names (init conduct-inquiry, init plan-inquiry, etc.)"
    - "All tests pass after workflow renames"
  artifacts:
    - path: "grd/workflows/conduct-inquiry.md"
      provides: "Renamed execute-phase workflow"
      min_lines: 10
    - path: "grd/workflows/scope-inquiry.md"
      provides: "Renamed discuss-phase workflow"
      min_lines: 10
    - path: "grd/workflows/new-research.md"
      provides: "Renamed new-project workflow"
      min_lines: 10
  key_links:
    - from: "grd/bin/grd-tools.cjs"
      to: "grd/workflows/*.md"
      via: "command routing switch/case"
      pattern: "conduct-inquiry|scope-inquiry|plan-inquiry"
    - from: "grd/workflows/*.md"
      to: "grd/workflows/*.md"
      via: "Skill() cross-references"
      pattern: "Skill.*grd:"
    - from: "grd/bin/lib/init.cjs"
      to: "grd/bin/grd-tools.cjs"
      via: "init subcommand routing"
      pattern: "conduct-inquiry|plan-inquiry|new-research|verify-inquiry"
---

<objective>
Rename 10 workflow files to match research-native command vocabulary and update all CLI routing, init subcommands, and Skill() cross-references to use new names.

Purpose: Complete the namespace migration by aligning internal filenames with the user-facing command vocabulary from the spec table. After this plan, typing `/grd:conduct-inquiry` maps to `grd/workflows/conduct-inquiry.md`, not `execute-phase.md`.
Output: 10 renamed workflow files, updated CLI routing, updated init subcommand names, all cross-references updated.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-namespace-migration/17-CONTEXT.md
@.planning/phases/17-namespace-migration/17-RESEARCH.md
@.planning/phases/17-namespace-migration/17-01-SUMMARY.md

<interfaces>
<!-- Workflow file rename table (from spec + CONTEXT.md decisions) -->

| Old Filename | New Filename | Old Command | New Command |
|-------------|-------------|-------------|-------------|
| new-project.md | new-research.md | grd:new-project | grd:new-research |
| discuss-phase.md | scope-inquiry.md | grd:discuss-phase | grd:scope-inquiry |
| plan-phase.md | plan-inquiry.md | grd:plan-phase | grd:plan-inquiry |
| execute-phase.md | conduct-inquiry.md | grd:execute-phase | grd:conduct-inquiry |
| verify-work.md | verify-inquiry.md | grd:verify-work | grd:verify-inquiry |
| complete-milestone.md | complete-study.md | grd:complete-milestone | grd:complete-study |
| insert-phase.md | insert-inquiry.md | grd:insert-phase | grd:insert-inquiry |
| add-phase.md | add-inquiry.md | grd:add-phase | grd:add-inquiry |
| remove-phase.md | remove-inquiry.md | grd:remove-phase | grd:remove-inquiry |
| audit-milestone.md | audit-study.md | grd:audit-milestone | grd:audit-study |

Commands with prefix-only change (NO file rename -- keep existing filename):
- grd:new-milestone (new-milestone.md stays)
- grd:settings (settings.md stays)
- grd:progress (progress.md stays)
- grd:quick (quick.md stays)
- grd:debug (diagnose-issues.md stays)
- grd:pause-work (pause-work.md stays)
- grd:resume-work (resume-work.md stays)
- grd:autonomous (autonomous.md stays)
- grd:health (health.md stays)
- grd:stats (stats.md stays)
- grd:help (help.md stays)
- grd:map-codebase (map-codebase.md stays)
- grd:validate-phase, grd:add-tests, grd:check-todos, grd:note, grd:add-todo, grd:cleanup, grd:do, grd:update, grd:reapply-patches (all keep existing filenames)

Init subcommand renames (in grd-tools.cjs switch/case):
- `init execute-phase` -> `init conduct-inquiry`
- `init plan-phase` -> `init plan-inquiry`
- `init new-project` -> `init new-research`
- `init verify-work` -> `init verify-inquiry`
- `init discuss-phase` -> `init scope-inquiry`

Skill() call patterns to update (inside workflow .md files):
- `Skill(skill="grd:plan-phase"` -> `Skill(skill="grd:plan-inquiry"`
- `Skill(skill="grd:execute-phase"` -> `Skill(skill="grd:conduct-inquiry"`
- `Skill(skill="grd:discuss-phase"` -> `Skill(skill="grd:scope-inquiry"`
- `Skill(skill="grd:verify-work"` -> `Skill(skill="grd:verify-inquiry"`
- `Skill(skill="grd:complete-milestone"` -> `Skill(skill="grd:complete-study"`
- `Skill(skill="grd:audit-milestone"` -> `Skill(skill="grd:audit-study"`
- `Skill(skill="grd:new-project"` -> `Skill(skill="grd:new-research"`
- `Skill(skill="grd:add-phase"` -> `Skill(skill="grd:add-inquiry"`
- `Skill(skill="grd:remove-phase"` -> `Skill(skill="grd:remove-inquiry"`
- `Skill(skill="grd:insert-phase"` -> `Skill(skill="grd:insert-inquiry"`

Also update plain-text references like `/grd:execute-phase` -> `/grd:conduct-inquiry` in prose within all workflow files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename 10 workflow files and update all internal cross-references</name>
  <files>
    grd/workflows/new-research.md (renamed from new-project.md)
    grd/workflows/scope-inquiry.md (renamed from discuss-phase.md)
    grd/workflows/plan-inquiry.md (renamed from plan-phase.md)
    grd/workflows/conduct-inquiry.md (renamed from execute-phase.md)
    grd/workflows/verify-inquiry.md (renamed from verify-work.md)
    grd/workflows/complete-study.md (renamed from complete-milestone.md)
    grd/workflows/insert-inquiry.md (renamed from insert-phase.md)
    grd/workflows/add-inquiry.md (renamed from add-phase.md)
    grd/workflows/remove-inquiry.md (renamed from remove-phase.md)
    grd/workflows/audit-study.md (renamed from audit-milestone.md)
    grd/workflows/*.md (all other workflow files -- update Skill() calls and /grd: references)
  </files>
  <read_first>
    - grd/workflows/execute-phase.md (primary workflow -- see Skill() calls, cross-references to other workflows)
    - grd/workflows/autonomous.md (chains multiple Skill() calls -- see cross-reference pattern)
    - grd/workflows/discuss-phase.md (references to plan-phase, execute-phase)
    - grd/workflows/plan-phase.md (references to execute-phase)
    - grd/workflows/verify-work.md (references in other workflows)
    - grd/workflows/complete-milestone.md (references to audit-milestone)
    - grd/workflows/help.md (lists all commands -- needs vocabulary update)
  </read_first>
  <action>
**Step 1: Rename 10 workflow files via git mv**

```bash
git mv grd/workflows/new-project.md grd/workflows/new-research.md
git mv grd/workflows/discuss-phase.md grd/workflows/scope-inquiry.md
git mv grd/workflows/plan-phase.md grd/workflows/plan-inquiry.md
git mv grd/workflows/execute-phase.md grd/workflows/conduct-inquiry.md
git mv grd/workflows/verify-work.md grd/workflows/verify-inquiry.md
git mv grd/workflows/complete-milestone.md grd/workflows/complete-study.md
git mv grd/workflows/insert-phase.md grd/workflows/insert-inquiry.md
git mv grd/workflows/add-phase.md grd/workflows/add-inquiry.md
git mv grd/workflows/remove-phase.md grd/workflows/remove-inquiry.md
git mv grd/workflows/audit-milestone.md grd/workflows/audit-study.md
```

**Step 2: Update command vocabulary in ALL workflow .md files**

Apply these text replacements across ALL 41 workflow files (both the renamed ones and the ones that kept their names). Order matters -- longer strings first:

1. `complete-milestone` -> `complete-study` (in Skill() args and /grd: refs)
2. `audit-milestone` -> `audit-study`
3. `execute-phase` -> `conduct-inquiry`
4. `discuss-phase` -> `scope-inquiry`
5. `plan-phase` -> `plan-inquiry`
6. `verify-work` -> `verify-inquiry`
7. `new-project` -> `new-research`
8. `insert-phase` -> `insert-inquiry`
9. `add-phase` -> `add-inquiry`
10. `remove-phase` -> `remove-inquiry`

IMPORTANT: Only replace these strings when they appear as command names (after `grd:`, in Skill() calls, in `/grd:` references, in prose describing commands). Do NOT replace them when they appear as:
- Internal function names in code (e.g., `cmdInitExecutePhase` in .cjs files -- these are JavaScript identifiers)
- Phase planning terminology in prose (e.g., "execute the phase plan" as a verb phrase)
- Historical references that are clearly documenting the old system

The workflow files contain these patterns to update:
- `Skill(skill="grd:execute-phase"` -> `Skill(skill="grd:conduct-inquiry"`
- `/grd:execute-phase` -> `/grd:conduct-inquiry`
- `grd:plan-phase` -> `grd:plan-inquiry` (in any context)
- And similarly for all 10 renamed commands

Also update `grd/references/*.md` files and `grd/templates/*.md` files that reference command names.

**Step 3: Update commands.cjs scaffold templates**

In `grd/bin/lib/commands.cjs`, the `cmdScaffold` function contains template strings with command references. Update any `grd:execute-phase`, `grd:discuss-phase`, etc. references in those templates to use new names.
  </action>
  <verify>
    <automated>ls grd/workflows/conduct-inquiry.md grd/workflows/scope-inquiry.md grd/workflows/plan-inquiry.md grd/workflows/new-research.md grd/workflows/verify-inquiry.md grd/workflows/complete-study.md grd/workflows/audit-study.md grd/workflows/insert-inquiry.md grd/workflows/add-inquiry.md grd/workflows/remove-inquiry.md && echo "All 10 renamed files exist"</automated>
  </verify>
  <acceptance_criteria>
    - `ls grd/workflows/conduct-inquiry.md` succeeds
    - `ls grd/workflows/execute-phase.md 2>/dev/null` fails (old file gone)
    - `ls grd/workflows/scope-inquiry.md` succeeds
    - `ls grd/workflows/discuss-phase.md 2>/dev/null` fails (old file gone)
    - `grep -r "grd:execute-phase" grd/workflows/ | wc -l` returns 0
    - `grep -r "grd:discuss-phase" grd/workflows/ | wc -l` returns 0
    - `grep -r "grd:plan-phase" grd/workflows/ | wc -l` returns 0
    - `grep -r "grd:verify-work" grd/workflows/ | wc -l` returns 0
    - `grep -r "grd:complete-milestone" grd/workflows/ | wc -l` returns 0
    - `grep -r "grd:new-project" grd/workflows/ | wc -l` returns 0
    - `grep -r "grd:conduct-inquiry" grd/workflows/` returns matches (new names present)
    - `grep -r "grd:scope-inquiry" grd/workflows/` returns matches
  </acceptance_criteria>
  <done>10 workflow files renamed to research-native names. All Skill() calls and /grd: references across all 41 workflow files use new command vocabulary. Zero residual old command names in grd/workflows/.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLI routing and init subcommands for new command names</name>
  <files>
    grd/bin/grd-tools.cjs
    grd/bin/lib/init.cjs
    grd/bin/lib/commands.cjs
  </files>
  <read_first>
    - grd/bin/grd-tools.cjs (CLI entry point -- see switch/case for command routing and init subcommand routing)
    - grd/bin/lib/init.cjs (see init subcommand function dispatch and resolveModelInternal calls)
    - grd/bin/lib/commands.cjs (see command registration and scaffold function)
  </read_first>
  <action>
**Step 1: Update CLI command routing in grd-tools.cjs**

The CLI entry point has a switch/case or if/else chain that maps command names to workflow files. Update the mappings:

- `execute-phase` case -> `conduct-inquiry` (maps to `grd/workflows/conduct-inquiry.md`)
- `discuss-phase` case -> `scope-inquiry` (maps to `grd/workflows/scope-inquiry.md`)
- `plan-phase` case -> `plan-inquiry` (maps to `grd/workflows/plan-inquiry.md`)
- `verify-work` case -> `verify-inquiry` (maps to `grd/workflows/verify-inquiry.md`)
- `new-project` case -> `new-research` (maps to `grd/workflows/new-research.md`)
- `complete-milestone` case -> `complete-study` (maps to `grd/workflows/complete-study.md`)
- `insert-phase` case -> `insert-inquiry` (maps to `grd/workflows/insert-inquiry.md`)
- `add-phase` case -> `add-inquiry` (maps to `grd/workflows/add-inquiry.md`)
- `remove-phase` case -> `remove-inquiry` (maps to `grd/workflows/remove-inquiry.md`)
- `audit-milestone` case -> `audit-study` (maps to `grd/workflows/audit-study.md`)

**Step 2: Update init subcommand routing in grd-tools.cjs**

The `init` subcommand has its own switch/case:
- `init execute-phase` -> `init conduct-inquiry`
- `init plan-phase` -> `init plan-inquiry`
- `init new-project` -> `init new-research`
- `init verify-work` -> `init verify-inquiry`
- `init discuss-phase` -> `init scope-inquiry`

The JavaScript function names called by these cases (e.g., `cmdInitExecutePhase`) can remain as-is -- they are internal identifiers, not user-facing. Only the string labels in the switch/case need to change.

**Step 3: Update commands.cjs command registration**

If `commands.cjs` has a command registry or help text listing command names, update those to use the new vocabulary.

**Step 4: Run full test suite**

```bash
node --test test/*.test.cjs
```

All tests must pass. The namespace.test.cjs should still pass (no new grd references introduced).
  </action>
  <verify>
    <automated>node --test test/namespace.test.cjs && node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "conduct-inquiry" grd/bin/grd-tools.cjs` matches (new command routed)
    - `grep "scope-inquiry" grd/bin/grd-tools.cjs` matches
    - `grep "plan-inquiry" grd/bin/grd-tools.cjs` matches
    - `grep "verify-inquiry" grd/bin/grd-tools.cjs` matches
    - `grep "new-research" grd/bin/grd-tools.cjs` matches
    - `grep "complete-study" grd/bin/grd-tools.cjs` matches
    - `grep "execute-phase" grd/bin/grd-tools.cjs` returns 0 matches (old name gone from routing)
    - `grep "discuss-phase" grd/bin/grd-tools.cjs` returns 0 matches
    - `node --test test/namespace.test.cjs` exits 0
    - `node --test test/*.test.cjs` exits 0
  </acceptance_criteria>
  <done>CLI routes all 10 renamed commands to their new workflow files. Init subcommands accept new names. All tests pass including namespace regression. Zero residual old command names in CLI routing code.</done>
</task>

</tasks>

<verification>
1. `ls grd/workflows/conduct-inquiry.md grd/workflows/scope-inquiry.md grd/workflows/plan-inquiry.md` -- renamed files exist
2. `ls grd/workflows/execute-phase.md 2>/dev/null` -- old files gone
3. `grep -rn "execute-phase\|discuss-phase\|plan-phase\|verify-work\|new-project\|complete-milestone\|audit-milestone\|insert-phase\|add-phase\|remove-phase" grd/bin/grd-tools.cjs | grep -v "cmdInit\|function\|//"` returns empty (no old command names in routing, excluding internal function names and comments)
4. `grep -rn "grd:execute-phase\|grd:discuss-phase\|grd:plan-phase\|grd:verify-work\|grd:new-project\|grd:complete-milestone\|grd:audit-milestone" grd/workflows/ | wc -l` returns 0
5. `node --test test/*.test.cjs` -- all tests pass
</verification>

<success_criteria>
- 10 workflow files renamed per spec table (conduct-inquiry, scope-inquiry, plan-inquiry, verify-inquiry, new-research, complete-study, insert-inquiry, add-inquiry, remove-inquiry, audit-study)
- Old workflow filenames do not exist
- CLI command routing uses new names
- Init subcommand routing uses new names
- All Skill() cross-references between workflows use new command names
- All /grd: references in prose use new command names
- Zero residual old command names in grd/ tree
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-namespace-migration/17-02-SUMMARY.md`
</output>

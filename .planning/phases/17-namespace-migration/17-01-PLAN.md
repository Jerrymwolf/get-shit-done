---
phase: 17-namespace-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/namespace.test.cjs
  - grd/bin/grd-tools.cjs
  - grd/bin/lib/model-profiles.cjs
  - grd/bin/lib/init.cjs
  - grd/bin/lib/commands.cjs
  - grd/bin/lib/config.cjs
  - grd/bin/lib/core.cjs
  - grd/bin/lib/state.cjs
  - grd/bin/lib/acquire.cjs
  - grd/bin/lib/bootstrap.cjs
  - grd/bin/lib/frontmatter.cjs
  - grd/bin/lib/milestone.cjs
  - grd/bin/lib/phase.cjs
  - grd/bin/lib/plan-checker-rules.cjs
  - grd/bin/lib/roadmap.cjs
  - grd/bin/lib/template.cjs
  - grd/bin/lib/vault.cjs
  - grd/bin/lib/verify.cjs
  - grd/bin/lib/verify-research.cjs
  - grd/references/model-profiles.md
  - grd/references/model-profile-resolution.md
  - grd/workflows/*.md
  - grd/templates/*.md
  - grd/agents/*.md
  - test/acquire.test.cjs
  - test/bootstrap.test.cjs
  - test/config-schema.test.cjs
  - test/core.test.cjs
  - test/e2e.test.cjs
  - test/model-profiles.test.cjs
  - test/plan-checker-rules.test.cjs
  - test/state.test.cjs
  - test/vault.test.cjs
  - test/verify-research.test.cjs
  - .planning/config.json
  - CLAUDE.md
autonomous: true
requirements: [NS-01, NS-03, NS-04, NS-05, NS-06, TEST-02]

must_haves:
  truths:
    - "The directory grd/ exists and grd/ does not"
    - "The CLI entry point is grd/bin/grd-tools.cjs (no grd-tools.cjs)"
    - "All 19 agent names use grd-* prefix in model-profiles.cjs"
    - "Zero residual grd, grd, or grd strings in grd/ or test/ trees"
    - "All existing tests pass with updated require() paths"
    - "A namespace regression test exists and passes"
  artifacts:
    - path: "test/namespace.test.cjs"
      provides: "Namespace residual scan regression test"
      min_lines: 40
    - path: "grd/bin/grd-tools.cjs"
      provides: "Renamed CLI entry point"
      contains: "grd-tools"
    - path: "grd/bin/lib/model-profiles.cjs"
      provides: "Agent name registry with grd-* prefix"
      contains: "grd-planner"
  key_links:
    - from: "test/*.test.cjs"
      to: "grd/bin/lib/*.cjs"
      via: "require() paths"
      pattern: "require.*grd/bin/lib"
    - from: "test/namespace.test.cjs"
      to: "grd/"
      via: "filesystem scan for residual references"
      pattern: "scanForPattern.*grd"
    - from: ".planning/config.json"
      to: "grd/bin/lib/vault.cjs"
      via: "vault_path config key"
      pattern: "grd/bin/lib/vault.cjs"
---

<objective>
Rename grd namespace to grd: directory rename, CLI tool rename, bulk text replacement across all files, agent name updates, and namespace regression test creation.

Purpose: Establish the grd identity across the entire codebase -- directory structure, CLI entry point, agent names, command prefixes, and brand text. This is the foundational rename that all subsequent plans build on.
Output: grd/ directory with all internal references updated, namespace regression test, all existing tests passing.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-namespace-migration/17-CONTEXT.md
@.planning/phases/17-namespace-migration/17-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs to understand -->

From grd/bin/lib/model-profiles.cjs (19 agent keys):
All keys currently use `grd-` prefix: grd-planner, grd-roadmapper, grd-executor, grd-phase-researcher, grd-project-researcher, grd-research-synthesizer, grd-debugger, grd-codebase-mapper, grd-verifier, grd-plan-checker, grd-integration-checker, grd-nyquist-auditor, grd-ui-researcher, grd-ui-checker, grd-ui-auditor, grd-source-researcher, grd-methods-researcher, grd-architecture-researcher, grd-limitations-researcher

From .planning/config.json:
```json
"vault_path": "grd/bin/lib/vault.cjs"
```
Must become: `"vault_path": "grd/bin/lib/vault.cjs"`

Replacement ordering (CRITICAL -- apply in this order to avoid partial matches):
1. `grd` -> `grd` (longest path first)
2. `grd-tools.cjs` -> `grd-tools.cjs` (specific filename)
3. `grd-` -> `grd-` (agent name prefix, before generic)
4. `grd:` -> `grd:` (command prefix)
5. `grd` -> `grd` (catch remaining, e.g. in comments)
6. `GRD` -> `GRD` (brand text in prose)
7. `grd` -> `grd` (underscore variant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create namespace regression test (Wave 0)</name>
  <files>test/namespace.test.cjs</files>
  <read_first>
    - test/core.test.cjs (see existing test patterns, require style, describe/it structure)
    - .planning/phases/17-namespace-migration/17-RESEARCH.md (namespace test code example at lines 277-329)
  </read_first>
  <action>
Create `test/namespace.test.cjs` that scans for residual `grd` references. The test must:

1. Define a `findFiles(dir, extensions)` helper that recursively finds files with given extensions (`.cjs`, `.md`, `.js`, `.json`), skipping `.git`, `node_modules`, and directories starting with `.` (except scan `.planning/`).

2. Define a `scanForPattern(dir, pattern, extensions)` helper that reads each file and returns relative paths of files containing the pattern.

3. Include these test cases using `node:test` describe/it and `node:assert`:

   a. `no "grd" in grd/ tree` -- scan `grd/` for `/grd/` pattern in `.cjs`, `.md`, `.js` files. Assert hits array is empty.

   b. `no "grd" in grd/ tree` -- scan `grd/` for `/grd/` pattern. Assert hits array is empty.

   c. `no "grd" in grd/ tree` -- scan `grd/` for `/grd/` pattern. Assert hits array is empty.

   d. `no "grd" in test/ tree` -- scan `test/` for `/grd/` pattern in `.cjs` files. Exclude `test/namespace.test.cjs` itself from results (it contains the patterns as test search strings).

   e. `no "grd" in .planning/ tree` -- scan `.planning/` for `/grd/` pattern in `.md`, `.json` files. Exclude files under `.planning/milestones/` (historical records).

   f. `grd/ directory exists` -- assert `fs.existsSync(path.join(ROOT, 'grd'))` is true.

   g. `grd/ directory does not exist` -- assert `fs.existsSync(path.join(ROOT, 'grd'))` is false.

   h. `grd-tools.cjs exists` -- assert `fs.existsSync(path.join(ROOT, 'grd', 'bin', 'grd-tools.cjs'))` is true.

   i. `grd-tools.cjs does not exist` -- assert `fs.existsSync(path.join(ROOT, 'grd', 'bin', 'grd-tools.cjs'))` is false.

ROOT is `path.join(__dirname, '..')`.

The test will FAIL initially (before rename) -- this is expected Wave 0 behavior.
  </action>
  <verify>
    <automated>node --test test/namespace.test.cjs 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File test/namespace.test.cjs exists with 9 test cases
    - `grep -c "it(" test/namespace.test.cjs` returns 9 or more
    - `grep "grd" test/namespace.test.cjs` shows pattern strings used in scan (the test itself references them as search targets)
    - `grep "grd" test/namespace.test.cjs` shows pattern strings used in scan
    - `grep "require.*node:test" test/namespace.test.cjs` matches
    - `grep "require.*node:assert" test/namespace.test.cjs` matches
  </acceptance_criteria>
  <done>Namespace regression test file exists with 9 test cases covering all residual patterns and directory existence checks. Test will fail until rename is complete (expected).</done>
</task>

<task type="auto">
  <name>Task 2: Directory rename + CLI tool rename + bulk text replacement</name>
  <files>
    grd/ (entire directory, renamed from grd/)
    grd/bin/grd-tools.cjs (renamed from grd-tools.cjs)
    test/*.test.cjs (all 10 test files - require path updates)
    .planning/config.json
    CLAUDE.md
    scripts/verify-rename.cjs
  </files>
  <read_first>
    - grd/bin/grd-tools.cjs (CLI entry point -- see shebang, Usage line, command routing)
    - grd/bin/lib/model-profiles.cjs (19 agent name keys to rename)
    - grd/bin/lib/init.cjs (resolveModelInternal calls with agent names)
    - grd/bin/lib/commands.cjs (scaffold template strings with command refs)
    - .planning/config.json (vault_path to update)
    - CLAUDE.md (contains grd/ path references that must be updated)
    - .planning/phases/17-namespace-migration/17-RESEARCH.md (full rename inventory, replacement ordering)
    - test/model-profiles.test.cjs (hardcoded grd-* assertions to update)
    - test/core.test.cjs (require paths + grd-* references)
  </read_first>
  <action>
Execute the full namespace migration as one atomic commit. Operations in order:

**Step 1: Directory and file renames via git mv**
```bash
git mv grd/ grd/
git mv grd/bin/grd-tools.cjs grd/bin/grd-tools.cjs
```

**Step 2: Bulk text replacement across ALL files in scope**

Write and run a Node.js script (or use sed) that applies replacements IN THIS ORDER to avoid partial matches:

1. `grd` -> `grd` (in all .cjs, .md, .js, .json files under grd/, test/, .planning/, scripts/)
2. `grd-tools.cjs` -> `grd-tools.cjs` (specific filename refs)
3. `grd-` -> `grd-` (agent name prefix -- covers grd-planner, grd-executor, etc.)
4. `grd:` -> `grd:` (command prefix -- covers /grd:execute-phase, Skill("grd:.."), etc.)
5. `grd` -> `grd` (remaining bare references in comments, strings)
6. `GRD` -> `GRD` (brand text in prose/comments)
7. `grd` -> `grd` (underscore variant in variable names)

**Scope of files to process:**
- All `.cjs` files under `grd/bin/` and `grd/bin/lib/`
- All `.md` files under `grd/workflows/`, `grd/references/`, `grd/templates/`
- All `.md` files under `grd/agents/` (agent prompt descriptions and context sections -- per user decision)
- All `.test.cjs` files under `test/`
- `.planning/config.json` (update vault_path)
- `CLAUDE.md` (update grd/ path references -- per user decision)
- `.planning/phases/17-namespace-migration/17-CONTEXT.md` and `17-RESEARCH.md` (update path references)
- `scripts/verify-rename.cjs` (update or mark as superseded by namespace.test.cjs)

**Step 3: Verify specific critical updates**

After bulk replacement, verify these specific values are correct:
- `.planning/config.json` contains `"vault_path": "grd/bin/lib/vault.cjs"`
- `CLAUDE.md` contains no `grd` references (all updated to `grd`)
- `grd/bin/grd-tools.cjs` header references `grd-tools` not `grd-tools`
- `grd/bin/lib/model-profiles.cjs` has all 19 agent keys with `grd-` prefix
- `grd/bin/lib/init.cjs` resolveModelInternal calls use `grd-` agent names
- All 10 test files use `require('../grd/bin/lib/...')` not `require('../grd/...')`
- `grd/agents/*.md` files contain `GRD` not `GRD` in descriptions

**Step 4: Run tests**
```bash
node --test test/*.test.cjs
```

All 192+ existing tests must pass. The new namespace.test.cjs must also pass (the directory existence and residual scan tests should now be green).

**Step 5: Commit everything atomically**

Stage all changes and commit with message: `feat(17-01): rename grd namespace to grd across entire codebase`

IMPORTANT: Do NOT update workflow filenames in this task. Workflow files keep their current names (execute-phase.md, discuss-phase.md, etc.) -- the CONTENT inside them gets grd->grd replacement, but the file renames happen in Plan 02.
  </action>
  <verify>
    <automated>node --test test/namespace.test.cjs && node --test test/model-profiles.test.cjs && node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `ls grd/bin/grd-tools.cjs` succeeds (file exists)
    - `ls grd/ 2>/dev/null` fails (directory gone)
    - `grep -r "grd" grd/ --include="*.cjs" --include="*.md" -l` returns empty (zero hits)
    - `grep -r "grd" grd/ --include="*.cjs" --include="*.md" -l` returns empty
    - `grep -r "grd" test/ --include="*.cjs" -l` returns only test/namespace.test.cjs (which contains patterns as search strings)
    - `grep -r "grd" CLAUDE.md` returns empty (zero hits)
    - `grep "vault_path" .planning/config.json` shows `grd/bin/lib/vault.cjs`
    - `grep "grd-planner" grd/bin/lib/model-profiles.cjs` matches
    - `grep "grd-executor" grd/bin/lib/model-profiles.cjs` matches
    - `node --test test/*.test.cjs` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>Directory renamed to grd/, CLI tool renamed to grd-tools.cjs, all ~700 grd references replaced with grd across all files including CLAUDE.md and grd/agents/*.md, all 19 agent names updated, config.json vault_path updated, all tests pass including namespace regression test.</done>
</task>

</tasks>

<verification>
1. `test -d grd && ! test -d grd` -- directory rename confirmed
2. `test -f grd/bin/grd-tools.cjs && ! test -f grd/bin/grd-tools.cjs` -- CLI tool rename confirmed
3. `node --test test/namespace.test.cjs` -- all 9 namespace regression tests pass
4. `node --test test/*.test.cjs` -- all 192+ existing tests pass with updated paths
5. `grep -rn "grd" grd/ --include="*.cjs" --include="*.md" | wc -l` returns 0
</verification>

<success_criteria>
- grd/ exists, grd/ does not
- grd/bin/grd-tools.cjs exists, grd-tools.cjs does not
- Zero residual grd/grd/grd/GRD in grd/ tree
- All 19 agent names in model-profiles.cjs use grd-* prefix
- .planning/config.json vault_path points to grd/bin/lib/vault.cjs
- CLAUDE.md contains no stale grd references
- grd/agents/*.md files contain GRD not GRD in descriptions
- All test require() paths use grd/ prefix
- All 192+ existing tests pass
- Namespace regression test (9 cases) passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-namespace-migration/17-01-SUMMARY.md`
</output>

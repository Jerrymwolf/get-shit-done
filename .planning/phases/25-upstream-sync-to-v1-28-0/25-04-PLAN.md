---
phase: 25-upstream-sync-to-v1-28-0
plan: 04
type: execute
wave: 3
depends_on: ["25-02", "25-03"]
files_modified:
  - grd/bin/lib/uat.cjs
  - grd/bin/lib/workstream.cjs
  - grd/bin/lib/profile-output.cjs
  - grd/bin/lib/profile-pipeline.cjs
  - grd/bin/grd-tools.cjs
autonomous: true
requirements:
  - SYNC-01

must_haves:
  truths:
    - "5 new upstream modules exist in grd/bin/lib/ and load without error"
    - "grd-tools.cjs wires all 5 new modules with require() statements"
    - "grd-tools.cjs routes new commands: audit-uat, workstream, state begin-phase/signal-waiting/signal-resume"
    - "grd-tools.cjs supports new flags: --pick, --no-verify, --ws, --id"
    - "All existing GRD-specific command wiring preserved in grd-tools.cjs"
    - "All 514+ tests pass after sync"
  artifacts:
    - path: "grd/bin/lib/uat.cjs"
      provides: "Cross-phase UAT scanner"
      exports: ["cmdAuditUat"]
    - path: "grd/bin/lib/workstream.cjs"
      provides: "Multi-workspace orchestration"
      exports: ["migrateToWorkstreams", "cmdWorkstreamCreate", "cmdWorkstreamList", "cmdWorkstreamStatus", "cmdWorkstreamComplete", "cmdWorkstreamSet", "cmdWorkstreamGet", "cmdWorkstreamProgress", "getOtherActiveWorkstreams"]
    - path: "grd/bin/lib/profile-output.cjs"
      provides: "User profiling output generation"
      exports: ["cmdWriteProfile", "cmdProfileQuestionnaire", "cmdGenerateDevPreferences", "cmdGenerateClaudeProfile", "cmdGenerateClaudeMd"]
    - path: "grd/bin/lib/profile-pipeline.cjs"
      provides: "Session scanning for profiling"
      exports: ["cmdScanSessions", "cmdExtractMessages", "cmdProfileSample"]
    - path: "grd/bin/grd-tools.cjs"
      provides: "CLI entry point with all new module wiring"
      contains: "require('./lib/workstream.cjs')"
  key_links:
    - from: "grd/bin/grd-tools.cjs"
      to: "grd/bin/lib/uat.cjs"
      via: "require and audit-uat command routing"
      pattern: "require.*uat"
    - from: "grd/bin/grd-tools.cjs"
      to: "grd/bin/lib/workstream.cjs"
      via: "require and workstream command routing"
      pattern: "require.*workstream"
    - from: "grd/bin/grd-tools.cjs"
      to: "grd/bin/lib/security.cjs"
      via: "safeJsonParse import"
      pattern: "require.*security"
---

<objective>
Adopt the 5 new upstream modules (uat.cjs, workstream.cjs, profile-output.cjs, profile-pipeline.cjs, security.cjs already done) and sync grd-tools.cjs CLI entry point to wire everything together. The CLI needs new commands, new flags, and new module requires while preserving all GRD-specific wiring.

Purpose: Complete the CJS module layer by adding new upstream modules and wiring them into the CLI.
Output: 5 new modules + updated CLI entry point, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-CONTEXT.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-RESEARCH.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt 4 new upstream modules</name>
  <files>grd/bin/lib/uat.cjs, grd/bin/lib/workstream.cjs, grd/bin/lib/profile-output.cjs, grd/bin/lib/profile-pipeline.cjs</files>
  <read_first>
    - ~/.claude/get-shit-done/bin/lib/uat.cjs (upstream source)
    - ~/.claude/get-shit-done/bin/lib/workstream.cjs (upstream source)
    - ~/.claude/get-shit-done/bin/lib/profile-output.cjs (upstream source)
    - ~/.claude/get-shit-done/bin/lib/profile-pipeline.cjs (upstream source)
  </read_first>
  <action>
Per D-01, adopt all 4 remaining new upstream modules (security.cjs already adopted in Plan 01). Per D-02, apply GRD namespace pattern.

For each module, copy from upstream `~/.claude/get-shit-done/bin/lib/` to `grd/bin/lib/`. These are all new files -- no merge needed.

**uat.cjs (189 lines):**
- Copy as-is. Uses `planningDir` from core.cjs, `getMilestonePhaseFilter` from core.cjs, `extractFrontmatter` from frontmatter.cjs.
- Exports: `cmdAuditUat`
- No namespace changes needed.

**workstream.cjs (491 lines):**
- Copy as-is. Uses core.cjs functions.
- Exports: `migrateToWorkstreams`, `cmdWorkstreamCreate`, `cmdWorkstreamList`, `cmdWorkstreamStatus`, `cmdWorkstreamComplete`, `cmdWorkstreamSet`, `cmdWorkstreamGet`, `cmdWorkstreamProgress`, `getOtherActiveWorkstreams`
- If module references `gsd-tools.cjs` anywhere internally, update to `grd-tools.cjs`.
- If module references `get-shit-done` paths, update to GRD paths.

**profile-output.cjs (952 lines):**
- Copy as-is. Uses core.cjs functions.
- Exports: `cmdWriteProfile`, `cmdProfileQuestionnaire`, `cmdGenerateDevPreferences`, `cmdGenerateClaudeProfile`, `cmdGenerateClaudeMd`, `PROFILING_QUESTIONS`, `CLAUDE_INSTRUCTIONS`
- If module references `gsd-tools.cjs` paths, update to `grd-tools.cjs`.

**profile-pipeline.cjs (539 lines):**
- Copy as-is. Uses core.cjs functions.
- Exports: `cmdScanSessions`, `cmdExtractMessages`, `cmdProfileSample`
- If module references upstream paths, update to GRD paths.

After copying, verify each module loads without requiring modules that don't exist yet.
  </action>
  <verify>
    <automated>node -e "const u = require('./grd/bin/lib/uat.cjs'); if (!u.cmdAuditUat) throw new Error('Missing cmdAuditUat'); console.log('uat.cjs OK')" && node -e "const w = require('./grd/bin/lib/workstream.cjs'); if (!w.cmdWorkstreamCreate) throw new Error('Missing cmdWorkstreamCreate'); console.log('workstream.cjs OK')" && node -e "const po = require('./grd/bin/lib/profile-output.cjs'); if (!po.cmdWriteProfile) throw new Error('Missing cmdWriteProfile'); console.log('profile-output.cjs OK')" && node -e "const pp = require('./grd/bin/lib/profile-pipeline.cjs'); if (!pp.cmdScanSessions) throw new Error('Missing cmdScanSessions'); console.log('profile-pipeline.cjs OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grd/bin/lib/uat.cjs exists and exports cmdAuditUat
    - grd/bin/lib/workstream.cjs exists and exports cmdWorkstreamCreate
    - grd/bin/lib/profile-output.cjs exists and exports cmdWriteProfile
    - grd/bin/lib/profile-pipeline.cjs exists and exports cmdScanSessions
    - None of the 4 files contain `get-shit-done` path references (GRD namespace applied)
    - All 4 modules load without syntax errors
  </acceptance_criteria>
  <done>4 new upstream modules adopted into grd/bin/lib/ with GRD namespace, all load cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Sync grd-tools.cjs CLI entry point</name>
  <files>grd/bin/grd-tools.cjs</files>
  <read_first>
    - grd/bin/grd-tools.cjs (current GRD version -- MUST read fully)
    - ~/.claude/get-shit-done/bin/gsd-tools.cjs (upstream v1.28.0 -- MUST read fully)
  </read_first>
  <action>
Per D-04, diff upstream gsd-tools.cjs against GRD's grd-tools.cjs and apply the delta. This is the CLI entry point (611 diff lines). PRESERVE all GRD-specific command wiring.

**New require() statements to add at top:**
```javascript
const profilePipeline = require('./lib/profile-pipeline.cjs');
const profileOutput = require('./lib/profile-output.cjs');
const workstream = require('./lib/workstream.cjs');
const uat = require('./lib/uat.cjs');
const security = require('./lib/security.cjs');
```

**New commands to wire in the command routing switch/if-else:**
- `state begin-phase` -- route to state module's begin-phase function
- `state signal-waiting` -- write WAITING.json signal file
- `state signal-resume` -- remove WAITING.json signal
- `commit-to-subrepo` -- route commits to sub-repos
- `audit-uat` -- route to `uat.cmdAuditUat()`
- `workstream create` -- route to `workstream.cmdWorkstreamCreate()`
- `workstream list` -- route to `workstream.cmdWorkstreamList()`
- `workstream status` -- route to `workstream.cmdWorkstreamStatus()`
- `workstream complete` -- route to `workstream.cmdWorkstreamComplete()`
- `workstream set` -- route to `workstream.cmdWorkstreamSet()`
- `workstream get` -- route to `workstream.cmdWorkstreamGet()`
- `workstream progress` -- route to `workstream.cmdWorkstreamProgress()`
- Profile commands -- route to profile-output and profile-pipeline functions

**New flags to support:**
- `--pick <field>` -- extract single field from JSON output (replaces jq dependency)
- `--no-verify` -- pass to cmdCommit as noVerify parameter
- `--ws <name>` -- workstream selection, pass to functions that accept ws parameter
- `--id` -- on `phase add` command

**Other upstream changes:**
- `safeJsonParse()` from security.cjs for parsing JSON input safely
- Any new error handling patterns
- Replace bare `catch {}` with `catch { /* intentionally empty */ }`

**CRITICAL PRESERVATIONS:**
- ALL GRD-specific command wiring (research commands, tier-strip, vault, acquire, verify-research, etc.)
- GRD tool path references (do NOT change to get-shit-done paths)
- GRD namespace in any string literals

Tech debt D-11: Verify config-set-model-profile stub is properly handled (research says it was fixed in Phase 15 -- just confirm).

Run tests after completion.
  </action>
  <verify>
    <automated>node -e "require('./grd/bin/grd-tools.cjs')" 2>&1 | head -5 && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grd-tools.cjs contains `require('./lib/workstream.cjs')`
    - grd-tools.cjs contains `require('./lib/uat.cjs')`
    - grd-tools.cjs contains `require('./lib/profile-output.cjs')`
    - grd-tools.cjs contains `require('./lib/profile-pipeline.cjs')`
    - grd-tools.cjs contains `require('./lib/security.cjs')`
    - grd-tools.cjs contains `audit-uat` command routing
    - grd-tools.cjs contains `workstream` command routing
    - grd-tools.cjs contains `--pick` flag handling
    - grd-tools.cjs does NOT contain `get-shit-done` path references
    - grd-tools.cjs still contains GRD research command wiring (vault, acquire, tier-strip, etc.)
    - `node scripts/run-tests.cjs` shows 513+ passing
  </acceptance_criteria>
  <done>grd-tools.cjs synced with all new module wiring, new commands, new flags, GRD-specific wiring preserved, all tests pass</done>
</task>

</tasks>

<verification>
- All 4 new modules + grd-tools.cjs load without error
- `node scripts/run-tests.cjs` shows 513+ passing tests
- New commands accessible via grd-tools.cjs
- GRD-specific wiring preserved
</verification>

<success_criteria>
- 4 new modules adopted and loading cleanly
- grd-tools.cjs wires all new modules and commands
- New flags (--pick, --no-verify, --ws, --id) supported
- All GRD-specific command wiring preserved
- Test suite passes (513+ pass)
</success_criteria>

<output>
After completion, create `.planning/phases/25-upstream-sync-to-v1-28-0/25-04-SUMMARY.md`
</output>

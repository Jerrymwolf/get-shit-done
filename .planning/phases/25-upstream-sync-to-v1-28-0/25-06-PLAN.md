---
phase: 25-upstream-sync-to-v1-28-0
plan: 06
type: execute
wave: 5
depends_on: ["25-05"]
files_modified:
  - grd/templates/*.md
  - grd/templates/config.json
  - commands/gsd-r/workstreams.md
  - grd/VERSION
autonomous: true
requirements:
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-06

must_haves:
  truths:
    - "28 shared templates have upstream changes applied while preserving GRD research templates"
    - "4 new upstream templates adopted (claude-md.md, dev-preferences.md, discussion-log.md, user-profile.md)"
    - "GRD-only research templates untouched (research-note.md, source-log.md, etc.)"
    - "Agent files verified unchanged (4 GRD-only agents intact)"
    - "New workstreams.md command file adopted"
    - "VERSION file reads 1.28.0"
    - "All 514+ tests pass (full regression suite green)"
  artifacts:
    - path: "grd/VERSION"
      provides: "Version marker"
      contains: "1.28.0"
    - path: "grd/templates/claude-md.md"
      provides: "New Claude.md generation template"
    - path: "grd/templates/dev-preferences.md"
      provides: "New developer preferences template"
    - path: "grd/templates/discussion-log.md"
      provides: "New discussion logging template"
    - path: "grd/templates/user-profile.md"
      provides: "New user profiling template"
    - path: "commands/gsd-r/workstreams.md"
      provides: "New workstreams command"
  key_links:
    - from: "grd/templates/config.json"
      to: "grd/bin/lib/config.cjs"
      via: "Config template has new keys matching config module"
      pattern: "firecrawl|exa_search|workflow"
    - from: "grd/VERSION"
      to: "project identity"
      via: "Version stamp"
      pattern: "1.28.0"
---

<objective>
Complete the upstream sync by syncing all templates, adopting the new workstreams command, verifying agents are intact, updating the VERSION file to 1.28.0, and running the full test suite to confirm zero regressions.

Purpose: Finalize the v1.28.0 sync across all file types and stamp the version.
Output: All templates synced, new command adopted, VERSION = 1.28.0, full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-CONTEXT.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync templates and adopt new command file</name>
  <files>grd/templates/research.md, grd/templates/project.md, grd/templates/requirements.md, grd/templates/roadmap.md, grd/templates/context.md, grd/templates/UAT.md, grd/templates/phase-prompt.md, grd/templates/state.md, grd/templates/planner-subagent-prompt.md, grd/templates/config.json, grd/templates/debug-subagent-prompt.md, grd/templates/discovery.md, grd/templates/claude-md.md, grd/templates/dev-preferences.md, grd/templates/discussion-log.md, grd/templates/user-profile.md, commands/gsd-r/workstreams.md</files>
  <read_first>
    - grd/templates/research.md (highest diff -- 587 lines, RESEARCH CONFLICT)
    - ~/.claude/get-shit-done/templates/research.md (upstream)
    - grd/templates/project.md (263 diff lines)
    - ~/.claude/get-shit-done/templates/project.md (upstream)
    - grd/templates/config.json (current)
    - ~/.claude/get-shit-done/templates/config.json (upstream)
    - ~/.claude/get-shit-done/commands/gsd/workstreams.md (new command to adopt)
  </read_first>
  <action>
**A) Sync 12 shared templates with changes (ranked by diff size):**

Per research Pitfall 4, the research.md template needs the most careful attention.

1. **research.md (587 diff lines -- HIGHEST CONFLICT):** This is the most complex template merge. Upstream overhauled the research template structure. GRD has its own research-specific template content. Manual merge required:
   - Take upstream structural improvements (new sections, better organization)
   - PRESERVE GRD research-specific content (tier references, epistemological stance, source acquisition patterns, research-note workflow references)
   - If upstream added entirely new sections, include them
   - If upstream restructured existing sections that GRD modified, prefer GRD's research-focused version unless upstream's is strictly better

2. **project.md (263 lines):** Add new sections, workstream support. Preserve any GRD research project references.

3. **requirements.md (249 lines):** Add new format/sections from upstream.

4. **roadmap.md (156 lines):** Add workstream columns, new format from upstream.

5. **context.md (46 lines), UAT.md (30 lines), phase-prompt.md (29 lines), state.md (14 lines), planner-subagent-prompt.md (14 lines), config.json (13 lines), debug-subagent-prompt.md (8 lines), discovery.md (4 lines):** Apply upstream changes. These are small, straightforward diffs.

6. **config.json template (13 lines):** Add new config keys to match what config.cjs now supports (firecrawl, exa_search, workflow.*, hooks.*). PRESERVE GRD research config keys (vault_path, commit_research, researcher_tier, etc.).

**Identical templates (12 files -- skip):** continue-here.md, copilot-instructions.md, DEBUG.md, milestone-archive.md, milestone.md, retrospective.md, summary-complex.md, summary-minimal.md, summary-standard.md, summary.md, UI-SPEC.md, VALIDATION.md

**B) Adopt 4 new upstream templates:**
Copy from `~/.claude/get-shit-done/templates/` to `grd/templates/`:
- claude-md.md (Claude.md generation template)
- dev-preferences.md (developer preferences template)
- discussion-log.md (discussion logging template)
- user-profile.md (user profiling template)

Apply GRD namespace: replace any `get-shit-done` or `/gsd:` references with GRD equivalents.

**C) GRD-only templates (10 files -- untouched):**
bootstrap.md, decision-log.md, executive-summary.md, framework.md, gaps.md, research-note.md, research-task.md, source-log.md, terminal-deliverable.md, themes.md -- no sync needed.

**D) Adopt new command file:**
Copy upstream `~/.claude/get-shit-done/commands/gsd/workstreams.md` to `commands/gsd-r/workstreams.md`. Apply GRD namespace (replace /gsd: with /grd:, update tool paths).

**E) Verify agents (SYNC-03):**
Per research, upstream has 0 agent files. GRD has 4 research-only agents. No sync needed. Just verify all 4 exist:
- grd/agents/grd-argument-constructor.md
- grd/agents/grd-framework-integrator.md
- grd/agents/grd-gap-analyzer.md
- grd/agents/grd-thematic-synthesizer.md
  </action>
  <verify>
    <automated>ls grd/templates/claude-md.md grd/templates/dev-preferences.md grd/templates/discussion-log.md grd/templates/user-profile.md 2>&1 && echo "---" && ls commands/gsd-r/workstreams.md 2>&1 && echo "---" && ls grd/agents/grd-argument-constructor.md grd/agents/grd-framework-integrator.md grd/agents/grd-gap-analyzer.md grd/agents/grd-thematic-synthesizer.md 2>&1 && echo "---" && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grd/templates/claude-md.md exists (new template adopted)
    - grd/templates/dev-preferences.md exists (new template adopted)
    - grd/templates/discussion-log.md exists (new template adopted)
    - grd/templates/user-profile.md exists (new template adopted)
    - grd/templates/research.md contains GRD research-specific content (tier, epistemological, source)
    - grd/templates/config.json contains `firecrawl` or `exa_search` (new upstream keys)
    - grd/templates/config.json contains `vault_path` or `researcher_tier` (research keys preserved)
    - commands/gsd-r/workstreams.md exists (new command adopted)
    - commands/gsd-r/workstreams.md contains `/grd:` not `/gsd:` (namespace applied)
    - All 4 agent files exist in grd/agents/
    - `node scripts/run-tests.cjs` shows 513+ passing
  </acceptance_criteria>
  <done>All templates synced, 4 new templates adopted, new command file adopted, agents verified, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update VERSION file and run final validation</name>
  <files>grd/VERSION</files>
  <read_first>
    - grd/VERSION (current -- should read 1.25.1)
  </read_first>
  <action>
1. Update `grd/VERSION` to read exactly `1.28.0` (single line, no trailing whitespace or newline beyond the standard one)

2. Run the complete test suite as final validation:
   ```
   node scripts/run-tests.cjs
   ```

3. Verify the complete sync by checking key indicators:
   - `cat grd/VERSION` outputs `1.28.0`
   - `node -e "require('./grd/bin/lib/security.cjs')"` -- new module loads
   - `node -e "require('./grd/bin/lib/uat.cjs')"` -- new module loads
   - `node -e "require('./grd/bin/lib/workstream.cjs')"` -- new module loads
   - `node -e "require('./grd/bin/lib/profile-output.cjs')"` -- new module loads
   - `node -e "require('./grd/bin/lib/profile-pipeline.cjs')"` -- new module loads
   - `grep -rl "get-shit-done" grd/bin/lib/ grd/workflows/ grd/templates/ commands/gsd-r/ 2>/dev/null | wc -l` outputs 0

4. If any tests fail that were passing before (baseline: 513 pass, 1 known fail in namespace.test.cjs subtest 5), investigate and fix. Per D-09, do NOT write new tests -- only fix regressions.
  </action>
  <verify>
    <automated>cat grd/VERSION && echo "---" && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `cat grd/VERSION` outputs exactly `1.28.0`
    - `node scripts/run-tests.cjs` shows 513+ passing tests (same or better than pre-sync baseline)
    - No new test failures compared to pre-sync baseline (513 pass, 1 known fail)
    - `grep -rl "get-shit-done" grd/bin/lib/ grd/workflows/ grd/templates/ commands/gsd-r/ 2>/dev/null | wc -l` outputs 0
  </acceptance_criteria>
  <done>VERSION reads 1.28.0, full test suite green (513+ pass), zero upstream path leaks, sync complete</done>
</task>

</tasks>

<verification>
- `cat grd/VERSION` outputs `1.28.0`
- All 5 new CJS modules load cleanly
- No `get-shit-done` path leaks anywhere in GRD active files
- `node scripts/run-tests.cjs` shows 513+ passing (SYNC-06 satisfied)
- 4 agent files verified present (SYNC-03 satisfied)
- Templates synced with research content preserved (SYNC-04 satisfied)
</verification>

<success_criteria>
- VERSION = 1.28.0 (SYNC-05)
- All templates synced, 4 new templates adopted, research templates preserved (SYNC-04)
- Agent files verified intact (SYNC-03)
- New workstreams command adopted
- Full test suite green with 513+ passing (SYNC-06)
- Zero `get-shit-done` path leaks
- Phase 25 upstream sync complete
</success_criteria>

<output>
After completion, create `.planning/phases/25-upstream-sync-to-v1-28-0/25-06-SUMMARY.md`
</output>

---
phase: 25-upstream-sync-to-v1-28-0
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - grd/bin/lib/frontmatter.cjs
  - grd/bin/lib/template.cjs
  - grd/bin/lib/milestone.cjs
  - grd/bin/lib/roadmap.cjs
  - grd/bin/lib/verify.cjs
  - grd/bin/lib/model-profiles.cjs
autonomous: true
requirements:
  - SYNC-01

must_haves:
  truths:
    - "frontmatter.cjs handles CRLF line endings and null byte guards"
    - "template.cjs uses normalizeMd on file writes"
    - "milestone.cjs uses planningPaths and normalizeMd"
    - "roadmap.cjs uses planningPaths and extractCurrentMilestone"
    - "verify.cjs uses planningDir and custom phase naming skip"
    - "model-profiles.cjs keeps grd-* agent names and all 8 research agents"
    - "All 514+ tests pass after sync"
  artifacts:
    - path: "grd/bin/lib/frontmatter.cjs"
      provides: "CRLF-tolerant frontmatter parsing"
      contains: "normalizeMd"
    - path: "grd/bin/lib/model-profiles.cjs"
      provides: "Model tier assignments with grd-* namespace"
      contains: "grd-executor"
  key_links:
    - from: "grd/bin/lib/milestone.cjs"
      to: "grd/bin/lib/core.cjs"
      via: "planningPaths import"
      pattern: "planningPaths"
    - from: "grd/bin/lib/roadmap.cjs"
      to: "grd/bin/lib/core.cjs"
      via: "extractCurrentMilestone import"
      pattern: "extractCurrentMilestone"
---

<objective>
Sync the 6 LOW-conflict shared CJS modules: frontmatter.cjs, template.cjs, milestone.cjs, roadmap.cjs, verify.cjs, and model-profiles.cjs. These have smaller diffs (4-89 lines each) and minimal research conflicts. model-profiles.cjs is a namespace-preservation exercise only.

Purpose: Complete the shared CJS module sync with the simpler, lower-risk modules.
Output: 6 updated CJS modules, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-CONTEXT.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-RESEARCH.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync frontmatter.cjs, template.cjs, milestone.cjs, and roadmap.cjs</name>
  <files>grd/bin/lib/frontmatter.cjs, grd/bin/lib/template.cjs, grd/bin/lib/milestone.cjs, grd/bin/lib/roadmap.cjs</files>
  <read_first>
    - grd/bin/lib/frontmatter.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/frontmatter.cjs (upstream v1.28.0)
    - grd/bin/lib/template.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/template.cjs (upstream v1.28.0)
    - grd/bin/lib/milestone.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/milestone.cjs (upstream v1.28.0)
    - grd/bin/lib/roadmap.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/roadmap.cjs (upstream v1.28.0)
  </read_first>
  <action>
Diff each upstream file against its GRD counterpart and apply upstream changes. These are all LOW-complexity changes per the research diff manifest.

**frontmatter.cjs (24 diff lines):**
1. Add CRLF-tolerant regex patterns -- update frontmatter extraction regex to handle `\r\n`
2. Add support for multiple frontmatter blocks
3. Add null byte guards: reject content with `\0` characters
4. Add `normalizeMd()` import from core.cjs and use it on output

**template.cjs (4 diff lines -- TRIVIAL):**
1. Add `normalizeMd` import from core.cjs
2. Wrap template write output with `normalizeMd()` call

**milestone.cjs (77 diff lines):**
1. Replace hardcoded `.planning/` paths with `planningPaths(cwd)` calls
2. Add `already_complete` tracking in milestone completion logic
3. Add `normalizeMd()` on file writes
4. Add `stateReplaceFieldWithFallback` import from state.cjs
5. Update require() destructuring for new core.cjs imports

**roadmap.cjs (57 diff lines):**
1. Replace hardcoded paths with `planningPaths(cwd)` calls
2. Replace `stripShippedMilestones` usage with `extractCurrentMilestone` from core.cjs
3. Add flexible table column handling (handle varying markdown table widths)
4. Update require() destructuring for new imports

None of these 4 modules have research-specific modifications. These are straightforward upstream adoptions.
  </action>
  <verify>
    <automated>node -e "['frontmatter','template','milestone','roadmap'].forEach(m => { require('./grd/bin/lib/' + m + '.cjs'); console.log(m + '.cjs loads OK'); })" && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - frontmatter.cjs contains `\r` or `\\r` (CRLF handling)
    - frontmatter.cjs contains `normalizeMd` (import present)
    - template.cjs contains `normalizeMd` (import present)
    - milestone.cjs contains `planningPaths` (path migration)
    - milestone.cjs contains `normalizeMd` (output normalization)
    - roadmap.cjs contains `planningPaths` or `planningDir` (path migration)
    - roadmap.cjs contains `extractCurrentMilestone` (new function usage)
    - `node scripts/run-tests.cjs` shows 513+ passing
  </acceptance_criteria>
  <done>frontmatter, template, milestone, and roadmap modules synced with upstream improvements, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Sync verify.cjs and model-profiles.cjs (namespace-sensitive)</name>
  <files>grd/bin/lib/verify.cjs, grd/bin/lib/model-profiles.cjs</files>
  <read_first>
    - grd/bin/lib/verify.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/verify.cjs (upstream v1.28.0)
    - grd/bin/lib/model-profiles.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/model-profiles.cjs (upstream v1.28.0)
  </read_first>
  <action>
**verify.cjs (89 diff lines):**
1. Replace hardcoded `.planning/` paths with `planningDir(cwd)` calls
2. Add custom phase naming skip logic (skip verification for custom-named phases if needed)
3. Replace bare `catch {}` with `catch { /* intentionally empty */ }` (annotated catches)
4. Update require() destructuring for new core.cjs imports

verify.cjs has GRD research-specific verification code (verify-research.cjs, verify-sufficiency.cjs, three-tier verification). This code is in SEPARATE files, not in verify.cjs itself. Apply upstream changes to verify.cjs without touching the research verification modules.

**model-profiles.cjs (51 diff lines -- NAMESPACE PRESERVATION):**
This module is a namespace preservation exercise per research Pitfall 2. DO NOT apply upstream agent name changes.

1. Keep ALL `grd-*` agent name prefixes (do NOT change to `gsd-*`)
2. Keep ALL 8 GRD research-specific agents: grd-argument-constructor, grd-framework-integrator, grd-gap-analyzer, grd-thematic-synthesizer, grd-source-researcher, grd-researcher, grd-reviewer, grd-synthesizer
3. Check if upstream changed any model tier assignments (opus/sonnet/haiku). Research says they did NOT change. If any tier assignments DID change, update those tiers while keeping grd-* names.
4. If upstream added any new agent entries, add them with `grd-*` prefix instead of `gsd-*`
5. Do NOT remove any existing GRD agent entries

Run tests after completion.
  </action>
  <verify>
    <automated>node -e "const v = require('./grd/bin/lib/verify.cjs'); console.log('verify.cjs loads OK')" && node -e "const mp = require('./grd/bin/lib/model-profiles.cjs'); const s = JSON.stringify(mp); if (s.includes('gsd-executor')) throw new Error('gsd-* namespace leaked into model-profiles'); if (!s.includes('grd-executor')) throw new Error('Missing grd-executor'); console.log('model-profiles.cjs namespace OK')" && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - verify.cjs contains `planningDir` (path migration)
    - verify.cjs does NOT contain bare `catch {}` (all annotated)
    - model-profiles.cjs contains `grd-executor` (namespace preserved)
    - model-profiles.cjs does NOT contain `gsd-executor` (no namespace regression)
    - model-profiles.cjs contains `grd-argument-constructor` (research agents preserved)
    - model-profiles.cjs contains `grd-framework-integrator` (research agents preserved)
    - `node scripts/run-tests.cjs` shows 513+ passing
  </acceptance_criteria>
  <done>verify.cjs and model-profiles.cjs synced, grd-* namespace preserved, research agents intact, all tests pass</done>
</task>

</tasks>

<verification>
- All 6 modules load: `node -e "['frontmatter','template','milestone','roadmap','verify','model-profiles'].forEach(m => require('./grd/bin/lib/' + m + '.cjs'))"`
- `node scripts/run-tests.cjs` shows 513+ passing tests
- No gsd-* namespace leaks in model-profiles.cjs
</verification>

<success_criteria>
- 6 LOW-conflict modules synced with upstream v1.28.0
- model-profiles.cjs namespace preserved (grd-*, all research agents)
- All annotated catch blocks
- Test suite passes (513+ pass)
</success_criteria>

<output>
After completion, create `.planning/phases/25-upstream-sync-to-v1-28-0/25-03-SUMMARY.md`
</output>

---
phase: 25-upstream-sync-to-v1-28-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grd/bin/lib/security.cjs
  - grd/bin/lib/core.cjs
autonomous: true
requirements:
  - SYNC-01

must_haves:
  truths:
    - "security.cjs exists in grd/bin/lib/ and exports all 9 upstream functions"
    - "core.cjs exports all 18+ new upstream functions (planningDir, planningPaths, normalizeMd, etc.)"
    - "Existing core.cjs functions remain unchanged"
    - "All 514+ tests pass after foundation sync"
  artifacts:
    - path: "grd/bin/lib/security.cjs"
      provides: "Centralized security checks"
      exports: ["validatePath", "requireSafePath", "INJECTION_PATTERNS", "scanForInjection", "sanitizeForPrompt", "validateShellArg", "safeJsonParse", "validatePhaseNumber", "validateFieldName"]
    - path: "grd/bin/lib/core.cjs"
      provides: "Foundation utilities with new planning path resolution, workstream support, file locking"
      exports: ["normalizeMd", "extractCurrentMilestone", "extractOneLinerFromBody", "resolveWorktreeRoot", "withPlanningLock", "findProjectRoot", "detectSubRepos", "reapStaleTempFiles", "MODEL_ALIAS_MAP", "planningDir", "planningRoot", "planningPaths", "getActiveWorkstream", "setActiveWorkstream", "filterPlanFiles", "filterSummaryFiles", "getPhaseFileStats", "readSubdirectories"]
  key_links:
    - from: "grd/bin/lib/security.cjs"
      to: "grd/bin/lib/commands.cjs"
      via: "sanitizeForPrompt import (wired in Plan 02)"
      pattern: "require.*security"
    - from: "grd/bin/lib/core.cjs"
      to: "all other CJS modules"
      via: "planningDir/planningPaths imports (wired in Plans 02-03)"
      pattern: "require.*core"
---

<objective>
Sync the two foundation modules -- core.cjs and security.cjs -- from GSD v1.28.0. These must land first because every other module depends on core.cjs new exports (planningDir, planningPaths, normalizeMd, extractCurrentMilestone) and several modules depend on security.cjs (sanitizeForPrompt, validatePath, validateFieldName).

Purpose: Establish the foundation that all subsequent module syncs depend on.
Output: Two updated/new CJS modules with all upstream v1.28.0 functions, all existing tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-CONTEXT.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt security.cjs from upstream</name>
  <files>grd/bin/lib/security.cjs</files>
  <read_first>
    - ~/.claude/get-shit-done/bin/lib/security.cjs (upstream source of truth)
    - grd/bin/lib/core.cjs (to understand existing export patterns)
  </read_first>
  <action>
Copy upstream `~/.claude/get-shit-done/bin/lib/security.cjs` to `grd/bin/lib/security.cjs`. This is a brand new file (GRD does not have it yet), so it is a straight copy with minimal adaptation per D-01 and D-02.

After copying, verify the module exports exactly these 9 functions/constants:
- validatePath
- requireSafePath
- INJECTION_PATTERNS
- scanForInjection
- sanitizeForPrompt
- validateShellArg
- safeJsonParse
- validatePhaseNumber
- validateFieldName

The module is standalone (no GRD-specific dependencies). No namespace changes needed -- it is pure infrastructure code. Per D-02, apply the same module.exports pattern used by other grd/bin/lib/ modules.
  </action>
  <verify>
    <automated>node -e "const s = require('./grd/bin/lib/security.cjs'); const fns = ['validatePath','requireSafePath','INJECTION_PATTERNS','scanForInjection','sanitizeForPrompt','validateShellArg','safeJsonParse','validatePhaseNumber','validateFieldName']; fns.forEach(f => { if (!s[f]) throw new Error('Missing: ' + f); }); console.log('All 9 security exports present')"</automated>
  </verify>
  <acceptance_criteria>
    - grd/bin/lib/security.cjs exists
    - `node -e "require('./grd/bin/lib/security.cjs')"` exits 0 (no syntax errors)
    - All 9 exports listed above are present and non-undefined
    - File contains `module.exports` with all 9 names
  </acceptance_criteria>
  <done>security.cjs exists in grd/bin/lib/ with all 9 upstream exports, loads without error</done>
</task>

<task type="auto">
  <name>Task 2: Sync core.cjs with upstream v1.28.0</name>
  <files>grd/bin/lib/core.cjs</files>
  <read_first>
    - grd/bin/lib/core.cjs (current GRD version -- MUST read fully to see existing state)
    - ~/.claude/get-shit-done/bin/lib/core.cjs (upstream v1.28.0 source of truth)
  </read_first>
  <action>
Per D-04, diff upstream core.cjs against GRD's current core.cjs and apply the delta. Per research, core.cjs has NO research-specific modifications -- this is a pure additive sync.

Add ALL new functions from upstream v1.28.0 (785 diff lines). The new functions to add:
- `detectSubRepos(cwd)` -- scan child dirs for separate git repos
- `findProjectRoot(startDir)` -- walk up to find project root owning .planning/
- `resolveWorktreeRoot(cwd)` -- handle git worktree setups
- `withPlanningLock(cwd, fn)` -- file locking for concurrent access
- `normalizeMd(content)` -- normalize line endings in markdown output
- `planningDir(cwd, ws)` -- resolve .planning/ path (workstream-aware)
- `planningRoot(cwd)` -- resolve planning root
- `planningPaths(cwd, ws)` -- structured paths object for all planning files
- `getActiveWorkstream(cwd)` / `setActiveWorkstream(cwd, name)` -- workstream state
- `extractCurrentMilestone(content, cwd)` -- replaces stripShippedMilestones usage
- `extractOneLinerFromBody(content)` -- fallback one-liner extraction
- `filterPlanFiles(files)` / `filterSummaryFiles(files)` -- file filtering helpers
- `getPhaseFileStats(phaseDir)` -- phase directory statistics
- `readSubdirectories(dirPath, sort)` -- directory listing helper
- `reapStaleTempFiles(prefix, opts)` -- temp file cleanup
- `MODEL_ALIAS_MAP` -- model name alias mapping
- `isInsideFencedBlock` / `isClosingFence` -- markdown parsing helpers

Also update existing functions if upstream modified them (e.g., `execGit` may now use `execFileSync`).

Add ALL new items to module.exports at the bottom. Preserve ALL existing exports.

Tech debt D-11: Check if `replaceInCurrentMilestone` is still exported upstream. Research says it IS still exported and used by phase.cjs and roadmap.cjs -- keep it.

IMPORTANT: If upstream uses `require('child_process').execFileSync` at the top of the file, add that import too.

Run tests after completion.
  </action>
  <verify>
    <automated>node -e "const c = require('./grd/bin/lib/core.cjs'); const fns = ['normalizeMd','extractCurrentMilestone','extractOneLinerFromBody','resolveWorktreeRoot','withPlanningLock','findProjectRoot','detectSubRepos','reapStaleTempFiles','MODEL_ALIAS_MAP','planningDir','planningRoot','planningPaths','getActiveWorkstream','setActiveWorkstream','filterPlanFiles','filterSummaryFiles','getPhaseFileStats','readSubdirectories']; fns.forEach(f => { if (c[f] === undefined) throw new Error('Missing: ' + f); }); console.log('All 18 new core exports present')" && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - core.cjs contains `function planningDir(` or `const planningDir =` or equivalent
    - core.cjs contains `function planningPaths(` or `const planningPaths =` or equivalent
    - core.cjs contains `function normalizeMd(` or `const normalizeMd =` or equivalent
    - core.cjs contains `function extractCurrentMilestone(` or equivalent
    - core.cjs module.exports includes all 18 new export names listed above
    - All existing exports (execGit, stripShippedMilestones, replaceInCurrentMilestone, etc.) still present
    - `node scripts/run-tests.cjs` shows 513+ passing (same baseline or better)
  </acceptance_criteria>
  <done>core.cjs has all 18+ new upstream exports, all existing exports preserved, all tests pass</done>
</task>

</tasks>

<verification>
- `node -e "require('./grd/bin/lib/security.cjs')"` exits 0
- `node -e "require('./grd/bin/lib/core.cjs')"` exits 0
- `node scripts/run-tests.cjs` shows 513+ passing tests
- No regressions in existing functionality
</verification>

<success_criteria>
- security.cjs exists with 9 exports
- core.cjs has 18+ new exports plus all existing exports
- Test suite passes (513+ pass, matching pre-sync baseline)
</success_criteria>

<output>
After completion, create `.planning/phases/25-upstream-sync-to-v1-28-0/25-01-SUMMARY.md`
</output>

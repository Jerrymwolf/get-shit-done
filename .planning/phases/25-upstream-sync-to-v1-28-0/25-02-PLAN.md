---
phase: 25-upstream-sync-to-v1-28-0
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - grd/bin/lib/config.cjs
  - grd/bin/lib/init.cjs
  - grd/bin/lib/state.cjs
  - grd/bin/lib/phase.cjs
  - grd/bin/lib/commands.cjs
autonomous: true
requirements:
  - SYNC-01

must_haves:
  truths:
    - "config.cjs has all new upstream config keys AND preserves GRD research extensions (vault_path, commit_research, researcher_tier, review_type, epistemological_stance, SMART_DEFAULTS, configWithDefaults, applySmartDefaults)"
    - "init.cjs has sub-repo support and fallback phase resolution AND preserves research config fields in init output"
    - "state.cjs uses upstream stateExtractField/stateReplaceField AND GRD duplicate stateExtractField is removed (tech debt D-11)"
    - "phase.cjs has custom phase naming and planningDir AND model-profiles namespace is untouched"
    - "commands.cjs has security sanitization and noVerify param AND preserves GRD research command wiring"
    - "All 514+ tests pass after sync"
  artifacts:
    - path: "grd/bin/lib/config.cjs"
      provides: "Config management with new keys + research extensions"
      contains: "SMART_DEFAULTS"
    - path: "grd/bin/lib/init.cjs"
      provides: "Init with sub-repo support + research config"
      contains: "researcher_tier"
    - path: "grd/bin/lib/state.cjs"
      provides: "State management with security validation + shared helpers"
      contains: "stateReplaceFieldWithFallback"
    - path: "grd/bin/lib/phase.cjs"
      provides: "Phase management with custom naming + planningDir"
      contains: "planningDir"
    - path: "grd/bin/lib/commands.cjs"
      provides: "Command execution with security + noVerify"
      contains: "sanitizeForPrompt"
  key_links:
    - from: "grd/bin/lib/config.cjs"
      to: "grd/bin/lib/core.cjs"
      via: "planningDir/planningPaths imports from Plan 01"
      pattern: "require.*core"
    - from: "grd/bin/lib/state.cjs"
      to: "grd/bin/lib/security.cjs"
      via: "validatePath, validateFieldName imports from Plan 01"
      pattern: "require.*security"
    - from: "grd/bin/lib/commands.cjs"
      to: "grd/bin/lib/security.cjs"
      via: "sanitizeForPrompt import from Plan 01"
      pattern: "require.*security"
---

<objective>
Sync the 5 HIGH-conflict shared CJS modules: config.cjs, init.cjs, state.cjs, phase.cjs, and commands.cjs. These modules have the most research-extension conflicts and require case-by-case merge per D-05. They depend on core.cjs and security.cjs from Plan 01.

Purpose: Sync the most complex modules where research extensions must be carefully preserved.
Output: 5 updated CJS modules with upstream improvements + research extensions intact, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-CONTEXT.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-RESEARCH.md
@.planning/phases/25-upstream-sync-to-v1-28-0/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync config.cjs, init.cjs, and state.cjs (highest-conflict modules)</name>
  <files>grd/bin/lib/config.cjs, grd/bin/lib/init.cjs, grd/bin/lib/state.cjs</files>
  <read_first>
    - grd/bin/lib/config.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/config.cjs (upstream v1.28.0)
    - grd/bin/lib/init.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/init.cjs (upstream v1.28.0)
    - grd/bin/lib/state.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/state.cjs (upstream v1.28.0)
  </read_first>
  <action>
For each module, diff upstream v1.28.0 against GRD's current version, apply upstream delta, and preserve research extensions per D-04 and D-05.

**config.cjs (287 diff lines -- RESEARCH CONFLICT):**
1. Add new VALID_CONFIG_KEYS: `firecrawl`, `exa_search`, `workflow.auto_advance`, `workflow.node_repair`, `workflow.node_repair_budget`, `workflow.text_mode`, `workflow.research_before_questions`, `workflow.discuss_mode`, `workflow.skip_discuss`, `git.quick_branch_template`, `hooks.context_warnings`
2. PRESERVE GRD research keys in VALID_CONFIG_KEYS: `vault_path`, `commit_research`, `researcher_tier`, `review_type`, `epistemological_stance`, `workflow.critical_appraisal`, `workflow.temporal_positioning`, `workflow.synthesis`
3. PRESERVE the entire `SMART_DEFAULTS` object (~76 lines)
4. PRESERVE `configWithDefaults()` function
5. PRESERVE `applySmartDefaults()` function
6. Apply any other upstream changes to existing functions (e.g., new error handling, planningPaths usage)
7. Update require() destructuring to include new core.cjs imports if needed

**init.cjs (806 diff lines -- RESEARCH CONFLICT):**
1. Add `getLatestCompletedMilestone(cwd)` function
2. Add `withProjectRoot(cwd, result)` function
3. Add fallback phase resolution from ROADMAP.md when no phase directory exists
4. Add `sub_repos` and `context_window` to init output
5. PRESERVE research config fields in init output: `researcher_tier`, `review_type`, `epistemological_stance`, `critical_appraisal`, `temporal_positioning`, `synthesis`
6. KEEP `grd-executor` and `grd-verifier` agent names (do NOT change to `gsd-executor`/`gsd-verifier`)
7. Update require() destructuring for new core.cjs imports (planningDir, planningPaths, etc.)

**state.cjs (562 diff lines -- MODERATE CONFLICT):**
1. Add `getStatePath(cwd)` shorthand function
2. Add upstream's `stateExtractField(content, fieldName)` -- the shared version that handles bold and plain formats
3. DELETE GRD's duplicate `stateExtractField` at line ~12 (tech debt D-11) -- replace with upstream's proper version
4. Add `stateReplaceField` and `stateReplaceFieldWithFallback` functions
5. Add security validation: `validatePath()` calls, `validateFieldName()` calls
6. Replace all hardcoded `.planning/` paths with `planningPaths(cwd)` calls
7. Add `require('./security.cjs')` destructuring for validatePath, validateFieldName
8. Update `require('./core.cjs')` destructuring for planningPaths, planningDir

Run tests after all three files are updated.
  </action>
  <verify>
    <automated>node -e "const c = require('./grd/bin/lib/config.cjs'); if (!c.SMART_DEFAULTS) throw new Error('Missing SMART_DEFAULTS'); if (!c.configWithDefaults) throw new Error('Missing configWithDefaults'); console.log('config.cjs OK')" && node -e "const i = require('./grd/bin/lib/init.cjs'); console.log('init.cjs loads OK')" && node -e "const s = require('./grd/bin/lib/state.cjs'); if (!s.stateReplaceFieldWithFallback) throw new Error('Missing stateReplaceFieldWithFallback'); console.log('state.cjs OK')" && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - config.cjs contains `firecrawl` in VALID_CONFIG_KEYS
    - config.cjs contains `vault_path` in VALID_CONFIG_KEYS (research preserved)
    - config.cjs contains `SMART_DEFAULTS` (research preserved)
    - config.cjs contains `configWithDefaults` (research preserved)
    - config.cjs contains `applySmartDefaults` (research preserved)
    - init.cjs contains `researcher_tier` (research config preserved)
    - init.cjs contains `grd-executor` (namespace preserved, NOT gsd-executor)
    - init.cjs contains `sub_repos` (new upstream feature)
    - state.cjs contains `stateReplaceFieldWithFallback` (new upstream function)
    - state.cjs contains `validatePath` or `validateFieldName` (security integration)
    - state.cjs does NOT have duplicate stateExtractField definition (tech debt D-11 fixed)
    - `node scripts/run-tests.cjs` shows 513+ passing
  </acceptance_criteria>
  <done>config.cjs, init.cjs, and state.cjs synced with upstream improvements, research extensions preserved, tech debt D-11 resolved, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Sync phase.cjs and commands.cjs</name>
  <files>grd/bin/lib/phase.cjs, grd/bin/lib/commands.cjs</files>
  <read_first>
    - grd/bin/lib/phase.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/phase.cjs (upstream v1.28.0)
    - grd/bin/lib/commands.cjs (current GRD version)
    - ~/.claude/get-shit-done/bin/lib/commands.cjs (upstream v1.28.0)
  </read_first>
  <action>
**phase.cjs (559 diff lines):**
1. Add custom phase naming support (`config.phase_naming === 'custom'`) -- wherever phase names are generated, add conditional for custom naming
2. Replace `stripShippedMilestones` usage with `extractCurrentMilestone` from core.cjs
3. Add `stateExtractField`, `stateReplaceField`, `stateReplaceFieldWithFallback` imports from state.cjs
4. Replace hardcoded `.planning/` paths with `planningDir(cwd)` calls
5. Add `readSubdirectories` import from core.cjs
6. Add `loadConfig` import
7. Update require() destructuring for all new imports

Per research, phase.cjs has NO research-specific code. This is a straightforward upstream adoption.

**commands.cjs (306 diff lines):**
1. Add new imports from core.cjs: `extractCurrentMilestone`, `planningDir`, `planningPaths`, `extractOneLinerFromBody`
2. Add `getRoadmapPhaseInternal` import if it exists in upstream
3. Replace hardcoded paths with `planningDir(cwd)` / `planningPaths(cwd)` calls
4. Add null byte rejection in path handling: `if (somePath.includes('\0'))` guards
5. Add `sanitizeForPrompt()` import from security.cjs -- use for commit message sanitization
6. Add `noVerify` parameter to `cmdCommit()` function signature
7. Add auto-branch creation logic for `branching_strategy` config
8. Replace bare `catch {}` with `catch { /* intentionally empty */ }`
9. PRESERVE GRD's existing research command wiring (any GRD-specific command functions)

Run tests after completion.
  </action>
  <verify>
    <automated>node -e "const p = require('./grd/bin/lib/phase.cjs'); console.log('phase.cjs loads OK')" && node -e "const cmd = require('./grd/bin/lib/commands.cjs'); console.log('commands.cjs loads OK')" && node scripts/run-tests.cjs</automated>
  </verify>
  <acceptance_criteria>
    - phase.cjs contains `planningDir` (path migration)
    - phase.cjs contains `extractCurrentMilestone` (new function usage)
    - phase.cjs contains `readSubdirectories` (new import)
    - commands.cjs contains `sanitizeForPrompt` (security integration)
    - commands.cjs contains `noVerify` (new parameter)
    - commands.cjs contains `planningDir` or `planningPaths` (path migration)
    - `node scripts/run-tests.cjs` shows 513+ passing
  </acceptance_criteria>
  <done>phase.cjs and commands.cjs synced with upstream improvements, all tests pass</done>
</task>

</tasks>

<verification>
- All 5 modules load without error: `node -e "['config','init','state','phase','commands'].forEach(m => require('./grd/bin/lib/' + m + '.cjs'))"`
- `node scripts/run-tests.cjs` shows 513+ passing tests
- Research extensions preserved in config.cjs and init.cjs
- Tech debt D-11 (duplicate stateExtractField) resolved
</verification>

<success_criteria>
- 5 HIGH-conflict modules synced with upstream v1.28.0
- Research extensions preserved (SMART_DEFAULTS, research config fields, grd-* namespace)
- Tech debt D-11 resolved (duplicate stateExtractField removed)
- Test suite passes (513+ pass)
</success_criteria>

<output>
After completion, create `.planning/phases/25-upstream-sync-to-v1-28-0/25-02-SUMMARY.md`
</output>

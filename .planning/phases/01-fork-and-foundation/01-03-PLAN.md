---
phase: 01-fork-and-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - grd/bin/lib/vault.cjs
  - test/vault.test.cjs
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02

must_haves:
  truths:
    - "Vault write creates both a .md note file and its -sources/ sibling folder at the configured vault_path"
    - "File naming convention is enforced: {descriptive-slug}_{date-acquired}.{ext}"
    - "vault_path supports both absolute paths and tilde (~/) expansion"
  artifacts:
    - path: "grd/bin/lib/vault.cjs"
      provides: "Vault write module with writeNote, generateSourceFilename, expandTilde exports"
      exports: ["writeNote", "ensureVaultDir", "generateSourceFilename", "expandTilde"]
      min_lines: 40
    - path: "test/vault.test.cjs"
      provides: "Unit tests for vault.cjs functions"
      min_lines: 30
  key_links:
    - from: "grd/bin/lib/vault.cjs"
      to: "grd/templates/config.json"
      via: "vault_path config value consumed by vault module"
      pattern: "vault_path"
    - from: "grd/bin/lib/vault.cjs"
      to: "node:fs/promises"
      via: "writeFile and mkdir for note creation"
      pattern: "fs\\.writeFile|fs\\.mkdir"
---

<objective>
Create the filesystem vault write module (vault.cjs) that writes research notes and their -sources/ sibling folders to disk, with file naming convention enforcement and tilde expansion.

Purpose: This is the foundational write operation that all research workflows build upon. Phase 2 makes it atomic with git commits; Phase 3 adds source acquisition. This plan establishes the basic write capability.
Output: vault.cjs module with writeNote, generateSourceFilename, expandTilde functions, plus unit tests.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md
@GRD-Fork-Plan.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create vault.cjs with TDD</name>
  <files>grd/bin/lib/vault.cjs, test/vault.test.cjs</files>
  <behavior>
    - expandTilde("~/research") returns path.join(os.homedir(), "research")
    - expandTilde("/absolute/path") returns "/absolute/path" unchanged
    - expandTilde("relative/path") returns "relative/path" unchanged
    - generateSourceFilename("test-slug", "pdf") returns "test-slug_YYYY-MM-DD.pdf" (today's date)
    - generateSourceFilename("My Paper Title!", "md") returns "my-paper-title-_YYYY-MM-DD.md" (sanitized slug)
    - generateSourceFilename("arXiv-2502.06472", "pdf") returns "arxiv-2502-06472_YYYY-MM-DD.pdf" (dots replaced)
    - writeNote(vaultPath, "test-note.md", "# Content") creates the .md file at vaultPath/test-note.md
    - writeNote(vaultPath, "test-note.md", "# Content") creates test-note-sources/ directory as sibling
    - writeNote(vaultPath, "sub/dir/note.md", "# Content") creates intermediate directories
    - writeNote("relative/path", "note.md", "content") throws error (vault_path must be absolute)
    - ensureVaultDir validates path is absolute and creates directories recursively
  </behavior>
  <action>
1. Create `test/vault.test.cjs` first (RED phase):
   - Use Node.js built-in test runner (`const { describe, it } = require('node:test')`)
   - Use `assert` from `node:assert/strict`
   - Use `fs/promises` and `os.tmpdir()` for temp directory tests
   - Test expandTilde: three cases (tilde path, absolute path, relative path)
   - Test generateSourceFilename: slug sanitization, date format, extension handling
   - Test writeNote: creates note file with content, creates -sources/ sibling, creates intermediate dirs, rejects non-absolute vault_path
   - Clean up temp directories in afterEach/after hooks

2. Run tests -- they should ALL FAIL (no vault.cjs yet):
   ```
   node --test test/vault.test.cjs
   ```

3. Create `grd/bin/lib/vault.cjs` (GREEN phase):
   - Use the skeleton from RESEARCH.md as starting point
   - CommonJS format (module.exports)
   - Dependencies: node:fs/promises, node:path, node:os (all built-in, zero external deps)
   - `expandTilde(p)`: if starts with `~/`, join with os.homedir(); otherwise return as-is
   - `ensureVaultDir(vaultPath, notePath)`: expand tilde, validate absolute path (throw if not), create directory tree with `fs.mkdir({ recursive: true })`, return resolved path
   - `writeNote(vaultPath, notePath, content)`: call ensureVaultDir, write note file with fs.writeFile, create -sources/ sibling folder (replace .md extension with -sources), return { notePath: fullPath, sourcesDir }
   - `generateSourceFilename(slug, ext)`: sanitize slug (lowercase, replace non-alphanumeric with hyphens, collapse multiple hyphens), append today's date as YYYY-MM-DD, append extension
   - Export: { writeNote, ensureVaultDir, generateSourceFilename, expandTilde }

4. Run tests again -- they should ALL PASS:
   ```
   node --test test/vault.test.cjs
   ```

5. Refactor if needed (REFACTOR phase): clean up any code duplication, ensure error messages are clear.
  </action>
  <verify>
    <automated>node --test test/vault.test.cjs</automated>
  </verify>
  <done>vault.cjs exports writeNote, ensureVaultDir, generateSourceFilename, expandTilde. All unit tests pass. writeNote creates both .md file and -sources/ sibling folder. generateSourceFilename enforces {slug}_{date}.{ext} convention. expandTilde handles ~/paths. Non-absolute vault_path throws error.</done>
</task>

</tasks>

<verification>
1. `node --test test/vault.test.cjs` -- all tests pass
2. `node -e "const v = require('./grd/bin/lib/vault.cjs'); console.log(Object.keys(v))"` shows all 4 exports
3. `node -e "const v = require('./grd/bin/lib/vault.cjs'); console.log(v.generateSourceFilename('test-paper', 'pdf'))"` outputs correctly formatted filename
4. `node -e "const v = require('./grd/bin/lib/vault.cjs'); console.log(v.expandTilde('~/test'))"` outputs expanded path
</verification>

<success_criteria>
- vault.cjs is a zero-dependency CommonJS module with 4 exported functions
- writeNote creates note file + -sources/ sibling folder at vault_path
- generateSourceFilename enforces {descriptive-slug}_{date}.{ext} convention
- expandTilde handles ~/ paths correctly
- Non-absolute vault_path is rejected with clear error
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-03-SUMMARY.md`
</output>

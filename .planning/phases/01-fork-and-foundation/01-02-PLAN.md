---
phase: 01-fork-and-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .gitattributes
  - grd/templates/research-note.md
  - grd/templates/source-log.md
  - grd/templates/bootstrap.md
  - grd/templates/research-task.md
  - grd/templates/config.json
  - grd/references/source-protocol.md
  - grd/references/note-format.md
  - grd/references/research-verification.md
autonomous: true
requirements:
  - FORK-04
  - FOUN-03
  - FOUN-04

must_haves:
  truths:
    - "Git LFS is configured to track binary files before any binaries are committed"
    - "Research note template produces valid notes with YAML frontmatter and all required sections"
    - "config.json template includes vault_path and commit_research fields"
  artifacts:
    - path: ".gitattributes"
      provides: "LFS tracking for *.pdf, *.png, *.jpg, *.jpeg, *.gif, *.svg"
      contains: "filter=lfs"
    - path: "grd/templates/research-note.md"
      provides: "Research note template with frontmatter + 5 sections"
      contains: "Key Findings"
    - path: "grd/templates/source-log.md"
      provides: "SOURCE-LOG.md template with table format"
      contains: "Source | URL | Method"
    - path: "grd/templates/bootstrap.md"
      provides: "BOOTSTRAP.md template with three tiers"
      contains: "Already Established"
    - path: "grd/templates/research-task.md"
      provides: "Research task XML template with src blocks"
      contains: "src method="
    - path: "grd/templates/config.json"
      provides: "Config template with vault_path field"
      contains: "vault_path"
    - path: "grd/references/source-protocol.md"
      provides: "Source Attachment Protocol reference"
    - path: "grd/references/note-format.md"
      provides: "Canonical research note format reference"
    - path: "grd/references/research-verification.md"
      provides: "Two-tier verification criteria reference"
  key_links:
    - from: "grd/templates/config.json"
      to: "grd/bin/lib/config.cjs"
      via: "config schema parsed by config module"
      pattern: "vault_path"
---

<objective>
Configure Git LFS for binary tracking, create all four research templates, extend config.json with vault_path, and add three research reference files.

Purpose: Establish the foundational templates and configuration that all research workflows depend on. LFS must be configured before Phase 3 adds binary source files.
Output: .gitattributes with LFS patterns, 4 template files, updated config.json template, 3 reference files.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md
@GRD-Fork-Plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Git LFS and create research templates</name>
  <files>.gitattributes, grd/templates/research-note.md, grd/templates/source-log.md, grd/templates/bootstrap.md, grd/templates/research-task.md</files>
  <action>
1. Create `.gitattributes` at project root with LFS tracking patterns (use exact format from RESEARCH.md):
   ```
   # PDF documents (research papers)
   *.pdf filter=lfs diff=lfs merge=lfs -text

   # Images
   *.png filter=lfs diff=lfs merge=lfs -text
   *.jpg filter=lfs diff=lfs merge=lfs -text
   *.jpeg filter=lfs diff=lfs merge=lfs -text
   *.gif filter=lfs diff=lfs merge=lfs -text
   *.svg filter=lfs diff=lfs merge=lfs -text
   ```

2. Create `grd/templates/research-note.md` using the template from GRD-Fork-Plan.md:
   - YAML frontmatter: project, domain, status (draft|reviewed|final), date, sources (count)
   - Sections: Key Findings, Analysis, Implications for [Project], Open Questions, References
   - Include inline comments/instructions for each section explaining what goes there
   - The "Implications for [Project]" section title uses a placeholder [Project] that executors replace with the actual project name

3. Create `grd/templates/source-log.md`:
   - Title: "# Source Acquisition Log"
   - Table with columns: Source | URL | Method | File | Status | Notes
   - Status values documented: acquired | partial | paywall | unavailable | rate-limited
   - Include one example row as a template comment

4. Create `grd/templates/bootstrap.md`:
   - Title: "# Bootstrap: Existing Research Inventory"
   - Three sections with table templates:
     - "## Already Established (do not re-research)" -- columns: Finding | Source Note | Confidence | Date
     - "## Partially Established (extend, don't restart)" -- columns: Finding | What's Known | What's Missing | Source Note
     - "## Not Yet Researched" -- columns: Topic | Why It Matters | Target Note

5. Create `grd/templates/research-task.md`:
   - XML task template with `type="research"`
   - Include `<sources>` block with `<src method="" format="">` elements
   - Include `<o>` (output path) element
   - Include research-oriented `<verify>` block with source-attachment checks
   - Include `<done>` element
   - Document valid method values: firecrawl, web_fetch, wget, gh-cli
   - Document valid format values: md, pdf, png, sql
  </action>
  <verify>
    <automated>cat .gitattributes | grep -c 'filter=lfs' && test -f grd/templates/research-note.md && test -f grd/templates/source-log.md && test -f grd/templates/bootstrap.md && test -f grd/templates/research-task.md && grep -q 'Key Findings' grd/templates/research-note.md && grep -q 'Source | URL | Method' grd/templates/source-log.md && grep -q 'Already Established' grd/templates/bootstrap.md && grep -q 'src method=' grd/templates/research-task.md && echo "ALL TEMPLATES OK"</automated>
  </verify>
  <done>.gitattributes tracks 6 binary extensions via LFS. Four research templates exist with correct structure: research-note.md (frontmatter + 5 sections), source-log.md (table format), bootstrap.md (3 tiers), research-task.md (XML with src blocks).</done>
</task>

<task type="auto">
  <name>Task 2: Extend config.json and create research reference files</name>
  <files>grd/templates/config.json, grd/references/source-protocol.md, grd/references/note-format.md, grd/references/research-verification.md</files>
  <action>
1. Read existing `grd/templates/config.json` (already renamed from Plan 01). Add two new fields at the top level:
   - `"vault_path": ""` -- string, absolute path to research output directory
   - `"commit_research": true` -- boolean, whether vault writes auto-commit to git
   Preserve ALL existing GSD config fields (mode, granularity, workflow, planning, parallelization, gates, safety). Use the config.json example from RESEARCH.md as reference.

2. Create `grd/references/source-protocol.md`:
   - Full Source Attachment Protocol documentation
   - Folder convention: Note.md has sibling Note-sources/
   - File naming convention: {descriptive-slug}_{date-acquired}.{ext}
   - Acquisition protocol by source type table (from GRD-Fork-Plan.md: arXiv, GitHub, docs, conference papers, etc.)
   - Fallback chain: firecrawl -> web_fetch -> wget/curl -> mark unavailable
   - SOURCE-LOG.md format specification
   - The rule: every cited source must have a corresponding file or documented exception

3. Create `grd/references/note-format.md`:
   - Canonical research note structure with all sections documented
   - Frontmatter field descriptions (project, domain, status, date, sources)
   - Section-by-section guidance (Key Findings, Analysis, Implications, Open Questions, References)
   - Citation format: link to local source file in -sources/, not URLs
   - Status values: draft, reviewed, final

4. Create `grd/references/research-verification.md`:
   - Two-tier verification criteria
   - Tier 1: Goal-backward -- "What must be TRUE for this research question to be answered?"
   - Tier 2: Source audit -- frontmatter complete, references have files, SOURCE-LOG.md complete, no orphan files
   - Verification runs in order: Tier 1 first, Tier 2 second
   - Fix task generation via /grd:quick when verification fails
  </action>
  <verify>
    <automated>grep -q 'vault_path' grd/templates/config.json && grep -q 'commit_research' grd/templates/config.json && test -f grd/references/source-protocol.md && test -f grd/references/note-format.md && test -f grd/references/research-verification.md && echo "CONFIG + REFERENCES OK"</automated>
  </verify>
  <done>config.json template includes vault_path and commit_research fields alongside all existing GSD fields. Three reference files exist: source-protocol.md (full SAP), note-format.md (canonical note structure), research-verification.md (two-tier verification).</done>
</task>

</tasks>

<verification>
1. `cat .gitattributes | grep -c 'filter=lfs'` returns 6 (pdf, png, jpg, jpeg, gif, svg)
2. All 4 template files exist in grd/templates/
3. All 3 reference files exist in grd/references/
4. config.json includes vault_path and commit_research
5. Research note template has all 5 required sections
</verification>

<success_criteria>
- Git LFS configured for 6 binary file types before any binaries exist
- Research note template produces valid notes with correct frontmatter and sections
- config.json template has vault_path (string) and commit_research (boolean) fields
- Three research reference files provide complete documentation for source protocol, note format, and verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-02-SUMMARY.md`
</output>

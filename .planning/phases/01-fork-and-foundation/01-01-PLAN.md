---
phase: 01-fork-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - commands/grd/ (all 32 command files)
  - agents/ (all 12 agent files, renamed grd-*)
  - grd/ (renamed directory)
  - grd/bin/grd-tools.cjs
  - bin/install.js
  - scripts/rename-gsd-to-grd.cjs
  - scripts/verify-rename.cjs
autonomous: true
requirements:
  - FORK-01
  - FORK-02
  - FORK-03

must_haves:
  truths:
    - "GRD repo exists as an independent clone with renamed directory structure"
    - "All /gsd:* commands are /grd:* commands with zero stale references"
    - "npm pack --dry-run succeeds and shows grd package with all expected files"
  artifacts:
    - path: "commands/grd/"
      provides: "Renamed command directory with all 32 command files"
    - path: "agents/grd-executor.md"
      provides: "Renamed agent files (grd-* prefix)"
    - path: "grd/bin/grd-tools.cjs"
      provides: "Renamed tools entry point"
    - path: "package.json"
      provides: "Package named grd with correct bin and files fields"
    - path: "scripts/verify-rename.cjs"
      provides: "Rename verification script"
  key_links:
    - from: "commands/grd/*.md"
      to: "grd/workflows/*.md"
      via: "@-references in execution_context"
      pattern: "grd/workflows/"
    - from: "commands/grd/*.md"
      to: "agents/grd-*.md"
      via: "agent: frontmatter field"
      pattern: "agent: grd-"
    - from: "bin/install.js"
      to: "grd/"
      via: "directory path references"
      pattern: "grd"
---

<objective>
Clone the upstream GSD repo and perform the complete gsd-to-grd rename across all 148 files, 1,071+ string occurrences.

Purpose: Establish GRD as an independent, properly namespaced project that can coexist with GSD without conflicts.
Output: A fully renamed GRD repository with working package.json, renamed commands/agents/workflows, and zero stale gsd references.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-and-foundation/01-RESEARCH.md
@GRD-Fork-Plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone upstream GSD and create rename script</name>
  <files>scripts/rename-gsd-to-grd.cjs, scripts/verify-rename.cjs</files>
  <action>
1. Clone the upstream repo into the current project directory:
   ```
   git clone https://github.com/gsd-build/get-shit-done.git .gsd-upstream-temp
   ```
   Copy all files from the clone into the project root (preserving structure but not .git):
   ```
   cp -R .gsd-upstream-temp/bin .gsd-upstream-temp/commands .gsd-upstream-temp/agents .gsd-upstream-temp/get-shit-done .gsd-upstream-temp/hooks .gsd-upstream-temp/scripts .gsd-upstream-temp/LICENSE .gsd-upstream-temp/README.md .gsd-upstream-temp/package.json ./
   rm -rf .gsd-upstream-temp
   ```

2. Create `scripts/rename-gsd-to-grd.cjs` -- a Node.js script that:
   - Renames directories first:
     - `commands/gsd/` -> `commands/grd/`
     - `get-shit-done/` -> `grd/`
   - Renames agent files: all `agents/gsd-*.md` -> `agents/grd-*.md`
   - Renames `grd/bin/gsd-tools.cjs` -> `grd/bin/grd-tools.cjs`
   - Performs string replacements across ALL .md, .cjs, .js files using the translation table from RESEARCH.md (longest patterns first to avoid partial matches):
     - `grdesearch-synthesizer` -> `grd-research-synthesizer`
     - `gsd-integration-checker` -> `grd-integration-checker`
     - `gsd-project-researcher` -> `grd-project-researcher`
     - `gsd-phase-researcher` -> `grd-phase-researcher`
     - `gsd-codebase-mapper` -> `grd-codebase-mapper`
     - `gsd-nyquist-auditor` -> `grd-nyquist-auditor`
     - `gsd-plan-checker` -> `grd-plan-checker`
     - `grdoadmapper` -> `grd-roadmapper`
     - `gsd-executor` -> `grd-executor`
     - `gsd-debugger` -> `grd-debugger`
     - `gsd-planner` -> `grd-planner`
     - `gsd-verifier` -> `grd-verifier`
     - `gsd-tools.cjs` -> `grd-tools.cjs`
     - `get-shit-done-cc` -> `grd`
     - `get-shit-done` -> `grd` (but NOT inside URLs containing `gsd-build/get-shit-done` -- preserve upstream references)
     - `/gsd:` -> `/grd:`
     - `commands/gsd/` -> `commands/grd/`
     - `gsd_state_version` -> `grd_state_version`
   - CRITICAL edge case: Do NOT replace `gsd-build` (GitHub org name) or URLs pointing to the upstream repo
   - Prints a summary of files modified and replacements made

3. Create `scripts/verify-rename.cjs` -- a verification script that:
   - Greps ALL files in commands/, agents/, grd/, workflows/ for stale `/gsd:` references (excluding `/grd:` and URLs to upstream)
   - Greps for stale `gsd-tools.cjs` (should be `grd-tools.cjs`)
   - Greps for stale `get-shit-done/` directory references (should be `grd/`)
   - Greps for stale agent names (e.g., `gsd-executor` without the `-r-`)
   - Exits with code 0 if clean, code 1 with details if stale references found
   - Excludes: URLs containing `gsd-build/get-shit-done`, comments about upstream, LICENSE file

4. Run the rename script: `node scripts/rename-gsd-to-grd.cjs`
5. Run the verification script: `node scripts/verify-rename.cjs`
6. Fix any stale references the verification script finds
  </action>
  <verify>
    <automated>node scripts/verify-rename.cjs && ls commands/grd/ | wc -l && ls agents/grd-*.md | wc -l && test -f grd/bin/grd-tools.cjs</automated>
  </verify>
  <done>All 148 files renamed/updated. Verification script confirms zero stale gsd references (excluding upstream URLs). commands/grd/ has 32 files, agents/ has 12 grd-* files, grd/bin/grd-tools.cjs exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update package.json and verify npm pack</name>
  <files>package.json, bin/install.js</files>
  <action>
1. Update package.json to match the GRD spec (use RESEARCH.md code example as reference):
   - name: "grd"
   - version: "0.1.0"
   - description: "A research-oriented fork of get-shit-done-cc. Meta-prompting system for AI research workflows with source attachment."
   - bin: { "grd": "bin/install.js" }
   - files: ["bin", "commands", "grd", "agents", "hooks/dist", "scripts"]
   - keywords: ["claude", "claude-code", "ai", "research", "meta-prompting", "context-engineering"]
   - author: "Jeremiah Wolf"
   - license: "MIT"
   - engines: { "node": ">=18.0.0" }
   - Preserve existing devDependencies (esbuild, c8) and scripts

2. Verify install.js internal references have been updated by the rename script:
   - Check that `file.startsWith('grd-')` pattern is in agent cleanup (was `gsd-`)
   - Check that banner text references GRD
   - Check that CODEX_AGENT_SANDBOX constants use `grd-` prefixed agent names
   - Check that all `get-shit-done` directory path references are `grd`
   - If the rename script missed any install.js-specific patterns, fix them manually

3. Run `npm pack --dry-run` to verify all expected files are included in the package.
   - Should list commands/grd/, agents/grd-*, grd/, bin/install.js
   - Total file count should be close to upstream (148 files)
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.name!=='grd') process.exit(1); if(!p.bin['grd']) process.exit(1); console.log('OK:', p.name, p.version)" && npm pack --dry-run 2>&1 | tail -5</automated>
  </verify>
  <done>package.json has name "grd", bin field points to bin/install.js, files field includes all renamed directories. npm pack --dry-run succeeds and lists all expected files.</done>
</task>

</tasks>

<verification>
1. `node scripts/verify-rename.cjs` exits 0 (zero stale references)
2. `ls commands/grd/ | wc -l` shows 32 command files
3. `ls agents/grd-*.md | wc -l` shows 12 agent files
4. `test -f grd/bin/grd-tools.cjs` succeeds
5. `npm pack --dry-run` succeeds and shows grd package
6. `grep -r '/gsd:' commands/grd/ grd/ agents/ | grep -v 'grd:' | grep -v http | wc -l` shows 0
</verification>

<success_criteria>
- GRD exists as a fully renamed fork with zero namespace conflicts with upstream GSD
- npm pack --dry-run succeeds showing all expected files
- Verification script confirms no stale gsd references remain
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-and-foundation/01-01-SUMMARY.md`
</output>

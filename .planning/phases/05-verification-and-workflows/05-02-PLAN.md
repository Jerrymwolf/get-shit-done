---
phase: 05-verification-and-workflows
plan: 02
type: wiring
wave: 2
depends_on: ["05-01"]
files_modified: [agents/grd-verifier.md, grd/workflows/verify-phase.md]
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03]
user_setup: []

must_haves:
  truths:
    - "The grd-verifier agent uses verify-research.cjs for research-specific two-tier verification"
    - "The verify-phase workflow routes to research verification when research notes are detected"
    - "The discuss/plan/execute/verify loop works end-to-end with research-specific agents"
  artifacts:
    - path: "agents/grd-verifier.md"
      provides: "Research-aware verifier agent with two-tier verification"
      min_lines: 100
    - path: "grd/workflows/verify-phase.md"
      provides: "Phase verification workflow with research routing"
      min_lines: 50
  key_links:
    - from: "agents/grd-verifier.md"
      to: "grd/bin/lib/verify-research.cjs"
      via: "Agent instructions reference verify-research module"
      pattern: "verify-research|verifyNote|verifyTier"
    - from: "grd/workflows/verify-phase.md"
      to: "agents/grd-verifier.md"
      via: "Workflow spawns verifier agent"
      pattern: "grd-verifier"
---

<objective>
Wire the research verification module into the agent and workflow layers so the discuss/plan/execute/verify loop works end-to-end for research projects.

Purpose: The verify-research.cjs module from Plan 01 implements the checks, but agents and workflows need to call it. This plan connects the module to the orchestration layer.
Output: Updated grd-verifier agent prompt and verify-phase workflow with research routing.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verification-and-workflows/05-CONTEXT.md
@.planning/phases/05-verification-and-workflows/05-01-PLAN.md
@agents/grd-verifier.md
@grd/workflows/verify-phase.md
@grd/bin/lib/verify-research.cjs
@grd/references/research-verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update grd-verifier agent for research verification</name>
  <files>agents/grd-verifier.md</files>
  <action>
Update the grd-verifier.md agent prompt to include research-specific verification:

1. Add a research verification section after the existing verification process:
   - Detect research phases: check if phase has research notes (files matching note-format.md template pattern)
   - If research notes found, add research-specific verification steps:
     a. For each research note, extract the research question from frontmatter or task context
     b. Run verifyNote() from verify-research.cjs (Tier 1 + Tier 2)
     c. Map results to the existing verification report format
     d. On failure, call generateFixTasks() and include fix task descriptions in the gaps section

2. Add instructions for using the verify-research.cjs module:
   ```bash
   node -e "
     const { verifyNote } = require('./grd/bin/lib/verify-research.cjs');
     const fs = require('fs');
     const noteContent = fs.readFileSync('path/to/note.md', 'utf8');
     const result = verifyNote(noteContent, 'path/to/note-sources/', null, 'research question');
     console.log(JSON.stringify(result, null, 2));
   "
   ```

3. Update the gap output format to include fix task descriptions from generateFixTasks()

4. Preserve ALL existing verification behavior (goal-backward, artifact checks, key links) — research verification is ADDITIVE
  </action>
  <verify>Read agents/grd-verifier.md and confirm research verification section exists with verify-research.cjs usage</verify>
  <done>grd-verifier.md updated with research-specific two-tier verification</done>
</task>

<task type="auto">
  <name>Task 2: Update verify-phase workflow for research routing</name>
  <files>grd/workflows/verify-phase.md</files>
  <action>
Update verify-phase.md workflow to detect and route research phases:

1. Add a detection step early in the workflow:
   - Check if the phase directory contains research notes (look for -sources/ directories or note files with research frontmatter)
   - Set a flag: `is_research_phase`

2. When `is_research_phase` is true:
   - Include verify-research.cjs in the verifier agent's context
   - Add research-verification.md reference to the agent's file reads
   - Ensure the agent knows to run two-tier verification

3. The verify-phase workflow should pass research context to the verifier agent spawn:
   - Research question (from plan context or task descriptions)
   - Note paths and their -sources/ directories
   - SOURCE-LOG.md locations

4. Preserve all existing verify-phase behavior — research routing is additive
  </action>
  <verify>Read grd/workflows/verify-phase.md and confirm research detection and routing logic exists</verify>
  <done>verify-phase.md updated with research phase detection and routing</done>
</task>

<task type="auto">
  <name>Task 3: Verify orchestration loop wiring</name>
  <files>grd/workflows/verify-phase.md, agents/grd-verifier.md</files>
  <action>
Verification task to confirm the full loop works:

1. Trace the orchestration path:
   - /grd:discuss-phase -> discuss-phase.md workflow (already works, uses grd-tools.cjs)
   - /grd:plan-phase -> plan-phase.md workflow (already works, spawns grd-planner)
   - /grd:execute-phase -> execute-phase.md workflow (already works, spawns grd-executor)
   - /grd:verify-work -> verify-phase.md workflow -> spawns grd-verifier (NOW research-aware)

2. Confirm each workflow references grd-tools.cjs (not gsd-tools.cjs)

3. Confirm the grd-verifier agent:
   - References verify-research.cjs for research notes
   - Calls generateFixTasks() on verification failure
   - Fix tasks can be passed to /grd:quick for execution

4. Document any gaps found in the wiring — these become Phase 5 bugs to fix immediately

5. Run all existing tests to confirm no regressions:
   - node --test test/verify-research.test.cjs
   - node --test test/acquire.test.cjs
   - node --test test/vault.test.cjs
   - node --test test/state.test.cjs
   - node --test test/plan-checker-rules.test.cjs
   - node --test test/bootstrap.test.cjs
  </action>
  <verify>All tests pass, orchestration path traced end-to-end</verify>
  <done>Full orchestration loop confirmed working with research-specific verification</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grd-verifier.md includes research-specific verification with verify-research.cjs
- [ ] verify-phase.md detects research phases and routes accordingly
- [ ] Orchestration loop traced: discuss -> plan -> execute -> verify all reference grd-tools.cjs
- [ ] All existing tests pass (no regressions)
- [ ] Fix task generation wired from verifier to /grd:quick
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The discuss/plan/execute/verify loop works end-to-end for research workflows
- Research verification is additive — doesn't break existing code verification
- No regressions to existing modules
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-and-workflows/05-02-SUMMARY.md`
</output>

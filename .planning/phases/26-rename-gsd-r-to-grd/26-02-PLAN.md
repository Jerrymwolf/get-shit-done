---
phase: 26-rename-gsd-r-to-grd
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - commands/grd/*.md (34 files, content replacement)
  - agents/grd-*.md (16 files, content replacement)
  - hooks/grd-*.js (3 files, content if needed)
  - grd/workflows/*.md (content replacement)
  - bin/install.js
  - README.md
  - docs/DESIGN.md
  - .gitignore
  - package.json
  - test/state.test.cjs
  - test/namespace.test.cjs
autonomous: true
requirements:
  - REN-04
  - REN-05
  - REN-06
  - REN-07
  - REN-09

must_haves:
  truths:
    - "Zero instances of /gsd-r: in active files"
    - "Zero instances of GSD-R in active files"
    - "Zero instances of get-shit-done-r in active files"
    - "install.js marker constants use GRD_ prefix"
    - "package.json files array contains grd not get-research-done"
    - ".gitignore patterns use grd-* not gsd-*"
    - "namespace.test.cjs excludes REQUIREMENTS.md, ROADMAP.md, and CONTEXT.md from .planning/ scan"
    - "npm test passes with zero failures"
  artifacts:
    - path: "bin/install.js"
      provides: "Updated marker constants and variable names"
      contains: "GRD_CODEX_MARKER"
    - path: "package.json"
      provides: "Fixed files array"
      contains: "grd"
    - path: ".gitignore"
      provides: "Updated Codex/Copilot patterns"
      contains: "grd-*"
    - path: "test/namespace.test.cjs"
      provides: "Fixed exclusion list for .planning/ scan"
  key_links:
    - from: "bin/install.js"
      to: "agents/grd-*.md"
      via: "CODEX_AGENT_SANDBOX mapping"
      pattern: "grd-"
    - from: "test/namespace.test.cjs"
      to: ".planning/"
      via: "file exclusion filter"
      pattern: "REQUIREMENTS|ROADMAP|CONTEXT"
---

<objective>
Bulk content replacement across all active files, install.js internal renames, config file updates, and test fixes.

Purpose: Steps 4-7 of D-01 sequence -- eliminate all GSD-R string references from file contents, update install.js marker constants, fix config files, and fix the pre-existing namespace test failure.
Output: Zero instances of gsd-r/GSD-R/get-shit-done-r in active files. All tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-rename-gsd-r-to-grd/26-CONTEXT.md
@.planning/phases/26-rename-gsd-r-to-grd/26-RESEARCH.md
@.planning/phases/26-rename-gsd-r-to-grd/26-01-SUMMARY.md
@docs/GRD-Rename-Spec.md (sections 1.3-1.6 only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk content replacement (6 passes) + install.js internals + config files</name>
  <files>
    commands/grd/*.md
    agents/grd-*.md
    grd/workflows/*.md
    bin/install.js
    README.md
    docs/DESIGN.md
    .gitignore
    package.json
  </files>
  <read_first>
    docs/GRD-Rename-Spec.md
    bin/install.js
    .gitignore
    package.json
    README.md
  </read_first>
  <action>
**PART A: Bulk content replacement (D-01 Step 4, D-02 pass order, REN-04)**

Run 6 replacement passes in EXACT spec order (most-specific to least-specific) per D-02. Each pass runs across ALL active files before the next starts. Active files = all *.md, *.js, *.cjs, *.json files EXCLUDING .planning/, node_modules/, package-lock.json, and the spec docs (docs/GRD-Rename-Spec.md, docs/GRD-v1.2-Research-Reorientation-Spec.md, docs/superpowers/).

**Pass 1:** `/gsd-r:` -> `/grd:` (command prefix)
**Pass 2:** `GSD-R` -> `GRD` (branded name, case-sensitive)
**Pass 3:** `get-shit-done-r` -> `grd` (all path and package refs)
**Pass 4:** `gsd-r-tools.cjs` -> `grd-tools.cjs` (tool binary filename stragglers)
**Pass 5:** `gsd_state_version` -> `grd_state_version` (state variable name)
**Pass 6:** `gsd-r-` (agent prefix) -> `grd-` (agent name references)

CRITICAL per D-09: Do NOT replace bare `get-shit-done` (without `-r`). The ~26 instances in bin/install.js are upstream GSD references.

CRITICAL: Exclude docs/GRD-Rename-Spec.md and docs/GRD-v1.2-Research-Reorientation-Spec.md and docs/superpowers/ from replacement -- these are spec documents that naturally reference old names.

Use `find` + `sed -i ''` (macOS) for each pass, or use the Edit tool to make targeted replacements file-by-file. The find command for each pass:
```bash
find . -type f \( -name "*.md" -o -name "*.js" -o -name "*.cjs" -o -name "*.json" \) \
  ! -path "./.planning/*" ! -path "./node_modules/*" ! -name "package-lock.json" \
  ! -path "./docs/GRD-Rename-Spec.md" ! -path "./docs/GRD-v1.2-Research-Reorientation-Spec.md" \
  ! -path "./docs/superpowers/*" \
  -exec sed -i '' 's|FIND|REPLACE|g' {} +
```

Replace FIND/REPLACE for each pass with the patterns above.

**PART B: install.js internal renames (D-01 Step 5, REN-05)**

These are NOT caught by the 6 bulk passes because they use `GSD_` prefix (no hyphen-R):

In bin/install.js, make these explicit replacements:
- `GSD_CODEX_MARKER` -> `GRD_CODEX_MARKER` (variable name, ~6 occurrences at lines 17, 846, 865, 912, 3023)
- `GSD_COPILOT_INSTRUCTIONS_MARKER` -> `GRD_COPILOT_INSTRUCTIONS_MARKER` (variable name, ~5 occurrences at lines 20, 944, 955, 3034)
- `GSD_COPILOT_INSTRUCTIONS_CLOSE_MARKER` -> `GRD_COPILOT_INSTRUCTIONS_CLOSE_MARKER` (variable name, ~5 occurrences at lines 21, 946, 956, 961, 984, 988, 3035)
- `'# GSD Agent Configuration'` -> `'# GRD Agent Configuration'` (string literal at line 17)
- `'<!-- GSD Configuration'` -> `'<!-- GRD Configuration'` (string literal at line 20)
- `'<!-- /GSD Configuration -->'` -> `'<!-- /GRD Configuration -->'` (string literal at line 21)
- `codexGsdPath` -> `codexGrdPath` (variable name at line 1011 and references at lines 1016-1017)

IMPORTANT per D-09: Leave ALL bare `get-shit-done` references (without `-r`) intact. These ~26 instances handle upstream GSD coexistence.

**PART C: Config file updates (D-01 Step 6, REN-06)**

**.gitignore** (per D-10): Update lines 33-36:
- `.github/agents/gsd-*` -> `.github/agents/grd-*`
- `.github/skills/gsd-*` -> `.github/skills/grd-*`
- `.github/get-shit-done/*` -> `.github/grd/*`
- `.github/skills/get-shit-done` -> `.github/skills/grd`

**package.json** (per D-12): In the `files` array, replace `"get-research-done"` with `"grd"`. The npm package name stays `get-research-done`.

**.claude/settings.local.json** (per spec 1.5): This file is gitignored. Update for local correctness if it exists:
- `get-shit-done-r/bin/lib/*.cjs` -> `grd/bin/lib/*.cjs`
- `get-shit-done-r/workflows/*.md` -> `grd/workflows/*.md`
(Note: these may have already been caught by bulk Pass 3.)

Run `npm test` after this task per D-03.
  </action>
  <verify>
    <automated>grep -r "gsd-r" --include="*.md" --include="*.js" --include="*.cjs" --include="*.json" . | grep -v node_modules | grep -v .planning | grep -v package-lock | grep -v "GRD-Rename-Spec" | grep -v "GRD-v1.2" | grep -v "docs/superpowers" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "gsd-r" --include="*.md" --include="*.js" --include="*.cjs" --include="*.json" . | grep -v node_modules | grep -v .planning | grep -v package-lock | grep -v "GRD-Rename-Spec" | grep -v "GRD-v1.2" | grep -v "docs/superpowers"` returns zero lines
    - `grep -r "GSD-R" --include="*.md" --include="*.js" --include="*.cjs" --include="*.json" . | grep -v node_modules | grep -v .planning | grep -v package-lock | grep -v "GRD-Rename-Spec" | grep -v "GRD-v1.2" | grep -v "docs/superpowers"` returns zero lines
    - `grep -r "get-shit-done-r" --include="*.md" --include="*.js" --include="*.cjs" --include="*.json" . | grep -v node_modules | grep -v .planning | grep -v package-lock | grep -v "GRD-Rename-Spec" | grep -v "GRD-v1.2" | grep -v "docs/superpowers"` returns zero lines
    - `grep "GRD_CODEX_MARKER" bin/install.js` returns matches
    - `grep "GSD_CODEX_MARKER" bin/install.js` returns zero results
    - `grep "codexGrdPath" bin/install.js` returns matches
    - `grep "codexGsdPath" bin/install.js` returns zero results
    - `grep '"grd"' package.json` returns a match (in files array)
    - `grep '"get-research-done"' package.json | grep -v '"name"'` returns zero results (not in files array)
    - `grep "grd-\*" .gitignore` returns matches
    - `grep "gsd-\*" .gitignore` returns zero results
  </acceptance_criteria>
  <done>Zero instances of gsd-r/GSD-R/get-shit-done-r in active files (excluding spec docs and .planning/). install.js uses GRD_ marker constants. package.json files array has "grd". .gitignore uses grd-* patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Fix namespace test and verify all tests pass</name>
  <files>
    test/namespace.test.cjs
    test/state.test.cjs
  </files>
  <read_first>
    test/namespace.test.cjs
    test/state.test.cjs
  </read_first>
  <action>
**Fix pre-existing namespace test failure (per RESEARCH open question 1):**

In test/namespace.test.cjs, the subtest "no old long path in .planning/ tree" (starting at line 78) has an exclusion filter that allows milestones/ and SUMMARY/VERIFICATION files but not REQUIREMENTS.md, ROADMAP.md, or *-CONTEXT.md files. These planning files naturally contain references to `get-shit-done-r` as historical records.

Update the `filtered` variable's filter function (around line 81-85) to also exclude:
- Files ending with `REQUIREMENTS.md`
- Files ending with `ROADMAP.md`
- Files matching `*-CONTEXT.md`
- Files matching `*-RESEARCH.md`
- Files matching `*-DISCUSSION-LOG.md`
- Files matching `*-VALIDATION.md`
- Files matching `config.json` in .planning/

The current filter:
```javascript
const filtered = hits.filter(h =>
  !h.startsWith('.planning/milestones/') &&
  !h.endsWith('-SUMMARY.md') &&
  !h.endsWith('-VERIFICATION.md')
);
```

Update to:
```javascript
const filtered = hits.filter(h =>
  !h.startsWith('.planning/milestones/') &&
  !h.endsWith('-SUMMARY.md') &&
  !h.endsWith('-VERIFICATION.md') &&
  !h.endsWith('REQUIREMENTS.md') &&
  !h.endsWith('ROADMAP.md') &&
  !h.endsWith('-CONTEXT.md') &&
  !h.endsWith('-RESEARCH.md') &&
  !h.endsWith('-DISCUSSION-LOG.md') &&
  !h.endsWith('-VALIDATION.md') &&
  !h.endsWith('config.json')
);
```

**Update state.test.cjs (per REN-07):**

Check test/state.test.cjs for any remaining `gsd_state_version` references. If bulk Pass 5 already converted them, verify. If any remain, replace `gsd_state_version` with `grd_state_version`.

Run `npm test` after both updates per D-03.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with zero failures
    - `grep "REQUIREMENTS.md" test/namespace.test.cjs` returns a match (exclusion is present)
    - `grep "ROADMAP.md" test/namespace.test.cjs` returns a match (exclusion is present)
    - `grep "CONTEXT.md" test/namespace.test.cjs` returns a match (exclusion is present)
    - `grep "gsd_state_version" test/state.test.cjs` returns zero results (if it existed before)
  </acceptance_criteria>
  <done>namespace.test.cjs exclusion list expanded to cover all .planning/ artifact types. state.test.cjs uses grd_state_version. npm test passes with zero failures.</done>
</task>

</tasks>

<verification>
- Three grep commands from spec section 1.8 all return zero results (excluding spec docs, .planning/, node_modules/, package-lock.json)
- `npm test` passes with zero failures
- install.js uses GRD_ prefix for all marker constants
- package.json files array contains "grd"
- .gitignore uses grd-* patterns
</verification>

<success_criteria>
All GSD-R content references eliminated from active files. install.js internals updated. Config files corrected. All tests pass with zero failures.
</success_criteria>

<output>
After completion, create `.planning/phases/26-rename-gsd-r-to-grd/26-02-SUMMARY.md`
</output>

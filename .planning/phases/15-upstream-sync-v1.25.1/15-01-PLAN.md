---
phase: 15-upstream-sync-v1.25.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done-r/bin/lib/core.cjs
  - get-shit-done-r/bin/lib/config.cjs
  - get-shit-done-r/bin/gsd-r-tools.cjs
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "execGit uses spawnSync instead of execSync with manual shell escaping"
    - "VALID_CONFIG_KEYS set exists and includes both upstream keys and GSD-R keys (vault_path, commit_research)"
    - "cmdConfigSetModelProfile function exists in config.cjs and is wired in gsd-r-tools.cjs (replacing the stub)"
    - "resolveModelInternal no longer maps opus to inherit"
  artifacts:
    - path: "get-shit-done-r/bin/lib/core.cjs"
      provides: "execGit with spawnSync, stripShippedMilestones, getMilestonePhaseFilter, replaceInCurrentMilestone"
      contains: "spawnSync"
    - path: "get-shit-done-r/bin/lib/config.cjs"
      provides: "VALID_CONFIG_KEYS, ensureConfigFile, setConfigValue, cmdConfigSetModelProfile, validateKnownConfigKeyPath"
      exports: ["VALID_CONFIG_KEYS", "ensureConfigFile", "setConfigValue", "cmdConfigSet", "cmdConfigSetModelProfile", "cmdConfigEnsureSection"]
    - path: "get-shit-done-r/bin/gsd-r-tools.cjs"
      provides: "CLI entry point with config-set-model-profile wired to real implementation"
      contains: "cmdConfigSetModelProfile"
  key_links:
    - from: "get-shit-done-r/bin/lib/config.cjs"
      to: "get-shit-done-r/bin/lib/model-profiles.cjs"
      via: "require for cmdConfigSetModelProfile"
      pattern: "require.*model-profiles"
    - from: "get-shit-done-r/bin/gsd-r-tools.cjs"
      to: "get-shit-done-r/bin/lib/config.cjs"
      via: "cmdConfigSetModelProfile import and routing"
      pattern: "cmdConfigSetModelProfile"
---

<objective>
Sync core.cjs and config.cjs with GSD v1.25.1 upstream, then wire the new config-set-model-profile command in the CLI entry point. These are the foundation modules that all other CJS modules depend on.

Purpose: Establish the v1.25.1 baseline for core utilities (execGit with spawnSync, milestone scoping functions) and config management (key validation, model profile setting) that downstream modules will use.
Output: Three synced files with all upstream v1.25.1 improvements and GSD-R research extensions preserved.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-upstream-sync-v1.25.1/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync core.cjs with v1.25.1 upstream</name>
  <files>get-shit-done-r/bin/lib/core.cjs</files>
  <read_first>
    - get-shit-done-r/bin/lib/core.cjs (current fork version)
    - ~/.claude/get-shit-done/bin/lib/core.cjs (upstream v1.25.1 source of truth)
  </read_first>
  <action>
Diff-and-apply upstream v1.25.1 changes to core.cjs:

1. **Replace execGit implementation** — Replace the `execSync` + manual shell escaping (8 lines of escape logic) with upstream's `spawnSync('git', args, ...)` approach. Add `const { spawnSync } = require('child_process');` to the require destructuring at the top. The return shape `{ exitCode, stdout, stderr }` stays identical. Remove the try/catch wrapper around execSync.

2. **Remove opus-to-inherit mapping in resolveModelInternal** — The fork maps `'opus'` to `'inherit'` in two places (around lines 385 and 393-394). Upstream returns the value directly. Remove both mapping blocks so `opus` passes through as-is.

3. **Verify stripShippedMilestones and getMilestonePhaseFilter exist** — These functions should already exist from v1.1 sync. Confirm they match upstream. If not, adopt upstream versions.

4. **Verify replaceInCurrentMilestone exists and is exported** — This function IS used by upstream's phase.cjs and roadmap.cjs. After syncing those files (Plan 02/03), it will be called. Confirm it exists and is in module.exports. Tech debt item #5 resolves itself — it's not actually dead code.

5. **Preserve all existing exports** — Do not remove any functions from module.exports.
  </action>
  <verify>
    <automated>node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - core.cjs contains `spawnSync` in the require destructuring (grep: `spawnSync.*require`)
    - core.cjs does NOT contain `execSync` (except possibly in comments)
    - core.cjs does NOT contain the string `'inherit'` in resolveModelInternal (no opus-to-inherit mapping)
    - core.cjs exports `stripShippedMilestones` (grep: `stripShippedMilestones`)
    - core.cjs exports `getMilestonePhaseFilter` (grep: `getMilestonePhaseFilter`)
    - core.cjs exports `replaceInCurrentMilestone` (grep: `replaceInCurrentMilestone`)
    - All 164+ tests pass: `node --test test/*.test.cjs` exits 0
  </acceptance_criteria>
  <done>core.cjs uses spawnSync for git operations, opus passes through without mapping, all milestone scoping functions exported, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Sync config.cjs with v1.25.1 upstream and wire in gsd-r-tools.cjs</name>
  <files>get-shit-done-r/bin/lib/config.cjs, get-shit-done-r/bin/gsd-r-tools.cjs</files>
  <read_first>
    - get-shit-done-r/bin/lib/config.cjs (current fork — 169 lines)
    - ~/.claude/get-shit-done/bin/lib/config.cjs (upstream v1.25.1 — 307 lines, source of truth)
    - get-shit-done-r/bin/gsd-r-tools.cjs (current fork CLI entry point)
    - ~/.claude/get-shit-done/bin/gsd-tools.cjs (upstream entry point for routing reference)
    - get-shit-done-r/bin/lib/model-profiles.cjs (needed by cmdConfigSetModelProfile)
  </read_first>
  <action>
This is effectively a full rewrite of config.cjs to match upstream structure, then extend with GSD-R keys. Resolves tech debt item #2 (config-set-model-profile stub).

**config.cjs changes:**

1. **Adopt VALID_CONFIG_KEYS Set** — Copy the upstream Set definition. Then add GSD-R keys: `vault_path` and `commit_research`. The set should include all upstream keys plus these two.

2. **Adopt CONFIG_KEY_SUGGESTIONS map** — Copy the upstream typo correction map as-is.

3. **Adopt validateKnownConfigKeyPath() function** — Copy from upstream. This is called before cmdConfigSet to warn on unknown keys.

4. **Adopt ensureConfigFile() extracted helper** — Upstream splits the fork's `cmdConfigEnsureSection` into `ensureConfigFile()` (reusable, no exit/output) and `cmdConfigEnsureSection()` (exits with output). Adopt this separation.

5. **Adopt setConfigValue() extracted helper** — Same pattern: reusable without exit. Fork inlines this in `cmdConfigSet`. Adopt upstream's extraction.

6. **Adopt cmdConfigSetModelProfile() function** — New command that sets profile and shows agent-to-model mapping table. Requires `require('./model-profiles.cjs')` import. Copy from upstream.

7. **Adopt getCmdConfigSetModelProfileResultMessage() helper** — Formats the model profile result. Copy from upstream.

8. **Preserve GSD-R namespace** — Any `/gsd:` references in output messages should be `/gsd-r:`. Any `get-shit-done` path references should be `get-shit-done-r`.

9. **Update module.exports** — Export all new functions: `VALID_CONFIG_KEYS`, `ensureConfigFile`, `setConfigValue`, `cmdConfigSet`, `cmdConfigSetModelProfile`, `cmdConfigEnsureSection`, `validateKnownConfigKeyPath`, and any other upstream exports.

**gsd-r-tools.cjs changes:**

1. **Replace config-set-model-profile stub** — Find the stub (likely a placeholder that outputs a not-implemented message). Replace with a call to `cmdConfigSetModelProfile` imported from config.cjs.

2. **Import cmdConfigSetModelProfile** — Add to the config.cjs require destructuring.

3. **Sync any new command routing** — Compare upstream gsd-tools.cjs routing against fork's gsd-r-tools.cjs. Add any new command routes (e.g., for `do` or `note` commands if they have CLI counterparts).

4. **Preserve all GSD-R-specific command routing** — Keep: state add-note, state get-notes, state add-gap, state resolve-gap, state get-gaps, verify research-plan, bootstrap generate, and all other research-specific commands.
  </action>
  <verify>
    <automated>node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - config.cjs contains `VALID_CONFIG_KEYS` as a Set (grep: `VALID_CONFIG_KEYS.*new Set`)
    - config.cjs VALID_CONFIG_KEYS includes `vault_path` (grep: `vault_path`)
    - config.cjs VALID_CONFIG_KEYS includes `commit_research` (grep: `commit_research`)
    - config.cjs contains `function ensureConfigFile` (grep: `ensureConfigFile`)
    - config.cjs contains `function setConfigValue` or `const setConfigValue` (grep: `setConfigValue`)
    - config.cjs contains `function cmdConfigSetModelProfile` or `cmdConfigSetModelProfile` (grep: `cmdConfigSetModelProfile`)
    - config.cjs contains `validateKnownConfigKeyPath` (grep: `validateKnownConfigKeyPath`)
    - config.cjs contains `require.*model-profiles` (grep: `model-profiles`)
    - gsd-r-tools.cjs contains `cmdConfigSetModelProfile` import (grep: `cmdConfigSetModelProfile`)
    - gsd-r-tools.cjs does NOT contain a stub/placeholder for config-set-model-profile (no "not implemented" or "TODO" for this command)
    - gsd-r-tools.cjs preserves `cmdStateAddNote` routing (grep: `cmdStateAddNote`)
    - All 164+ tests pass: `node --test test/*.test.cjs` exits 0
  </acceptance_criteria>
  <done>config.cjs matches upstream v1.25.1 structure with GSD-R key extensions, cmdConfigSetModelProfile is fully implemented and wired in gsd-r-tools.cjs, config-set-model-profile stub eliminated, all tests pass</done>
</task>

</tasks>

<verification>
- `node --test test/*.test.cjs` — all 164+ tests pass
- `grep -c 'spawnSync' get-shit-done-r/bin/lib/core.cjs` returns >= 1
- `grep -c 'execSync' get-shit-done-r/bin/lib/core.cjs` returns 0 (no execSync usage)
- `grep -c 'VALID_CONFIG_KEYS' get-shit-done-r/bin/lib/config.cjs` returns >= 1
- `grep -c 'cmdConfigSetModelProfile' get-shit-done-r/bin/gsd-r-tools.cjs` returns >= 1
</verification>

<success_criteria>
- core.cjs uses spawnSync for all git operations (no execSync)
- core.cjs passes opus through without mapping to inherit
- config.cjs has VALID_CONFIG_KEYS including vault_path and commit_research
- config.cjs exports ensureConfigFile, setConfigValue, cmdConfigSetModelProfile
- gsd-r-tools.cjs routes config-set-model-profile to real implementation
- All 164+ existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-upstream-sync-v1.25.1/15-01-SUMMARY.md`
</output>

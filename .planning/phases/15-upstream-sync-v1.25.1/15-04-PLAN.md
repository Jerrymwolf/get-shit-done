---
phase: 15-upstream-sync-v1.25.1
plan: 04
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - grd/workflows/discuss-phase.md
  - grd/workflows/plan-phase.md
  - grd/workflows/new-project.md
  - grd/workflows/quick.md
  - grd/workflows/help.md
autonomous: true
requirements: [SYNC-03]

must_haves:
  truths:
    - "discuss-phase.md preserves research vocabulary (principal investigator, research assistant, etc.) while incorporating v1.25.1 structural improvements"
    - "plan-phase.md preserves research-specific planning flow while incorporating v1.25.1 improvements"
    - "new-project.md preserves research project setup while incorporating v1.25.1 improvements"
    - "quick.md uses YYMMDD-xxx quick ID format matching the commands.cjs change"
    - "help.md lists new upstream commands (do, note) alongside research commands"
    - "No stale Skill() namespace calls remain (tech debt #4 fully resolved)"
  artifacts:
    - path: "grd/workflows/discuss-phase.md"
      provides: "Research-adapted discussion workflow with v1.25.1 structural improvements"
      contains: "principal investigator"
    - path: "grd/workflows/plan-phase.md"
      provides: "Research-adapted planning workflow with v1.25.1 improvements"
      contains: "grd"
    - path: "grd/workflows/quick.md"
      provides: "Quick task workflow with YYMMDD-xxx ID format"
      contains: "YYMMDD"
    - path: "grd/workflows/help.md"
      provides: "Help with all commands including do, note, and research commands"
      contains: "/grd:do"
  key_links:
    - from: "grd/workflows/quick.md"
      to: "grd/bin/lib/commands.cjs"
      via: "YYMMDD-xxx quick ID format consistency"
      pattern: "YYMMDD"
    - from: "grd/workflows/discuss-phase.md"
      to: "grd/workflows/plan-phase.md"
      via: "Discussion feeds into planning"
      pattern: "grd:plan-phase"
---

<objective>
Carefully merge the 5 large-divergence workflow files where GRD has deep research customizations and upstream has significant structural improvements. These require case-by-case merge, not simple namespace replacement.

Purpose: Incorporate v1.25.1 structural improvements into the most customized workflow files without losing the research-specific vocabulary, flow, and features that make GRD a research tool.
Output: 5 carefully merged workflow files, tech debt #4 fully resolved.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-upstream-sync-v1.25.1/15-RESEARCH.md
@.planning/phases/15-upstream-sync-v1.25.1/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge discuss-phase.md and plan-phase.md with v1.25.1</name>
  <files>grd/workflows/discuss-phase.md, grd/workflows/plan-phase.md</files>
  <read_first>
    - grd/workflows/discuss-phase.md (fork — has deep research vocabulary: principal investigator, research assistant, inquiry vocabulary)
    - ~/.claude/get-shit-done/workflows/discuss-phase.md (upstream — 453 diff lines, ~283 functional)
    - grd/workflows/plan-phase.md (fork — has research-specific planning flow)
    - ~/.claude/get-shit-done/workflows/plan-phase.md (upstream — 230 diff lines, ~123 functional)
  </read_first>
  <action>
These are the two most complex merges. Both files have deep research customizations (vocabulary, flow, agent behavior) AND significant upstream structural improvements.

**discuss-phase.md merge strategy:**
1. Start with upstream v1.25.1 version (gets all structural improvements)
2. Apply GRD namespace: `/gsd:` -> `/grd:`, paths, agent names (`grd-*`)
3. Re-apply research vocabulary substitutions from fork:
   - "principal investigator" terminology where upstream says "user" or "developer" in the context of project ownership
   - "research assistant" terminology where upstream says "Claude" in the context of agent role
   - Research-specific discussion framing (inquiry, investigation, etc.)
   - Any research-specific sections or decision types the fork added
4. **Tech debt #4:** Fix stale `Skill()` call at line ~682 — ensure it uses `Skill(skill="grd:...")`  not `Skill(skill="gsd:...")`
5. Verify no upstream structural improvements were lost during vocabulary merge

**plan-phase.md merge strategy:**
1. Start with upstream v1.25.1 version
2. Apply GRD namespace
3. Re-apply research-specific planning adaptations from fork:
   - Research-specific task types or planning vocabulary
   - Any research-adapted sections
4. **Tech debt #4:** Fix stale `Skill()` call at line ~529 — ensure it uses `grd:` namespace
5. Verify no upstream planning improvements were lost

**Merge validation for both files:**
After merging, scan each file for:
- No bare `/gsd:` references (must be `/grd:`)
- No bare `gsd-tools.cjs` (must be `grd-tools.cjs`)
- No `Skill(skill="gsd:` (must be `grd:`)
- Research vocabulary still present
  </action>
  <verify>
    <automated>node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - discuss-phase.md contains research vocabulary (grep: `principal investigator\|research assistant\|inquiry`)
    - discuss-phase.md does NOT contain `Skill(skill="gsd:` (must be `grd:`)
    - discuss-phase.md contains `/grd:` references (not bare `/gsd:`)
    - plan-phase.md does NOT contain `Skill(skill="gsd:` (must be `grd:`)
    - plan-phase.md contains `/grd:` references (not bare `/gsd:`)
    - Both files reflect upstream v1.25.1 structural improvements (file sizes should be larger than or comparable to upstream after merge)
    - All 164+ tests pass: `node --test test/*.test.cjs` exits 0
  </acceptance_criteria>
  <done>discuss-phase.md and plan-phase.md merged with v1.25.1 structural improvements while preserving all research vocabulary and adaptations, tech debt #4 fully resolved for both files</done>
</task>

<task type="auto">
  <name>Task 2: Merge new-project.md, quick.md, and help.md with v1.25.1</name>
  <files>grd/workflows/new-project.md, grd/workflows/quick.md, grd/workflows/help.md</files>
  <read_first>
    - grd/workflows/new-project.md (fork — has research-specific project setup)
    - ~/.claude/get-shit-done/workflows/new-project.md (upstream — 304 diff lines, ~152 functional)
    - grd/workflows/quick.md (fork — has sequential IDs)
    - ~/.claude/get-shit-done/workflows/quick.md (upstream — 235 diff lines, ~122 functional, YYMMDD-xxx format)
    - grd/workflows/help.md (fork — has research commands)
    - ~/.claude/get-shit-done/workflows/help.md (upstream — 371 diff lines, ~122 functional, has do+note commands)
  </read_first>
  <action>
**new-project.md merge strategy:**
1. Start with upstream v1.25.1 version
2. Apply GRD namespace
3. Re-apply research-specific project setup from fork:
   - Research project initialization flow
   - Research-specific project template references
   - Any research vocabulary in project creation prompts
4. Verify upstream project creation improvements are preserved

**quick.md merge strategy:**
1. Start with upstream v1.25.1 version (has YYMMDD-xxx quick ID format)
2. Apply GRD namespace
3. The YYMMDD-xxx format change is the key functional improvement — adopt fully
4. Remove any references to sequential `next_num` format
5. Ensure quick task references match the commands.cjs YYMMDD format synced in Plan 02
6. Preserve any research-specific quick task adaptations from fork (if any)

**help.md merge strategy:**
1. Start with upstream v1.25.1 version (has new `do` and `note` commands)
2. Apply GRD namespace: all commands become `/grd:do`, `/grd:note`, etc.
3. Re-add research-specific commands from fork that upstream doesn't have:
   - Research commands (new-research, verify research, etc.)
   - Any research-specific help text
4. Final command list should include ALL upstream commands (with `/grd:` prefix) PLUS all GRD research-specific commands

**Namespace validation for all 3 files:**
- No bare `/gsd:` references
- No bare `gsd-tools.cjs`
- No bare `get-shit-done/` paths (must be `grd/`)
  </action>
  <verify>
    <automated>node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - new-project.md contains research-specific project vocabulary (grep for research-related terms)
    - new-project.md contains `/grd:` references (not bare `/gsd:`)
    - quick.md contains YYMMDD or date-based ID format references (grep: `YYMMDD\|date\|timestamp`)
    - quick.md does NOT contain `next_num` or sequential ID references
    - help.md contains `/grd:do` (new upstream command with namespace)
    - help.md contains `/grd:note` (new upstream command with namespace)
    - help.md contains research-specific commands (grep for research command names)
    - No file contains bare `/gsd:` without `-r` suffix
    - All 164+ tests pass: `node --test test/*.test.cjs` exits 0
  </acceptance_criteria>
  <done>new-project.md, quick.md, and help.md merged with v1.25.1, quick IDs use YYMMDD-xxx format, help includes do+note commands, all research adaptations preserved</done>
</task>

</tasks>

<verification>
- `node --test test/*.test.cjs` — all 164+ tests pass
- `grep -r 'Skill(skill="gsd:' grd/workflows/ --include='*.md'` returns empty (no stale Skill() calls — tech debt #4 fully resolved)
- `grep -c 'principal investigator\|research assistant' grd/workflows/discuss-phase.md` returns >= 1 (research vocabulary preserved)
- `grep '/grd:do\|/grd:note' grd/workflows/help.md` confirms new commands listed
</verification>

<success_criteria>
- All 5 large-merge workflows synced with v1.25.1 improvements
- Research vocabulary preserved in discuss-phase.md
- Research planning flow preserved in plan-phase.md
- Research project setup preserved in new-project.md
- Quick IDs use YYMMDD-xxx format in quick.md
- help.md lists all upstream + research commands
- Tech debt #4 fully resolved (no stale Skill() namespace calls in any workflow)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-upstream-sync-v1.25.1/15-04-SUMMARY.md`
</output>

---
phase: 15-upstream-sync-v1.25.1
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - get-shit-done-r/bin/lib/commands.cjs
  - get-shit-done-r/bin/lib/init.cjs
  - get-shit-done-r/bin/lib/phase.cjs
  - get-shit-done-r/bin/lib/verify.cjs
  - get-shit-done-r/bin/lib/roadmap.cjs
  - get-shit-done-r/bin/lib/model-profiles.cjs
  - get-shit-done-r/bin/lib/state.cjs
autonomous: true
requirements: [SYNC-01, TEST-01]

must_haves:
  truths:
    - "All 12 shared CJS modules reflect v1.25.1 upstream changes"
    - "Milestone scoping (stripShippedMilestones, getMilestonePhaseFilter) is used in commands, init, phase, verify, and roadmap modules"
    - "Quick task IDs use YYMMDD-xxx format instead of sequential integers"
    - "Phase discovery merges ROADMAP headings with disk directories"
    - "All 164+ tests pass after CJS module sync is complete"
  artifacts:
    - path: "get-shit-done-r/bin/lib/commands.cjs"
      provides: "ROADMAP-driven phase discovery, YYMMDD quick IDs, milestone phase filter"
      contains: "getMilestonePhaseFilter"
    - path: "get-shit-done-r/bin/lib/init.cjs"
      provides: "Milestone-scoped stats, ROADMAP-only phase support"
      contains: "stripShippedMilestones"
    - path: "get-shit-done-r/bin/lib/phase.cjs"
      provides: "Milestone-scoped phase operations, improved regex patterns"
      contains: "replaceInCurrentMilestone"
    - path: "get-shit-done-r/bin/lib/verify.cjs"
      provides: "Home directory safety guard, inherit profile validation, improved config defaults"
      contains: "os.homedir"
    - path: "get-shit-done-r/bin/lib/roadmap.cjs"
      provides: "Milestone-scoped roadmap operations"
      contains: "stripShippedMilestones"
    - path: "get-shit-done-r/bin/lib/state.cjs"
      provides: "State management with research extensions preserved"
      contains: "gsd_r_state_version"
  key_links:
    - from: "get-shit-done-r/bin/lib/commands.cjs"
      to: "get-shit-done-r/bin/lib/core.cjs"
      via: "getMilestonePhaseFilter import"
      pattern: "getMilestonePhaseFilter.*require.*core"
    - from: "get-shit-done-r/bin/lib/phase.cjs"
      to: "get-shit-done-r/bin/lib/core.cjs"
      via: "replaceInCurrentMilestone import"
      pattern: "replaceInCurrentMilestone.*require.*core"
---

<objective>
Sync the remaining 7 CJS modules with GSD v1.25.1 upstream. This completes SYNC-01 (all core CJS modules synced) and runs the first test gate (TEST-01).

Purpose: All CJS modules now use milestone scoping, improved regex patterns, ROADMAP-driven phase discovery, and other v1.25.1 improvements while preserving all GSD-R research extensions and namespace.
Output: Seven synced module files, all 164+ tests passing.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-upstream-sync-v1.25.1/15-RESEARCH.md
@.planning/phases/15-upstream-sync-v1.25.1/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync commands.cjs, init.cjs, phase.cjs, and verify.cjs with v1.25.1</name>
  <files>get-shit-done-r/bin/lib/commands.cjs, get-shit-done-r/bin/lib/init.cjs, get-shit-done-r/bin/lib/phase.cjs, get-shit-done-r/bin/lib/verify.cjs</files>
  <read_first>
    - get-shit-done-r/bin/lib/commands.cjs (fork)
    - ~/.claude/get-shit-done/bin/lib/commands.cjs (upstream)
    - get-shit-done-r/bin/lib/init.cjs (fork)
    - ~/.claude/get-shit-done/bin/lib/init.cjs (upstream)
    - get-shit-done-r/bin/lib/phase.cjs (fork)
    - ~/.claude/get-shit-done/bin/lib/phase.cjs (upstream)
    - get-shit-done-r/bin/lib/verify.cjs (fork)
    - ~/.claude/get-shit-done/bin/lib/verify.cjs (upstream)
  </read_first>
  <action>
For each of these 4 MEDIUM-priority modules, diff-and-apply upstream v1.25.1 changes while preserving GSD-R namespace (`gsd-r-*` agent names, `/gsd-r:` command references).

**commands.cjs changes:**
1. Add `getMilestonePhaseFilter` to the `require('./core.cjs')` destructuring
2. Add `stripShippedMilestones` to the core.cjs require if not already there
3. Adopt ROADMAP-driven phase discovery — upstream reads ROADMAP.md headings via `stripShippedMilestones()` to build a phase-by-number map, then merges disk directories on top
4. Replace sequential quick task ID (`nextNum`) with YYMMDD-xxx format (timestamp-based, collision-resistant)
5. Adopt archived phase handling in discuss-phase init — check `phaseInfo?.archived`
6. Replace `.padStart(2, '0')` with `normalizePhaseName()` calls
7. Update phase number regex from `\d+` to `\d+[A-Z]?` for letter suffix support
8. Adopt split `plan_percent` / `percent` progress calculation
9. Adopt improved `last_activity` regex (4 patterns instead of 1)
10. Adopt upstream git commit stats approach (`rev-list --count`)
11. Preserve all `gsd-r-*` agent name prefixes and `/gsd-r:` command references

**init.cjs changes:**
1. Add `getMilestonePhaseFilter` and `stripShippedMilestones` to core.cjs require destructuring
2. Adopt ROADMAP-only phase support in `cmdInitStats`
3. Replace `.padStart(2, '0')` with `normalizePhaseName()` calls
4. Update phase regex to `\d+[A-Z]?(?:\.\d+)*`
5. Change status label from `'Pending'` to `'Not Started'`
6. Preserve `gsd-r-*` agent names and `/gsd-r:discuss-phase` references

**phase.cjs changes:**
1. Add `stripShippedMilestones`, `replaceInCurrentMilestone`, `getMilestonePhaseFilter` to core.cjs require destructuring
2. Adopt milestone-scoped phase operations (add, insert, complete use replaceInCurrentMilestone)
3. Adopt flexible goal regex: support both `**Goal:**` and `**Goal**:` formats
4. Adopt flexible depends-on regex
5. Adopt more specific checkbox pattern: `Phase N:` or `Phase N ` (not bare `Phase N`)
6. Adopt roadmap completion checkbox trust
7. Adopt `requirementsUpdated` tracking and `requirements_updated` in result
8. Adopt `Pending|In Progress` matching in traceability table (not just `Pending`)
9. Adopt `escapeRegex` + scoped section matching for requirements extraction
10. Preserve `/gsd-r:plan-phase` references

**verify.cjs changes:**
1. Add `const os = require('os');` import
2. Adopt home directory guard — detect when CWD is `os.homedir()` and error with E010
3. Add `stripShippedMilestones` to core.cjs require destructuring
4. Add `'inherit'` to valid profiles list: `['quality', 'balanced', 'budget', 'inherit']`
5. Adopt improved config repair defaults (including `phase_branch_template`, `milestone_branch_template`, nested `workflow` object, `brave_search`)
6. Preserve all `/gsd-r:` command references in error messages
  </action>
  <verify>
    <automated>node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - commands.cjs contains `getMilestonePhaseFilter` (grep confirms)
    - commands.cjs contains `YYMMDD` or date-based quick ID format (grep: `toISOString\|getFullYear\|YYMMDD\|yymmdd\|dateStr\|padStart.*2.*0`)
    - commands.cjs does NOT contain `nextNum` for quick task IDs
    - init.cjs contains `stripShippedMilestones` (grep confirms)
    - init.cjs contains `'Not Started'` (grep confirms, not `'Pending'`)
    - phase.cjs contains `replaceInCurrentMilestone` (grep confirms)
    - phase.cjs contains `requirements_updated` or `requirementsUpdated` (grep confirms)
    - verify.cjs contains `os.homedir` or `require('os')` (grep confirms)
    - verify.cjs contains `'inherit'` in valid profiles (grep: `inherit`)
    - All files preserve `gsd-r` namespace (grep each for `gsd-r`)
    - All 164+ tests pass: `node --test test/*.test.cjs` exits 0
  </acceptance_criteria>
  <done>All 4 MEDIUM-priority CJS modules synced with v1.25.1 milestone scoping, improved regex patterns, ROADMAP-driven discovery, and GSD-R namespace preserved</done>
</task>

<task type="auto">
  <name>Task 2: Sync roadmap.cjs, model-profiles.cjs, state.cjs and run test gate</name>
  <files>get-shit-done-r/bin/lib/roadmap.cjs, get-shit-done-r/bin/lib/model-profiles.cjs, get-shit-done-r/bin/lib/state.cjs</files>
  <read_first>
    - get-shit-done-r/bin/lib/roadmap.cjs (fork)
    - ~/.claude/get-shit-done/bin/lib/roadmap.cjs (upstream)
    - get-shit-done-r/bin/lib/model-profiles.cjs (fork)
    - ~/.claude/get-shit-done/bin/lib/model-profiles.cjs (upstream)
    - get-shit-done-r/bin/lib/state.cjs (fork — 979 lines with research extensions)
    - ~/.claude/get-shit-done/bin/lib/state.cjs (upstream — 723 lines)
  </read_first>
  <action>
Sync 3 LOW-priority modules. Clean up tech debt item #1.

**roadmap.cjs changes (straightforward, no research-specific changes):**
1. Add `stripShippedMilestones` and `replaceInCurrentMilestone` to core.cjs require destructuring
2. All roadmap read operations use `stripShippedMilestones` to strip shipped milestones before processing
3. Roadmap write operations (update plan count, checkbox) use `replaceInCurrentMilestone` instead of `.replace()`
4. Adopt flexible goal regex: `**Goal:**` and `**Goal**:` formats
5. Adopt flexible depends-on regex

**model-profiles.cjs changes (minimal):**
1. Replace Unicode escape sequences (`\u2500`, `\u2502`, `\u253C`) with literal Unicode characters (`─`, `│`, `┼`) for table drawing — match upstream for readability
2. Keep `gsd-r-*` agent name prefix (do NOT adopt upstream's `gsd-*` prefix)
3. Keep all 4 research-only agents: `gsd-r-source-researcher`, `gsd-r-methods-researcher`, `gsd-r-architecture-researcher`, `gsd-r-limitations-researcher`
4. Keep fork comment text "GSD-R agent to model" (not "GSD agent to model")

**state.cjs changes (minimal + tech debt):**
1. **Tech debt item #1:** Remove duplicate `stateExtractField` dead code at lines 12-19 (duplicate of the real implementation at lines ~184-193). The upstream has the same duplication — we clean it up in our fork.
2. Keep `gsd_r_state_version` key name (do NOT adopt upstream's `gsd_state_version`)
3. Preserve ALL research-specific functions: ensureStateSections, cmdStateAddNote, cmdStateUpdateNoteStatus, cmdStateGetNotes, cmdStateAddGap, cmdStateResolveGap, cmdStateGetGaps, parseTableSection, rebuildTableSection
4. Preserve all research-specific exports in module.exports
5. Verify the shared ~703 lines haven't diverged beyond the version key name

**After all 3 files synced, run full test suite as the Group 1 test gate.**
  </action>
  <verify>
    <automated>node --test test/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - roadmap.cjs contains `stripShippedMilestones` (grep confirms)
    - roadmap.cjs contains `replaceInCurrentMilestone` (grep confirms)
    - model-profiles.cjs contains `gsd-r-` agent prefix (grep confirms)
    - model-profiles.cjs contains all 4 research agents (grep: `source-researcher.*methods-researcher.*architecture-researcher.*limitations-researcher` across file)
    - state.cjs does NOT have duplicate stateExtractField near line 12 (the function should appear only once, around line 184)
    - state.cjs contains `gsd_r_state_version` (grep confirms, NOT `gsd_state_version`)
    - state.cjs contains `cmdStateAddNote` (grep confirms research extensions preserved)
    - state.cjs contains `cmdStateGetGaps` (grep confirms research extensions preserved)
    - All 164+ tests pass: `node --test test/*.test.cjs` exits 0 — this is the CJS module group test gate
  </acceptance_criteria>
  <done>All 12 shared CJS modules synced with v1.25.1 upstream, tech debt item #1 cleaned up, all research extensions preserved, full test suite passes (CJS group test gate complete)</done>
</task>

</tasks>

<verification>
- `node --test test/*.test.cjs` — all 164+ tests pass (CJS group test gate)
- All 12 shared modules reflect v1.25.1 changes (verified by diff analysis in tasks)
- No upstream functionality lost (milestone scoping, config validation, improved regexes all present)
- All GSD-R research extensions preserved (namespace, research agents, state operations)
</verification>

<success_criteria>
- All 9 changed CJS modules + entry point synced with v1.25.1
- Milestone scoping functions used across commands, init, phase, verify, roadmap
- Quick IDs use YYMMDD-xxx format
- Tech debt items #1 and #5 resolved
- All 164+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-upstream-sync-v1.25.1/15-02-SUMMARY.md`
</output>

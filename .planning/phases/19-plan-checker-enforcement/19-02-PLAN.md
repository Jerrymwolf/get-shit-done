---
phase: 19-plan-checker-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - grd/workflows/plan-inquiry.md
autonomous: true
requirements: [TRAP-02, PLAN-01]

must_haves:
  truths:
    - "TRAP-02 gate fires after 3-iteration revision loop exhaustion when blocking errors remain"
    - "Gate presents 3 options: Downgrade review type / Send back to planner / Override and proceed"
    - "Downgrade option calls applySmartDefaults() to permanently change config.json"
    - "Override option is per-plan only -- next phase starts fresh"
    - "Warnings do not trigger the gate -- only blocking errors"
    - "Checker prompt receives plan_check_rigor and total_phases from init context"
    - "Checker prompt includes instructions for qualitative diversity assessment"
    - "Planner prompt includes search-strategy, criteria XML schemas and tier attribute documentation"
    - "Checker output supports VERIFICATION PASSED (with warnings) format"
  artifacts:
    - path: "grd/workflows/plan-inquiry.md"
      provides: "TRAP-02 gate, rigor-aware checker prompt, planner prompt with new XML schemas"
      contains: "CHECKPOINT: Review Type Mismatch"
  key_links:
    - from: "grd/workflows/plan-inquiry.md step 10"
      to: "grd/bin/lib/plan-checker-rules.cjs"
      via: "checker prompt passes rigorLevel and phaseNumber to validateResearchPlan"
      pattern: "plan_check_rigor"
    - from: "grd/workflows/plan-inquiry.md step 12"
      to: "grd/bin/lib/config.cjs"
      via: "downgrade option calls applySmartDefaults"
      pattern: "applySmartDefaults"
    - from: "grd/workflows/plan-inquiry.md step 8"
      to: "planner agent"
      via: "planner prompt documents search-strategy, criteria, tier schemas"
      pattern: "search-strategy"
---

<objective>
Update the plan-inquiry workflow: (1) pass rigor level and phase position to the checker agent prompt, (2) add qualitative diversity assessment instructions to the checker prompt, (3) add TRAP-02 interactive gate after revision loop exhaustion, (4) update planner prompt to document new XML blocks (search-strategy, criteria, tier attribute), and (5) support "VERIFICATION PASSED (with warnings)" checker output.

Purpose: Wire the CJS enforcement infrastructure (from plan 01) into the workflow, enable the planner to emit rigor-appropriate XML, and give users escape hatches when rigor requirements cannot be met.
Output: Updated plan-inquiry.md with rigor-aware checker, TRAP-02 gate, and planner schema documentation.
</objective>

<execution_context>
@/Users/jeremiahwolf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jeremiahwolf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-plan-checker-enforcement/19-CONTEXT.md
@.planning/phases/19-plan-checker-enforcement/19-RESEARCH.md
@.planning/phases/19-plan-checker-enforcement/19-01-SUMMARY.md

<interfaces>
<!-- From plan 01: new exports in plan-checker-rules.cjs -->
```javascript
const RIGOR_LEVELS = {
  strict:   { primary_source_ratio: 'error',   search_strategy: 'error',   criteria: 'error' },
  moderate: { primary_source_ratio: 'warning', search_strategy: 'error',   criteria: 'error' },
  light:    { primary_source_ratio: 'warning', search_strategy: 'warning', criteria: 'warning' },
};

function validateResearchPlan(planContent, bootstrapContent, options = {})
// options: { rigorLevel: 'strict'|'moderate'|'light', phaseNumber: number, totalPhases: number }
// returns: { valid: boolean, issues: string[], warnings: string[] }
```

<!-- From plan 01: new init fields -->
```json
{
  "plan_check_rigor": "strict|moderate|light",
  "total_phases": 8
}
```

<!-- From config.cjs: applySmartDefaults for TRAP-02 downgrade -->
```javascript
function applySmartDefaults(config, newReviewType)
// Resets the 4 smart-default-controlled workflow keys for newReviewType
// Does not mutate input — returns new config object
```

<!-- From config.cjs: REVIEW_TYPE_ORDER for computing downgrade target -->
```javascript
const REVIEW_TYPE_ORDER = ['systematic', 'scoping', 'integrative', 'critical', 'narrative'];
```

<!-- From plan-inquiry.md current structure: -->
- Step 8: Planner prompt (lines 386-458)
- Step 9: Handle planner return (line 469)
- Step 10: Checker prompt (lines 475-520)
- Step 11: Handle checker return (lines 522-525)
- Step 12: Revision loop max 3 iterations (lines 527-572) — currently offers "Force proceed / Provide guidance / Abandon" at iteration >= 3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update checker prompt with rigor context, planner prompt with XML schemas, and add TRAP-02 gate</name>
  <files>grd/workflows/plan-inquiry.md</files>
  <read_first>
    - grd/workflows/plan-inquiry.md (full file, 687 lines — focus on steps 8, 10, 11, 12)
    - grd/bin/lib/config.cjs (lines 38, 80-100 for REVIEW_TYPE_ORDER and applySmartDefaults signature)
    - .planning/phases/19-plan-checker-enforcement/19-CONTEXT.md (locked decisions for TRAP-02 behavior)
    - .planning/phases/19-plan-checker-enforcement/19-RESEARCH.md (lines 211-408 for TRAP-02 gate pattern, XML schemas, anti-patterns)
  </read_first>
  <action>
**A. Update Step 8 planner prompt (around line 386-458):**

Add a new `<research_rigor>` block inside the planner prompt template, after the `<quality_gate>` block and before the closing triple-backtick. This instructs the planner to emit the new XML blocks when the review type requires them:

```markdown
<research_rigor>
## Review-Type-Specific Plan Elements

Current review type rigor: **{plan_check_rigor}** (from config)

When creating research plans, include these elements based on rigor level:

### Source Tier Attributes
Add `tier="primary|secondary|tertiary"` to every `<src>` tag:
- **primary**: Original empirical studies, foundational theoretical works
- **secondary**: Review articles, meta-analyses, systematic reviews
- **tertiary**: Textbooks, encyclopedias, handbooks

Example: `<src method="firecrawl" format="md" tier="primary">https://doi.org/...</src>`

### Search Strategy Block (required at strict and moderate rigor)
Add at plan level (outside `<tasks>`):
```xml
<search-strategy>
  <databases>PubMed, PsycINFO, Web of Science, Google Scholar</databases>
  <keywords>self-determination theory AND values, intrinsic motivation AND personal values</keywords>
  <date-range>2000-2026</date-range>
</search-strategy>
```

### Inclusion/Exclusion Criteria Block (required at strict and moderate rigor)
Add at plan level (outside `<tasks>`):
```xml
<criteria>
  <include>
    - Peer-reviewed empirical studies
    - English language
    - Primary framework alignment
  </include>
  <exclude>
    - Non-peer-reviewed grey literature (unless seminal)
    - Studies without empirical data
  </exclude>
</criteria>
```

At **light** rigor (narrative reviews): these blocks are optional but recommended.
At **moderate** rigor (scoping/integrative/critical): search-strategy and criteria required; primary source ratio advisory.
At **strict** rigor (systematic): all elements required; primary source ratio >= 50% enforced.
</research_rigor>
```

**B. Update Step 10 checker prompt (around line 486-511):**

Add rigor context fields to the `<verification_context>` block, after the `**Phase requirement IDs**` line:

```markdown
**Plan check rigor:** {plan_check_rigor}
**Phase position:** {phase_number} of {total_phases}
```

Add a new `<rigor_instructions>` block after `</verification_context>` and before `<expected_output>`:

```markdown
<rigor_instructions>
## Review-Type Enforcement

Use the CJS module `plan-checker-rules.cjs` for structural validation. Call `validateResearchPlan(planContent, bootstrapContent, { rigorLevel: '{plan_check_rigor}', phaseNumber: {phase_number}, totalPhases: {total_phases} })`.

The CJS module handles these structural checks:
1. Source duplication (universal — always blocking)
2. Source limits / max 3 per task (universal — always blocking)
3. Acquisition methods (universal — always blocking)
4. Context budget / >30 page papers (universal — always blocking)
5. Primary source ratio (review-type-specific — graduated by phase position)
6. Search strategy block existence and fields (review-type-specific — graduated by phase position)
7. Inclusion/exclusion criteria block existence (review-type-specific — graduated by phase position)

**You** assess these qualitative checks (CJS cannot do these):
- **Disciplinary diversity** (for interdisciplinary topics at any review type): Are sources drawn from multiple disciplinary perspectives relevant to the research questions? Flag if all sources come from a single discipline when the topic spans multiple fields. Cite Repko & Szostak (2021).
- **Methodological diversity** (for integrative reviews): Do sources include varied methodologies (qualitative, quantitative, mixed methods)? Flag if all sources use the same methodology. Cite Whittemore & Knafl (2005).

These qualitative checks produce **warnings** (never blocking errors) — they require judgment about the research topic.

## Output Format

If all CJS checks pass AND no qualitative warnings:
```
## VERIFICATION PASSED
```

If all CJS checks pass BUT qualitative warnings exist OR CJS returns warnings:
```
## VERIFICATION PASSED (with warnings)

### Warnings
{list of warning messages from CJS warnings array and your qualitative assessment}
```

If CJS returns blocking issues:
```
## ISSUES FOUND

### Blocking Issues
{list from CJS issues array}

### Warnings (if any)
{list from CJS warnings array and qualitative assessment}
```
</rigor_instructions>
```

**C. Update Step 11 checker return handling (around line 522-525):**

Add a third case to handle the new warning format:

```markdown
- **`## VERIFICATION PASSED`:** Display confirmation, proceed to step 13.
- **`## VERIFICATION PASSED (with warnings)`:** Display confirmation with warnings. Warnings are advisory — proceed to step 13. Display: `Plan-checker passed with advisory warnings:` followed by the warning list.
- **`## ISSUES FOUND`:** Display issues, check iteration count, proceed to step 12.
```

**D. Replace Step 12 iteration >= 3 branch (around lines 568-572):**

Replace the current 3-option block:
```
Offer: 1) Force proceed, 2) Provide guidance and retry, 3) Abandon
```

With the TRAP-02 gate:

```markdown
**If iteration_count >= 3 AND blocking issues remain:**

Determine the next-lower review type for downgrade option:
- Read current review_type from init context
- Use REVIEW_TYPE_ORDER: ['systematic', 'scoping', 'integrative', 'critical', 'narrative']
- The downgrade target is the next entry in the array (e.g., systematic -> scoping, scoping -> integrative)
- If already at 'narrative' (lowest), omit the downgrade option

Display:

```
+------------------------------------------------------------------+
|  CHECKPOINT: Review Type Mismatch                                 |
+------------------------------------------------------------------+

The plan-checker found issues that could not be resolved after 3 attempts:
{remaining_blocking_issues}

Current review type: {review_type} (plan_check: {plan_check_rigor})

Options:
1. Downgrade review type to {next_lower_type} (relaxes rigor requirements permanently via applySmartDefaults)
2. Send back to planner with additional guidance (you provide specific instructions)
3. Override and proceed (this plan only; next phase starts fresh)

YOUR ACTION: Enter 1, 2, or 3
```

**Handling each option:**
- **Option 1 (Downgrade):** Run `node grd/bin/grd-tools.cjs config-set review_type {next_lower_type}` then `node grd/bin/grd-tools.cjs apply-smart-defaults {next_lower_type}`. Display: `Review type downgraded to {next_lower_type}. Rigor requirements updated.` Re-run checker with new rigor level (go to step 10 with updated plan_check_rigor).
- **Option 2 (Retry with guidance):** Prompt user for additional guidance text. Spawn planner with guidance appended to revision prompt. Reset iteration_count to 0. Go to step 10 after planner returns.
- **Option 3 (Override):** Display: `Override accepted for this plan. Proceeding with {N} unresolved issues.` Proceed to step 13.

**If iteration_count >= 3 AND only warnings remain (no blocking issues):**

This should not happen (warnings don't trigger revision loop), but as a safety net: proceed to step 13 with warnings displayed.
```
  </action>
  <verify>
    <automated>grep -q 'CHECKPOINT: Review Type Mismatch' grd/workflows/plan-inquiry.md && grep -q 'plan_check_rigor' grd/workflows/plan-inquiry.md && grep -q 'search-strategy' grd/workflows/plan-inquiry.md && grep -q 'VERIFICATION PASSED (with warnings)' grd/workflows/plan-inquiry.md && grep -q 'rigor_instructions' grd/workflows/plan-inquiry.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - plan-inquiry.md contains `<research_rigor>` block in step 8 planner prompt
    - plan-inquiry.md contains `tier="primary|secondary|tertiary"` in planner prompt
    - plan-inquiry.md contains `<search-strategy>` XML schema example in planner prompt
    - plan-inquiry.md contains `<criteria>` with `<include>` and `<exclude>` schema in planner prompt
    - plan-inquiry.md step 10 checker prompt contains `**Plan check rigor:** {plan_check_rigor}`
    - plan-inquiry.md step 10 checker prompt contains `**Phase position:** {phase_number} of {total_phases}`
    - plan-inquiry.md contains `<rigor_instructions>` block with CJS module call signature
    - plan-inquiry.md contains `validateResearchPlan(planContent, bootstrapContent, { rigorLevel:`
    - plan-inquiry.md step 10 contains `Disciplinary diversity` qualitative check instructions
    - plan-inquiry.md step 10 contains `Methodological diversity` qualitative check instructions
    - plan-inquiry.md step 10 contains `Repko & Szostak` citation
    - plan-inquiry.md step 10 contains `Whittemore & Knafl` citation
    - plan-inquiry.md step 11 contains `## VERIFICATION PASSED (with warnings)` handling
    - plan-inquiry.md step 12 contains `CHECKPOINT: Review Type Mismatch`
    - plan-inquiry.md step 12 contains 3 options: `Downgrade review type`, `Send back to planner`, `Override and proceed`
    - plan-inquiry.md step 12 contains `applySmartDefaults` or `apply-smart-defaults` for downgrade
    - plan-inquiry.md step 12 contains `REVIEW_TYPE_ORDER` or the ordered list for computing next-lower type
    - plan-inquiry.md step 12 does NOT contain old text `Force proceed` (replaced by TRAP-02 gate)
  </acceptance_criteria>
  <done>Plan-inquiry workflow passes rigor level and phase position to checker, checker prompt includes qualitative diversity assessment instructions, planner prompt documents search-strategy/criteria/tier schemas, step 11 handles VERIFICATION PASSED (with warnings), step 12 presents TRAP-02 gate with downgrade/retry/override options instead of old force/retry/abandon.</done>
</task>

</tasks>

<verification>
- `grep -c 'CHECKPOINT: Review Type Mismatch' grd/workflows/plan-inquiry.md` returns 1
- `grep -c 'plan_check_rigor' grd/workflows/plan-inquiry.md` returns >= 3 (checker context, rigor instructions, TRAP-02 gate)
- `grep -c 'search-strategy' grd/workflows/plan-inquiry.md` returns >= 2 (planner schema, checker validation)
- `grep -c 'VERIFICATION PASSED (with warnings)' grd/workflows/plan-inquiry.md` returns >= 1
- `grep -c 'Force proceed' grd/workflows/plan-inquiry.md` returns 0 (old option removed)
- `node grd/bin/grd-tools.cjs run-tests` exits 0 (no regressions from workflow changes)
</verification>

<success_criteria>
- Checker agent receives rigor level and phase position in its prompt context
- Checker agent has instructions for qualitative diversity assessment (disciplinary + methodological)
- Checker output supports 3 formats: PASSED, PASSED (with warnings), ISSUES FOUND
- TRAP-02 gate replaces old force/retry/abandon at revision loop exhaustion
- Planner agent knows how to emit search-strategy, criteria blocks, and tier attributes
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-plan-checker-enforcement/19-02-SUMMARY.md`
</output>
